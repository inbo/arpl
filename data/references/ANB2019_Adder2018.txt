vlaanderen_20m.asc

assign(adder_bwk, bwk(4030%, cg%))

// geschikte biotopen mogen maximaal op een onderlinge afstand van 1 meter liggen
assign(adder_bwk_cluster, fuzzyClusterIdU(adder_bwk, 1, meter))
deleteVariable(adder_bwk)

// leefgebied moet minstens 25 hectare groot zijn
assign(adder_leefgebied, cFilterGeArea(adder_bwk_cluster, 0.04, hectare))

// beperk de leefgebiedenkaart tot bepaalde ecoregio's
assign(adder_ecodistrict, Ge('ecoregio.asc', 1))
assign(adder_leefgebied_ecodistrict, and(adder_leefgebied, adder_ecodistrict))
deleteVariable(adder_ecodistrict)
deleteVariable(adder_leefgebied)

// individuele nummers geven aan clusters
//write('adder_leefgebied_ecodistrict_id.asc', if(adder_leefgebied_ecodistrict, adder_bwk_cluster, 0))
deleteVariable(adder_bwk_cluster)

// 0/1 kaarten maken
write('ANB2019_Adder2018.asc', IsNonZero(adder_leefgebied_ecodistrict))
deleteVariable(adder_leefgebied_ecodistrict)

exit

// baken geschikte biotopen af
assign(adder_bwk, bwk(cd%, 2310%, 4030%, ce%, 4010%, 7150%, cg%, cm%, cv%, k(cg%, k(cv%, k(cm%, k(cd%, k(ce%))

// geschikte biotopen mogen maximaal op een onderlinge afstand van 100 meter liggen
assign(adder_bwk_cluster, fuzzyClusterIdU(adder_bwk, 100, meter))
deleteVariable(adder_bwk)

// leefgebied moet minstens 25 hectare groot zijn
assign(adder_leefgebied, cFilterGeArea(adder_bwk_cluster, 25, hectare))

// beperk de leefgebiedenkaart tot bepaalde ecoregio's
assign(adder_ecodistrict, Ge('ecoregio.asc', 1))
assign(adder_leefgebied_ecodistrict, and(adder_leefgebied, adder_ecodistrict))
deleteVariable(adder_ecodistrict)
deleteVariable(adder_leefgebied)

// individuele nummers geven aan clusters
//write('adder_leefgebied_ecodistrict_id.asc', if(adder_leefgebied_ecodistrict, adder_bwk_cluster, 0))
deleteVariable(adder_bwk_cluster)

// 0/1 kaarten maken
write('ANB2019_Adder2018.asc', IsNonZero(adder_leefgebied_ecodistrict))
deleteVariable(adder_leefgebied_ecodistrict)
