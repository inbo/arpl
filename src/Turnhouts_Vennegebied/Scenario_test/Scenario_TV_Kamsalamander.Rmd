---
title: "Scenario Leefgebiedenscript: Kamsalamander"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
params:
  scenario_rds_path: "data/input/Scenario_rds/TV_Scenario_bosbehoudss_ss31fix_tvg_vrij_2026.rds"
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE,
  include = FALSE
)

library(knitr)
library(here)
# Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
knitr::opts_knit$set(root.dir = here::here())

library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}
```

# Soort en scenario informatie ophalen
```{r Soort informatie}
soort <- "kamsalamander"

# Strip eventuele relatieve klim-paden (../) als die toch worden meegegeven
p_raw <- gsub("^(\\.\\./)+", "", params$scenario_rds_path)
scenario_path <- here::here(p_raw)

if (!file.exists(scenario_path)) {
  stop(paste("❌ FOUT: Scenario RDS bestand NIET gevonden op:", scenario_path))
}

scen_volledig <- basename(scenario_path)
scenario_naam <- gsub("^TV_Scenario_|^Scenario_|.rds$", "", scen_volledig)

message(paste("Verwerken van soort:", soort, "binnen scenario:", scenario_naam))

df <- read_excel(here::here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
resultaat <- df %>% filter(tolower(trimws(Soort)) == soort) %>% select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

oppervlakte_ha <- resultaat$MinOpp_ha[1]
afstand_m      <- resultaat$AfstandBiotopen_m[1]
buffer_m       <- resultaat$Dispersiecap_m[1]

rm(df, resultaat)
```

```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Turnhouts_Vennegebied.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

# Inlezen van de CSV-sleutel (gebruikt om tabel-indices te mappen naar codes)
df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_TV <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

# ==============================================================================
# ULTRA-SNELLE METHODE: Direct naar data.frame met celnummers in C++
# ==============================================================================
id_raster_TV_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_TV_masked <- mask(id_raster_TV_globale_values, area_buffer_fix)

message("-> Vertaaltabel bliksemsnel opbouwen via C++ dataframe extractie...")

# 'cells = TRUE' zorgt ervoor dat terra direct een kolom 'cell' (lokale id) 
# en een kolom met de waarde (globale id) genereert voor alle niet-NA cellen.
df_extractie <- as.data.frame(id_raster_TV_masked, cells = TRUE)

# Direct omzetten naar data.table en de kolommen juist benoemen
vertaal_df <- as.data.table(df_extractie)

# De kolommen heten nu standaard 'cell' en de naam van je raster (lyr.1 of gelijkaardig)
# We hernoemen ze direct naar jouw vertrouwde structuur:
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))

# Filter eventuele resterende NA's eruit
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_TV) <- NA
template_TV          <- id_raster_TV * NA

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_TV_globale_values, id_raster_TV_masked)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(scenario_path)
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

# Initialiseer de lijsten voor de verschillende habitat-bouwstenen
lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE DYNAMISCH VIA DATA.TABLE
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    # 1. Ontsnap speciale regex-tekens (zoals haakjes) om syntaxfouten te voorkomen
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    
    # 2. CRUCIALE FIX: Plak een '^' voor elke individuele code. 
    # Dit dwingt grepl() om alleen te matchen als de BWK-code BEGINT met deze letters.
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    # Filteren: EXACT-matches (geen letters voor/na) OF codes die BEGINNEN met de bevat-termen
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_TV <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_cel_som <- tabel_TV[, .(Oppervlakte = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
  
  r_match_type <- id_raster_TV * NA
  r_opp_type   <- id_raster_TV * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

# Uitpakken naar de exacte losse variabelen (Naadloze match met je database)
waterbiotoop_bwk_max <- lijst_matches[["waterbiotoop_bwk"]]
waterbiotoop_bwk_opp <- lijst_oppervlaktes[["waterbiotoop_bwk"]]
landbiotoop_bwk_max <- lijst_matches[["landbiotoop_bwk"]]
landbiotoop_bwk_opp <- lijst_oppervlaktes[["landbiotoop_bwk"]]

# Grote eindschoonmaak (vertaal_df weggooien mag nu pas!)
rm(resultaten_gegroepeerd, df_nieuw, lijst_matches, lijst_oppervlaktes)
gc()
```

```{r Watervlakken en Kleine Waters}
message("-> Watervlakken shapefile lokaal inlezen...")

# 1. Lees de shapefile in als SpatVector
watervlakken_v <- vect(here("data/input/ASCI Files/watervlakken2024.shp"))

# 2. Zorg dat de projectie exact klopt met je mastergrid
watervlakken_proj <- project(watervlakken_v, crs(template_TV))

# --- STAP A: REPAREER JE BUFFER ---
message("-> Geometrie van de studiegebied-buffer herstellen...")
area_buffer_clean <- makeValid(area_buffer_fix)
area_buffer_clean <- aggregate(area_buffer_clean) 

# --- STAP B: REPAREER DE WATERVLAKKEN ---
message("-> Topologische fouten in de watervlakken herstellen...")
watervlakken_clean <- makeValid(watervlakken_proj)

# 3. GEOGRAFISCHE CROP
message("-> Watervlakken uitsnijden op studiegebied...")
watervlakken_TV <- crop(watervlakken_clean, area_buffer_clean)

# ==============================================================================
# SPOOR 1 & 2: RECHTSTREEKSE VRASTERISATIE & COMBINATIE (LOGICA HERSTELD)
# ==============================================================================
message("-> Open water verrasteren en direct combineren met BWK-water...")

# We verrasteren de volledige, ongefilterde watervlakken-laag. 
# Open water telt als fractie 1.0 (100% pixelbedekking)
r_open_water <- terra::rasterize(watervlakken_TV, template_TV, field = 1, background = NA)

# COMBINEREN VÓÓR HET FILTEREN (Jouw nieuwe logica):
# Spoor 1: Maximale binaire potentie samenvoegen
kamsalamander_water_gecombineerd_max <- terra::cover(waterbiotoop_bwk_max, r_open_water)

# Spoor 2: Werkelijke oppervlakte-fractie samenvoegen
kamsalamander_water_gecombineerd_opp <- terra::cover(waterbiotoop_bwk_opp, r_open_water)


# ==============================================================================
# CLUSTEREN EN OPPERVLAKTE-FILTERING OP HET TOTAALPLAATJE
# ==============================================================================
message("-> Gecombineerd water clusteren en filteren op totale oppervlakte (< 0.4 ha)...")

# Drempels definiëren (0.4 hectare = 4000 m²; Zoekafstand = 10m)
drempel_m2 <- 4000
zoekafstand_m <- 10

# We voeren de filtering uit op de gecombineerde laag.
# We voeden 'kamsalamander_water_gecombineerd_opp' als opp_laag zodat de werkelijke 
# fracties (m2) worden opgeteld binnen een cluster.
water_gefilterd_lijst <- cluster_filter_compleet(
  masker = kamsalamander_water_gecombineerd_max,
  opp_laag = kamsalamander_water_gecombineerd_opp,
  drempel_m2 = drempel_m2,
  dist_m = zoekafstand_m,
  werkelijk = TRUE
)

# De uiteindelijke, biologisch correct gefilterde waterlagen voor de kamsalamander:
# Spoor 1: Binaire eindlaag
kamsalamander_waterbiotoop1 <- water_gefilterd_lijst$raster

# Spoor 2: Fractie/oppervlakte eindlaag (als masker over de opp-laag leggen)
kamsalamander_waterbiotoop1_opp <- terra::mask(kamsalamander_water_gecombineerd_opp, kamsalamander_waterbiotoop1)

# ==============================================================================
# OPGEKUST EN KLAAR
# ==============================================================================
suppify <- suppressWarnings(
  rm(watervlakken_v, watervlakken_proj, area_buffer_clean, watervlakken_clean, watervlakken_TV, 
     r_open_water, kamsalamander_water_gecombineerd_max, kamsalamander_water_gecombineerd_opp, water_gefilterd_lijst)
)
gc()
```

```{r 5 buren}
# ==============================================================================
# 1. CLUSTERS IDENTIFICEREN & CONNECTIVITEITSCHECK (5 buren binnen 500m)
# ==============================================================================
message("-> Unieke clusters identificeren voor het waterbiotoop van de kamsalamander...")
r_water_clusters <- terra::patches(kamsalamander_waterbiotoop1, directions = 8, zeroAsNA = TRUE)

# Initialiseer de finale objecten als leeg (failsafe)
kamsalamander_waterbiotoop1_finaal_max <- template_TV * NA
watervlakken_poly <- NULL
goedgekeurde_clusters_poly <- NULL

if (!all(is.na(terra::values(r_water_clusters, mat=FALSE)))) {
  
  message("-> Unieke naburige clusters tellen via edge-to-edge vector buffers...")
  
  # CRUCIALE FIX: dissolve = FALSE zorgt ervoor dat losse poelen 
  # aparte objecten blijven en dus correct als 'buur' geteld kunnen worden!
  watervlakken_poly <- terra::as.polygons(r_water_clusters, dissolve = FALSE)
  
  if (!is.null(watervlakken_poly) && nrow(watervlakken_poly) > 0) {
    
    # 2. Trek een buffer van 500 meter rondom ELKE individuele poel
    poly_buffer <- terra::buffer(watervlakken_poly, width = 500)
    
    message("-> Relaties berekenen via terra::is.related...")
    # 3. Binaire intersectie checken (NxN matrix)
    intersect_matrix <- terra::is.related(poly_buffer, watervlakken_poly, "intersects")
    
    # Omzetten naar een zuivere matrix
    intersect_matrix <- matrix(intersect_matrix, nrow = nrow(poly_buffer), ncol = nrow(watervlakken_poly))
    
    # 4. Tel per rij hoeveel poelen er binnen de 500m buffer liggen (inclusief zichzelf)
    cluster_counts <- rowSums(intersect_matrix)
    
    # 5. Koppel counts terug
    watervlakken_poly$unieke_buren_count <- cluster_counts
    
    # 6. Filter: minimaal 5 andere poelen + zichzelf = 6
    goedgekeurde_clusters_poly <- watervlakken_poly[watervlakken_poly$unieke_buren_count >= 6, ]
    
    # ==============================================================================
    # FINALE RASTERISATIE
    # ==============================================================================
    if (nrow(goedgekeurde_clusters_poly) > 0) {
      message("-> Goedgekeurde kamsalamanderclusters terugzetten naar mastergrid...")
      kamsalamander_waterbiotoop1_finaal_max <- terra::rasterize(
        goedgekeurde_clusters_poly, 
        template_TV, 
        field = 1, 
        background = NA
      )
    } else {
      message("⚠️ Waarschuwing: Geen enkel cluster voldoet aan de connectiviteitseis van 5 buren.")
    }
    
    # Opruimen tijdelijke objecten binnen de check
    suppressWarnings(rm(poly_buffer, intersect_matrix, cluster_counts))
    
  } else {
    message("⚠️ Waarschuwing: Geen geometrieën gevonden na as.polygons().")
  }
} else {
  message("⚠️ Waarschuwing: Geen initiële waterbiotopen gevonden om de connectiviteitscheck op uit te voeren.")
}

# ==============================================================================
# 2. PARALLEL: WERKELIJKE OPPERVLAKTE BEREKENEN (_opp)
# ==============================================================================
message("-> Werkelijke oppervlakte filteren op basis van de connectiviteitscheck...")
kamsalamander_waterbiotoop1_finaal_opp <- terra::mask(
  kamsalamander_waterbiotoop1_opp, 
  kamsalamander_waterbiotoop1_finaal_max
)

cl_id_max <- terra::mask(r_water_clusters, kamsalamander_waterbiotoop1_finaal_max)

# [Ha-berekening en printen blijft exact hetzelfde als jouw code]

# ==============================================================================
# 3. HECTAREBEREKENING & PRINTEN
# ==============================================================================
ha_potentie  <- calc_ha_exact(kamsalamander_waterbiotoop1_finaal_max)
ha_werkelijk <- calc_ha_exact(kamsalamander_waterbiotoop1_finaal_opp)

cat("\n=========================================================\n")
cat("          RESULTATEN WATERBIOTOOP kamsalamander              \n")
cat("=========================================================\n")
cat(paste("Geschikt gebied (Maximale Potentie):  ", round(ha_potentie, 2), "ha\n"))
cat(paste("Geschikt gebied (Werkelijke Fractie): ", round(ha_werkelijk, 2), "ha\n"))
cat("=========================================================\n\n")

suppressWarnings(rm(r_water_clusters, watervlakken_poly, goedgekeurde_clusters_poly))
gc()
```

```{r visuele check water en land}
# ==============================================================================
# INTERACTIEVE KAART VOOR VISUELE CHECK
# ==============================================================================
message("-> Interactieve kaart opbouwen...")

# 1. Projecteer de rasters en de grens naar de web-projectie (WGS84 / EPSG:4326)
r_water_web <- terra::project(kamsalamander_waterbiotoop1_finaal_max, "EPSG:4326", method = "near")
r_land_web  <- terra::project(landbiotoop_bwk_max, "EPSG:4326", method = "near")

# 2. Kleurenpaletten definiëren (Semi-transparant voor goedgekeurde gebieden)
pal_water <- colorFactor(c("#0073C2"), domain = 1, na.color = "transparent") # Blauw voor water
pal_land  <- colorFactor(c("#228B22"), domain = 1, na.color = "transparent") # Bosgroen voor land

# 3. Leaflet kaart opbouwen
kaart <- leaflet() %>%
  # Achtergrondkaarten (Luchtfoto + Topografische kaart als opties)
  addProviderTiles(providers$OpenStreetMap, group = "OpenStreetMap") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Luchtfoto (Esri)") %>%
  
  # Studiegebied grens (Turnhouts Vennegebied)
  addPolygons(
    data = grens_web, 
    color = "black", 
    weight = 2, 
    fill = FALSE, 
    group = "Grens Studiegebied"
  ) %>%
  
  # Landbiotoop laag (Maximale Potentie)
  addRasterImage(
    r_land_web, 
    colors = pal_land, 
    opacity = 0.6, 
    group = "Goedgekeurd Landbiotoop (Groen)"
  ) %>%
  
  # Waterbiotoop laag (Gefilterd & Goedgekeurd)
  addRasterImage(
    r_water_web, 
    colors = pal_water, 
    opacity = 0.8, 
    group = "Goedgekeurd Waterbiotoop (Blauw)"
  ) %>%
  
  # Lagenbeheer (Rechtsboven aan/uit vinken)
  addLayersControl(
    baseGroups = c("OpenStreetMap", "Luchtfoto (Esri)"),
    overlayGroups = c("Grens Studiegebied", "Goedgekeurd Landbiotoop (Groen)", "Goedgekeurd Waterbiotoop (Blauw)"),
    options = layersControlOptions(collapsed = FALSE)
  )

# Kaart tonen in RStudio of het HTML-rapport
kaart
```


```{r landbiotoop koppeling 1}
# ==============================================================================
# 5. RUIMTELIJKE RELATIE WATER- EN LANDBIOTOOP (Failsafe Cluster 500m eis)
# ==============================================================================
message("-> Connectiviteit tussen water- en landbiotoop controleren op CLUSTERNIVEAU (500m)...")

# 1. FUZZY LANDCLUSTERS MAKEN (Cellen binnen 20m horen bij elkaar)
message("-> Fuzzy landclusters genereren (zoekstraal 20m, geen min. opp)...")
dist_lijm_m <- 20
temp_mask_land <- terra::buffer(landbiotoop_bwk_max, width = dist_lijm_m / 2)

r_land_clusters <- terra::patches(temp_mask_land, directions = 8, zeroAsNA = TRUE) %>% 
  terra::mask(landbiotoop_bwk_max)

# Vers watercluster-raster maken om spook-IDs uit te sluiten
r_water_clusters_vers <- terra::patches(kamsalamander_waterbiotoop1_finaal_max, directions = 8, zeroAsNA = TRUE)

# Initialiseer de eindrasters standaard als leeg
kamsalamander_landbiotoop_bwk_max <- template_TV * NA
kamsalamander_waterbiotoop3_max   <- template_TV * NA

# 2. BEREKEN DE 500M INVLOEDSZONES
message("-> 500 meter invloedszones berekenen...")
water_buffer_500m <- terra::distance(kamsalamander_waterbiotoop1_finaal_max) <= 500
land_buffer_500m  <- terra::distance(landbiotoop_bwk_max) <= 500

# 3. ROBUUSTE EXTRACTIE VIA TERRA::ZONAL (Failsafe methode voor cluster-IDs)
message("-> Unieke overlappende cluster-IDs extraheren via zonale statistieken...")

# We kijken welke land-clusters vallen in de water-buffer
stats_land <- terra::zonal(water_buffer_500m, r_land_clusters, fun = "max", na.rm = TRUE)
goedgekeurde_land_cluster_ids <- stats_land[[1]][stats_land[[2]] == 1]

# We kijken welke water-clusters vallen in de land-buffer
stats_water <- terra::zonal(land_buffer_500m, r_water_clusters_vers, fun = "max", na.rm = TRUE)
goedgekeurde_water_cluster_ids <- stats_water[[1]][stats_water[[2]] == 1]

# Opschonen van eventuele NA's
goedgekeurde_land_cluster_ids  <- goedgekeurde_land_cluster_ids[!is.na(goedgekeurde_land_cluster_ids)]
goedgekeurde_water_cluster_ids <- goedgekeurde_water_cluster_ids[!is.na(goedgekeurde_water_cluster_ids)]

# ==============================================================================
# RUIMTELIJKE FILTERING TOEPASSEN
# ==============================================================================
if (length(goedgekeurde_land_cluster_ids) > 0 && length(goedgekeurde_water_cluster_ids) > 0) {
  
  message(paste("-> Succes!", length(goedgekeurde_land_cluster_ids), "landclusters en", 
                length(goedgekeurde_water_cluster_ids), "waterclusters gekoppeld."))
  
  # 4. VOLLEDIGE CLUSTERS BEHOUDEN
  kamsalamander_landbiotoop_bwk_max <- r_land_clusters %in% goedgekeurde_land_cluster_ids
  kamsalamander_landbiotoop_bwk_max <- terra::ifel(kamsalamander_landbiotoop_bwk_max == 1, 1, NA)

  kamsalamander_waterbiotoop3_max   <- r_water_clusters_vers %in% goedgekeurde_water_cluster_ids
  kamsalamander_waterbiotoop3_max   <- terra::ifel(kamsalamander_waterbiotoop3_max == 1, 1, NA)
  
} else {
  message("⚠️ Waarschuwing: Geen ruimtelijke overlap gevonden via zonale extractie.")
}

# 5. PARALLEL: WERKELIJKE OPPERVLAKTES MASKEREN (_opp)
kamsalamander_landbiotoop_bwk_opp <- terra::mask(landbiotoop_bwk_opp, kamsalamander_landbiotoop_bwk_max)
kamsalamander_waterbiotoop3_opp   <- terra::mask(kamsalamander_waterbiotoop1_finaal_opp, kamsalamander_waterbiotoop3_max)

# 6. GEHEUGEN SCHOONMAKEN
rm(temp_mask_land, r_land_clusters, r_water_clusters_vers, water_buffer_500m, land_buffer_500m, 
   stats_land, stats_water, goedgekeurde_land_cluster_ids, goedgekeurde_water_cluster_ids)
gc()
```


```{r landbiotoop koppeling 2}
# ==============================================================================
# 6. FINALE COMBINATIE: LEEFGEBIED kamsalamander
# ==============================================================================
message("-> Finaal leefgebied kamsalamander samenstellen...")

# Maximale Potentie (Binaire kaart)
kamsalamander_leefgebied1_max <- kamsalamander_waterbiotoop3_max | kamsalamander_landbiotoop_bwk_max
kamsalamander_leefgebied1_max <- terra::ifel(kamsalamander_leefgebied1_max == 1, 1, NA)

# Werkelijke Oppervlakte (Veilige optelsom van fracties)
# Vervang NA door 0 voor de berekening
water_fractie_clean <- terra::ifel(is.na(kamsalamander_waterbiotoop3_opp), 0, kamsalamander_waterbiotoop3_opp)
land_fractie_clean  <- terra::ifel(is.na(kamsalamander_landbiotoop_bwk_opp), 0, kamsalamander_landbiotoop_bwk_opp)

som_fracties <- water_fractie_clean + land_fractie_clean

# Top af op max 1.0 (100% van een cel)
som_gemedieerd <- terra::clamp(som_fracties, upper = 1.0)

# CRUCIALE CORRECTIE: Maskeer het resultaat met de binaire kaart. 
# Alles wat buiten de max-potentie valt, wordt hierdoor onmiddellijk weer NA!
kamsalamander_leefgebied1_opp <- terra::mask(som_gemedieerd, kamsalamander_leefgebied1_max)

# ==============================================================================
# 7. HECTAREBEREKENING & EINDRAPPORTAGE
# ==============================================================================
ha_leefgebied_pot  <- calc_ha_exact(kamsalamander_leefgebied1_max)
ha_leefgebied_werk <- calc_ha_exact(kamsalamander_leefgebied1_opp)

cat("\n=========================================================\n")
cat("            EINDPOTENTIE LEEFGEBIED kamsalamander           \n")
cat("=========================================================\n")
cat(paste("Totale Leefgebied (Maximale Potentie):  ", round(ha_leefgebied_pot, 2), "ha\n"))
cat(paste("Totale Leefgebied (Werkelijke Fractie): ", round(ha_leefgebied_werk, 2), "ha\n"))
cat("=========================================================\n\n")

# Netjes opruimen van alle zware tussenrasters uit deze en de vorige stappen
suppressWarnings(
  rm(
    kamsalamander_waterbiotoop3_max, kamsalamander_landbiotoop_bwk_max,
    kamsalamander_waterbiotoop3_opp, kamsalamander_landbiotoop_bwk_opp,
    water_fractie_clean, land_fractie_clean, som_fracties
  )
)
gc()
```

```{r clusters}
# ==============================================================================
# 8. CLUSTERING & OPPERVLAKTEFILTER (Minimaal 50 ha leefgebied)
# ==============================================================================
message("-> Leefgebied clusters identificeren en filteren op minimale grootte (14 ha)...")

# 1. Maak binaire clusters aan (FuzzyClusterIdU met 500m vertalen we door eerst te bufferen)
# Geef het leefgebied een buffer van 250m (zodat twee gebieden binnen 500m van elkaar versmelten)
leefgebied_buffered <- terra::buffer(kamsalamander_leefgebied1_max, width = 250)
r_leefgebied_clusters <- terra::patches(leefgebied_buffered, directions = 8)

# Snijd de gevormde clusters terug uit op het originele scherpe leefgebied
r_scherpe_clusters <- terra::mask(r_leefgebied_clusters, kamsalamander_leefgebied1_max)

# 2. Bereken de exacte oppervlakte van elk uniek cluster
cluster_freq <- terra::freq(r_scherpe_clusters)

# Bereken de oppervlakte in ha (1 cel van 10x10m = 100m² = 0.01 ha)
cluster_freq$area_ha <- cluster_freq$count * 0.01

# 3. Selecteer de ID's van clusters die minstens 14 hectare groot zijn
goedgekeurde_cluster_ids <- cluster_freq$value[cluster_freq$area_ha >= 14]

# 4. Filter de rasters op basis van de goedgekeurde ID's
if (length(goedgekeurde_cluster_ids) > 0) {
  # Maak een binaire kaart van enkel de goedgekeurde clusters
  kamsalamander_leefgebied_max <- r_scherpe_clusters %in% goedgekeurde_cluster_ids
  kamsalamander_leefgebied_max <- terra::ifel(kamsalamander_leefgebied_max == 1, 1, NA)
  
  # Maskeer de parallelle fractie-kaart met de goedgekeurde clusters
  kamsalamander_leefgebied_opp <- terra::mask(kamsalamander_leefgebied1_opp, kamsalamander_leefgebied_max)
} else {
  message("⚠️ Waarschuwing: Geen enkel leefgebied-cluster voldoet aan de minimale grootte van 50 ha.")
  kamsalamander_leefgebied_max <- template_TV * NA
  kamsalamander_leefgebied_opp <- template_TV * NA
}

# Geheugen netjes opruimen
rm(leefgebied_buffered, r_leefgebied_clusters, r_scherpe_clusters, cluster_freq, goedgekeurde_cluster_ids)
gc()

# ==============================================================================
# 9. FINALE EINDRAPPORTAGE (HET COMPLETE PLAATJE)
# ==============================================================================
ha_finaal_pot  <- calc_ha_exact(kamsalamander_leefgebied_max)
ha_finaal_werk <- calc_ha_exact(kamsalamander_leefgebied_opp)

cat("\n=========================================================\n")
cat("          DEFINITIEF LEEFGEBIED kamsalamander (>= 14 ha)     \n")
cat("=========================================================\n")
cat(paste("Finaal Leefgebied (Maximale Potentie):  ", round(ha_finaal_pot, 2), "ha\n"))
cat(paste("Finaal Leefgebied (Werkelijke Fractie): ", round(ha_finaal_werk, 2), "ha\n"))
cat("=========================================================\n\n")
```


# Export & Transitiekaart
```{r Export}
scenario_raster_dir <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", scenario_naam)
if (!dir.exists(scenario_raster_dir)) dir.create(scenario_raster_dir, recursive = TRUE)

final_opp_scen <- NULL

if (exists("resB_strikt") && !is.null(resB_strikt)) {
  final_opp_scen <- max(c(resB_strikt$kern, resB_strikt$bouw), na.rm = TRUE)
} else if (exists("final_opp")) {
  final_opp_scen <- final_opp
} else if (exists("bruinekiekendief_leefgebied1_opp")) {
  final_opp_scen <- bruinekiekendief_leefgebied1_opp
} else {
  mogelijke_vars <- ls(pattern = "(_final_opp|_leefgebied1_opp|_finaal_opp)$")
  if (length(mogelijke_vars) > 0) {
    final_opp_scen <- get(mogelijke_vars[1])
  } else {
    final_opp_scen <- terra::rast(template_TV, vals = NA)
  }
}

if (!all(is.na(terra::values(final_opp_scen, mat = FALSE)))) {
  werkelijk_scenario_rast <- terra::ifel(!is.na(final_opp_scen) & final_opp_scen > 0, 1, NA)
} else {
  werkelijk_scenario_rast <- terra::rast(template_TV, vals = NA)
}

bestandsnaam <- paste0("TV_", scenario_naam, "_", soort, ".tif")
file_path_scen <- file.path(scenario_raster_dir, bestandsnaam)

export_rast <- terra::deepcopy(werkelijk_scenario_rast)
export_rast <- terra::mask(export_rast, area_buffer_fix)

if (all(is.na(terra::values(export_rast, mat = FALSE)))) {
  export_rast <- terra::ifel(is.na(export_rast), 0, 0)
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
} else {
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
}

writeRaster(export_rast, 
            filename = file_path_scen, 
            overwrite = TRUE, 
            gdal = c("COMPRESS=LZW"), 
            datatype = "INT1U")

message(paste("✓ Scenario-raster succesvol weggeschreven naar:", file_path_scen))

referentie_file <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", "02_Werkelijke_Oppervlakte", paste0("Habitat_Werkelijke_Oppervlaktes_", soort, ".tif"))

if (file.exists(referentie_file)) {
  r_ref <- rast(referentie_file) %>% terra::resample(template_TV, method = "near")
} else {
  r_ref <- terra::rast(template_TV, vals = NA)
}

r_scen <- werkelijk_scenario_rast

r_rgb <- terra::ifel(!is.na(r_ref) & is.na(r_scen), 1, NA)
r_rgb <- terra::cover(r_rgb, terra::ifel(is.na(r_ref) & !is.na(r_scen), 2, NA))
r_rgb <- terra::cover(r_rgb, terra::ifel(!is.na(r_ref) & !is.na(r_scen), 3, NA))

r_rgb_web <- terra::project(terra::as.int(r_rgb), "EPSG:4326", method = "near")

kleuren_rgb <- c("#E74C3C", "#2ECC71", "#3498DB") 
pal_rgb <- colorFactor(palette = kleuren_rgb, levels = 1:3, na.color = "transparent")

leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  addRasterImage(r_rgb_web, colors = pal_rgb, opacity = 0.7, group = "Scenario Vergelijking") %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens TV") %>%
  addLegend(
    colors = kleuren_rgb,
    labels = c("Verloren gegaan leefgebied", "Nieuw bijgekomen leefgebied", "Status Quo (Behouden)"),
    title = paste("Scenario vs Referentie:", scenario_naam),
    position = "bottomright"
  ) %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Scenario Vergelijking", "Grens TV"),
    options = layersControlOptions(collapsed = FALSE)
  )
```
