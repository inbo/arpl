---
title: "Leefgebiedenscript in R: Turnhouts Vennegebied"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)

library(here)
# CRUCIALE FIX: Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
# Hierdoor werken relatieve paden met hier() overal hetzelfde.
knitr::opts_knit$set(root.dir = here())

library(knitr)
library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

# Exacte hectare-berekening behouden (cellSize voor 100% precisie)
calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

# Geoptimaliseerde get_stats helper
get_stats <- function(cat_id, label) {
  cid <- cat_id
  target_mask <- r_status == cid
  f_pix <- terra::freq(target_mask)
  n_pix <- if(nrow(f_pix) > 0) sum(f_pix$count[f_pix$value == 1], na.rm=TRUE) else 0
  area_ha <- (n_pix * 100) / 10000
  
  cl_src <- if(cid %in% c(1, 2)) cl_id_max else cl_id_opp
  cl_zone <- terra::mask(cl_src, target_mask)
  f_cl <- terra::freq(cl_zone)
  n_cl <- if(!is.null(f_cl) && nrow(f_cl) > 0) nrow(f_cl) else 0
  
  return(data.frame(Type = label, Clusters = n_cl, Oppervlakte_ha = round(area_ha, 2)))
}

maak_kaart_laag <- function(res_list) {
  if(is.null(res_list)) return(NULL)
  kernen <- terra::ifel(!is.na(res_list$kern), 1, NA)
  if(!is.null(res_list$bouw)) {
    bouw <- terra::ifel(!is.na(res_list$bouw), 2, NA)
    finaal <- terra::cover(kernen, bouw)
  } else {
    finaal = kernen
  }
  return(finaal)
}
```

# Soort informatie ophalen
```{r Soort informatie}
df <- read_excel(here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
soort <- "boomkikker"

resultaat <- df %>%
filter(Soort == soort) %>%
select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

# Variabelen definiëren
oppervlakte_ha <- resultaat$MinOpp_ha
afstand_m <- resultaat$AfstandBiotopen_m
buffer_m <- 1000 #resultaat$Dispersiecap_m

print(resultaat)
rm(df, resultaat)
```

# Gebied voorbereiden (Turnhouts Vennegebied)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Turnhouts_Vennegebied.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

# Inlezen van de CSV-sleutel (gebruikt om tabel-indices te mappen naar codes)
df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

# Vertaalraster bouwen (Zonder de zware TIF cube in te laden!)
message("-> Vertaalraster voor globale/lokale cellen opbouwen...")
id_raster_TV <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)
id_raster_TV_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")

extractie_ids <- terra::extract(id_raster_TV_globale_values, area_buffer_fix, cells = TRUE)

vertaal_df <- as.data.table(extractie_ids)
setnames(vertaal_df, c("cell", names(id_raster_TV_globale_values)), c("lokale_id", "globale_id"))
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_TV) <- NA
template_TV          <- id_raster_TV * NA

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_TV_globale_values)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# Biotoopfiltering (BWK-codes)
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(here("data/input/Raster_Vlaanderen/BWK_Tabel_Turnhout_Plus_50km.rds"))
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    # 1. Ontsnap speciale regex-tekens (zoals haakjes) om syntaxfouten te voorkomen
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    
    # 2. CRUCIALE FIX: Plak een '^' voor elke individuele code. 
    # Dit dwingt grepl() om alleen te matchen als de BWK-code BEGINT met deze letters.
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    # Filteren: EXACT-matches (geen letters voor/na) OF codes die BEGINNEN met de bevat-termen
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_TV <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_cel_som <- tabel_TV[, .(Oppervlakte = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
  
  r_match_type <- id_raster_TV * NA
  r_opp_type   <- id_raster_TV * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

# Uitpakken naar de exacte losse variabelen (Naadloze match met je database)
waterbiotoop_bwk_max <- lijst_matches[["waterbiotoop_bwk"]]
waterbiotoop_bwk_opp <- lijst_oppervlaktes[["waterbiotoop_bwk"]]
landbiotoop_hms_max <- lijst_matches[["landbiotoop_hms"]]
landbiotoop_hms_opp <- lijst_oppervlaktes[["landbiotoop_hms"]]
landbiotoop_k_max <- lijst_matches[["landbiotoop_k"]]
landbiotoop_k_opp <- lijst_oppervlaktes[["landbiotoop_k"]]
akkers_max <- lijst_matches[["akkers"]]
akkers_opp <- lijst_oppervlaktes[["akkers"]]

# Een back-up maken voor functies verderop die refereren naar raster_simpel_final
raster_simpel_final <- id_raster_TV

rm(tabel_vlaanderen, vertaal_df, lijst_matches, lijst_oppervlaktes)
gc()
```

```{r Watervlakken en Kleine Waters}
message("-> Watervlakken shapefile lokaal inlezen...")

# 1. Lees de shapefile in als SpatVector
watervlakken_v <- vect(here("data/input/ASCI Files/watervlakken2024.shp"))

# 2. Zorg dat de projectie exact klopt met je mastergrid
watervlakken_proj <- project(watervlakken_v, crs(template_TV))

# --- STAP A: REPAREER JE BUFFER ---
message("-> Geometrie van de studiegebied-buffer herstellen...")
area_buffer_clean <- makeValid(area_buffer_fix)
area_buffer_clean <- aggregate(area_buffer_clean) 

# --- STAP B: REPAREER DE WATERVLAKKEN ---
message("-> Topologische fouten in de watervlakken herstellen...")
watervlakken_clean <- makeValid(watervlakken_proj)

# 3. GEOGRAFISCHE CROP: Nu met twee gegarandeerd schone vectorlagen!
message("-> Watervlakken uitsnijden op studiegebied...")
watervlakken_TV <- crop(watervlakken_clean, area_buffer_clean)

# ==============================================================================
# FUZZY CLUSTERING (10m zoekstraal) IN VECTOR-OMGEVING
# ==============================================================================
message("-> Fuzzy clustering uitvoeren via 10m vector-aggregatie...")

# Geef elke polygoon een tijdelijke buffer van 5 meter
watervlakken_buffered <- buffer(watervlakken_TV, width = 5)

# Voeg overlappende buffers samen
watervlakken_fuzzy_clusters <- aggregate(watervlakken_buffered, by = NULL)

# Bereken de exacte oppervlakte in m² van de clusters
watervlakken_fuzzy_clusters$fuzzy_area_m2 <- terra::expanse(watervlakken_fuzzy_clusters)

# Filter de clusters die in totaal kleiner of gelijk zijn aan 0.4 ha (4000 m²)
kleine_clusters_v <- watervlakken_fuzzy_clusters[watervlakken_fuzzy_clusters$fuzzy_area_m2 <= 4000, ]

# ==============================================================================
# RASTERISATIE & COMBINATIE (Aangepast voor Boomkikker)
# ==============================================================================
message("-> Filterresultaat verrasteren en combineren met BWK voor boomkikker...")

if (nrow(kleine_clusters_v) > 0) {
  
  # Snijd de oorspronkelijke, scherpe watervlakken uit op basis van deze clusters
  kleine_watervlakken_finaal <- crop(watervlakken_TV, kleine_clusters_v)
  
  if (nrow(kleine_watervlakken_finaal) > 0) {
    # Verraster de shapefile: open water telt als 1 (volledige fractie / 100% potentie)
    r_boomkikker_kleinwater <- terra::rasterize(kleine_watervlakken_finaal, template_TV, field = 1, background = NA)
    
    # 1. Combineren met de Maximale Potentie (binaire cover)
    r_boomkikker_waterbiotoop1 <- terra::cover(waterbiotoop_bwk_max, r_boomkikker_kleinwater)
    
    # 2. Combineren met de Werkelijke Oppervlakte (fractie cover)
    r_boomkikker_waterbiotoop1_opp <- terra::cover(waterbiotoop_bwk_opp, r_boomkikker_kleinwater)
  } else {
    message("⚠️ Waarschuwing: Crop van kleine waters leverde 0 rijen op.")
    r_boomkikker_waterbiotoop1     <- waterbiotoop_bwk_max
    r_boomkikker_waterbiotoop1_opp <- waterbiotoop_bwk_opp
  }
} else {
  message("⚠️ Waarschuwing: Geen kleine waters gevonden in deze zone.")
  r_boomkikker_waterbiotoop1     <- waterbiotoop_bwk_max
  r_boomkikker_waterbiotoop1_opp <- waterbiotoop_bwk_opp
}

# Netjes opruimen van het geheugen (veilig door suppressWarnings)
suppressWarnings(
  rm(watervlakken_v, watervlakken_proj, area_buffer_clean, watervlakken_clean, watervlakken_TV, 
     watervlakken_buffered, watervlakken_fuzzy_clusters, kleine_clusters_v, kleine_watervlakken_finaal, r_boomkikker_kleinwater)
)
gc()
```

```{r 5 buren}
# ==============================================================================
# 1. CLUSTERS IDENTIFICEREN & FILTEREN (Boomkikker Metapopulatie-logica)
# ==============================================================================
message("-> Unieke clusters identificeren voor het waterbiotoop van de boomkikker...")
r_water_clusters <- terra::patches(r_boomkikker_waterbiotoop1, directions = 8)

# GEOPTIMALISEERD & VEILIG: via matrix-vermenigvuldiging.
message("-> BLIKSEMSNEL: Unieke naburige clusters tellen via vector-centroids...")

# 1. Zet het cluster-raster om naar polygonen (1 polygoon per cluster)
watervlakken_poly <- terra::as.polygons(r_water_clusters, dissolve = TRUE)

# 2. Neem het exacte middelpunt (centroid) van elk cluster
centroids <- terra::centroids(watervlakken_poly)

# 3. Trek een buffer van 500 meter rondom de centroids
centroids_buffer <- terra::buffer(centroids, width = 500)

# 4. Tel hoeveel centroids er binnen elke 500m buffer vallen (intersect)
# 'terra::relate' geeft een matrix van welke buffers welke centroids raken
intersect_matrix <- terra::relate(centroids_buffer, centroids, "intersects")

# 5. Tel per rij hoeveel TRUE's er zijn (dit is inclusief de eigen poel!)
cluster_counts <- rowSums(intersect_matrix)

# 6. Koppel deze counts terug aan de polygonen
watervlakken_poly$unieke_buren_count <- cluster_counts

# 7. Selecteer alleen de polygonen die aan de eis voldoen.
# Eis: minstens 5 ANDERE poelen. Omdat de eigen poel ook meetsnijdt, zoeken we naar >= 6.
goedgekeurde_clusters_poly <- watervlakken_poly[watervlakken_poly$unieke_buren_count >= 6, ]

# ==============================================================================
# FINALE RASTERISATIE (Boomkikker Waterbiotoop Max Potentie)
# ==============================================================================
message("-> Goedgekeurde boomkikkerclusters terugzetten naar mastergrid...")

if (nrow(goedgekeurde_clusters_poly) > 0) {
  # Verraster de goedgekeurde polygonen direct terug naar een binaire '1'
  boomkikker_waterbiotoop1_finaal_max <- terra::rasterize(
    goedgekeurde_clusters_poly, 
    template_TV, 
    field = 1, 
    background = NA
  )
} else {
  message("⚠️ Waarschuwing: Geen enkel cluster voldoet aan de connectiviteitseis voor boomkikker.")
  boomkikker_waterbiotoop1_finaal_max <- template_TV * NA
}

# Netjes opruimen van de vector-tussenstappen
rm(watervlakken_poly, centroids, centroids_buffer, intersect_matrix, cluster_counts, goedgekeurde_clusters_poly)
gc()

# ==============================================================================
# 2. PARALLEL: WERKELIJKE OPPERVLAKTE BEREKENEN (_opp)
# ==============================================================================
message("-> Werkelijke oppervlakte filteren op basis van de connectiviteitscheck...")

# We maskeren het samengestelde oppervlakteraster (BWK + watervlakken fracties) voor de boomkikker
boomkikker_waterbiotoop1_finaal_opp <- terra::mask(
  r_boomkikker_waterbiotoop1_opp, 
  boomkikker_waterbiotoop1_finaal_max
)

# ==============================================================================
# 3. HECTAREBEREKENING & PRINTEN
# ==============================================================================
# Bereken de exacte hectares met jouw eigen functie
ha_potentie  <- calc_ha_exact(boomkikker_waterbiotoop1_finaal_max)
ha_werkelijk <- calc_ha_exact(boomkikker_waterbiotoop1_finaal_opp)

# Netjes en overzichtelijk printen in de console/HTML output
cat("\n=========================================================\n")
cat("          RESULTATEN WATERBIOTOOP BOOMKIKKER             \n")
cat("=========================================================\n")
cat(paste("Geschikt gebied (Maximale Potentie):  ", round(ha_potentie, 2), "ha\n"))
cat(paste("Geschikt gebied (Werkelijke Fractie): ", round(ha_werkelijk, 2), "ha\n"))
cat("=========================================================\n\n")

# Opruimen van de resterende raster-tussenstappen
suppressWarnings(
  rm(r_water_clusters)
)
gc()
```

```{r landbiotoop opbouw}
# ==============================================================================
# 4. LANDBIOTOOP INTERACTIE & VARIATIE (HMS vs K)
# ==============================================================================
message("-> Ruimtelijke variatie tussen HMS en K landbiotopen berekenen...")

# 1. BEREKEN AFSTANDEN (op basis van maximale aanwezigheid)
dist_k   <- terra::distance(landbiotoop_k_max)
dist_hms <- terra::distance(landbiotoop_hms_max)

# 2. RUIMTELIJKE FILTERS (MAX)
# HMS binnen 500m van K, en K binnen 500m van HMS
landbiotoop_bwk1_max <- landbiotoop_hms_max == 1 & dist_k <= 500
landbiotoop_bwk2_max <- landbiotoop_k_max == 1   & dist_hms <= 500

# 3. PARALLELLE FRACTIONEEL FILTERING (OPP)
# Maskeer de werkelijke fracties met de ruimtelijk goedgekeurde cellen
landbiotoop_bwk1_opp <- terra::mask(landbiotoop_hms_opp, terra::ifel(landbiotoop_bwk1_max, 1, NA))
landbiotoop_bwk2_opp <- terra::mask(landbiotoop_k_opp, terra::ifel(landbiotoop_bwk2_max, 1, NA))

# 4. COMBINEER DE BIOTOOPTYPES (OR / COVER LOGIEK)
# Maximale Potentie (Binaire kaart)
landbiotoop_bwk3_max <- landbiotoop_bwk1_max | landbiotoop_bwk2_max

# Werkelijke Oppervlakte (Fractie kaart via cover, zodat overlap netjes overschrijft)
landbiotoop_bwk3_opp <- terra::cover(landbiotoop_bwk1_opp, landbiotoop_bwk2_opp)

# 5. AKKERS UITSLUITEN
# We maken één binaire maskerlaag voor waar GEEN akkers liggen
akkers_mask <- terra::ifel(is.na(akkers_max), 1, NA)

# Pas het masker toe op beide rasters
landbiotoop_na_max <- terra::mask(landbiotoop_bwk3_max, akkers_mask)
landbiotoop_na_opp <- terra::mask(landbiotoop_bwk3_opp, akkers_mask)

# 6. GEHEUGEN SCHOONMAKEN
rm(
  dist_k, dist_hms, 
  landbiotoop_bwk1_max, landbiotoop_bwk2_max, landbiotoop_bwk3_max,
  landbiotoop_bwk1_opp, landbiotoop_bwk2_opp, landbiotoop_bwk3_opp,
  akkers_mask
)
gc()
```


```{r landbiotoop koppeling 1}
# ==============================================================================
# 5. RUIMTELIJKE RELATIE WATER- EN LANDBIOTOOP (500 meter eis)
# ==============================================================================
message("-> Connectiviteit tussen water- en landbiotoop controleren (500m)...")

# 1. BEREKEN AFSTANDEN (op basis van maximale aanwezigheid)
dist_water_finaal <- terra::distance(boomkikker_waterbiotoop1_finaal_max)
dist_land_na      <- terra::distance(landbiotoop_na_max)

# 2. RUIMTELIJKE FILTERS (MAX)
# A. Landbiotoop moet aanwezig zijn binnen de 500m van het geschikte waterbiotoop
boomkikker_landbiotoop1_max <- landbiotoop_na_max == 1 & dist_water_finaal <= 500

# B. Waterbiotoop moet aanwezig zijn binnen de 500m van het geschikte landbiotoop
boomkikker_waterbiotoop3_max <- boomkikker_waterbiotoop1_finaal_max == 1 & dist_land_na <= 500

# 3. PARALLEL: WERKELIJKE OPPERVLAKTES MASKEREN (_opp)
# We gebruiken de binaire *_max rasters als masker over onze parallelle fractie-rasters
boomkikker_landbiotoop1_opp  <- terra::mask(landbiotoop_na_opp, terra::ifel(boomkikker_landbiotoop1_max, 1, NA))
boomkikker_waterbiotoop3_opp <- terra::mask(boomkikker_waterbiotoop1_finaal_opp, terra::ifel(boomkikker_waterbiotoop3_max, 1, NA))

# 4. GEHEUGEN SCHOONMAKEN
rm(dist_water_finaal, dist_land_na, landbiotoop_na_max, landbiotoop_na_opp)
gc()
```


```{r landbiotoop koppeling 2}
# ==============================================================================
# 6. FINALE COMBINATIE: LEEFGEBIED BOOMKIKKER
# ==============================================================================
message("-> Finaal leefgebied boomkikker samenstellen...")

# Maximale Potentie (Binaire kaart)
boomkikker_leefgebied1_max <- boomkikker_waterbiotoop3_max | boomkikker_landbiotoop1_max
boomkikker_leefgebied1_max <- terra::ifel(boomkikker_leefgebied1_max == 1, 1, NA)

# Werkelijke Oppervlakte (Veilige optelsom van fracties)
# Vervang NA door 0 voor de berekening
water_fractie_clean <- terra::ifel(is.na(boomkikker_waterbiotoop3_opp), 0, boomkikker_waterbiotoop3_opp)
land_fractie_clean  <- terra::ifel(is.na(boomkikker_landbiotoop1_opp), 0, boomkikker_landbiotoop1_opp)

som_fracties <- water_fractie_clean + land_fractie_clean

# Top af op max 1.0 (100% van een cel)
som_gemedieerd <- terra::clamp(som_fracties, upper = 1.0)

# CRUCIALE CORRECTIE: Maskeer het resultaat met de binaire kaart. 
# Alles wat buiten de max-potentie valt, wordt hierdoor onmiddellijk weer NA!
boomkikker_leefgebied1_opp <- terra::mask(som_gemedieerd, boomkikker_leefgebied1_max)

# ==============================================================================
# 7. HECTAREBEREKENING & EINDRAPPORTAGE
# ==============================================================================
ha_leefgebied_pot  <- calc_ha_exact(boomkikker_leefgebied1_max)
ha_leefgebied_werk <- calc_ha_exact(boomkikker_leefgebied1_opp)

cat("\n=========================================================\n")
cat("            EINDPOTENTIE LEEFGEBIED BOOMKIKKER           \n")
cat("=========================================================\n")
cat(paste("Totale Leefgebied (Maximale Potentie):  ", round(ha_leefgebied_pot, 2), "ha\n"))
cat(paste("Totale Leefgebied (Werkelijke Fractie): ", round(ha_leefgebied_werk, 2), "ha\n"))
cat("=========================================================\n\n")

# Netjes opruimen van alle zware tussenrasters uit deze en de vorige stappen
suppressWarnings(
  rm(
    boomkikker_waterbiotoop3_max, boomkikker_landbiotoop1_max,
    boomkikker_waterbiotoop3_opp, boomkikker_landbiotoop1_opp,
    water_fractie_clean, land_fractie_clean, som_fracties
  )
)
gc()
```

```{r clusters}
# ==============================================================================
# 8. CLUSTERING & OPPERVLAKTEFILTER (Minimaal 5 ha leefgebied)
# ==============================================================================
message("-> Leefgebied clusters identificeren en filteren op minimale grootte (5 ha)...")

# 1. Maak binaire clusters aan (FuzzyClusterIdU met 500m vertalen we door eerst te bufferen)
# Geef het leefgebied een buffer van 250m (zodat twee gebieden binnen 500m van elkaar versmelten)
leefgebied_buffered <- terra::buffer(boomkikker_leefgebied1_max, width = 250)
r_leefgebied_clusters <- terra::patches(leefgebied_buffered, directions = 8)

# Snijd de gevormde clusters terug uit op het originele scherpe leefgebied
r_scherpe_clusters <- terra::mask(r_leefgebied_clusters, boomkikker_leefgebied1_max)

# 2. Bereken de exacte oppervlakte van elk uniek cluster
cluster_freq <- terra::freq(r_scherpe_clusters)

# Bereken de oppervlakte in ha (1 cel van 10x10m = 100m² = 0.01 ha)
cluster_freq$area_ha <- cluster_freq$count * 0.01

# 3. Selecteer de ID's van clusters die minstens 5 hectare groot zijn
goedgekeurde_cluster_ids <- cluster_freq$value[cluster_freq$area_ha >= 5.0]

# 4. Filter de rasters op basis van de goedgekeurde ID's
if (length(goedgekeurde_cluster_ids) > 0) {
  # Maak een binaire kaart van enkel de goedgekeurde clusters
  boomkikker_leefgebied_max <- r_scherpe_clusters %in% goedgekeurde_cluster_ids
  boomkikker_leefgebied_max <- terra::ifel(boomkikker_leefgebied_max == 1, 1, NA)
  
  # Maskeer de parallelle fractie-kaart met de goedgekeurde clusters
  boomkikker_leefgebied_opp <- terra::mask(boomkikker_leefgebied1_opp, boomkikker_leefgebied_max)
} else {
  message("⚠️ Waarschuwing: Geen enkel leefgebied-cluster voldoet aan de minimale grootte van 5 ha.")
  boomkikker_leefgebied_max <- template_TV * NA
  boomkikker_leefgebied_opp <- template_TV * NA
}

# Geheugen netjes opruimen
rm(leefgebied_buffered, r_leefgebied_clusters, r_scherpe_clusters, cluster_freq, goedgekeurde_cluster_ids)
gc()

# ==============================================================================
# 9. FINALE EINDRAPPORTAGE (HET COMPLETE PLAATJE)
# ==============================================================================
ha_finaal_pot  <- calc_ha_exact(boomkikker_leefgebied_max)
ha_finaal_werk <- calc_ha_exact(boomkikker_leefgebied_opp)

cat("\n=========================================================\n")
cat("          DEFINITIEF LEEFGEBIED BOOMKIKKER (>= 5 ha)     \n")
cat("=========================================================\n")
cat(paste("Finaal Leefgebied (Maximale Potentie):  ", round(ha_finaal_pot, 2), "ha\n"))
cat(paste("Finaal Leefgebied (Werkelijke Fractie): ", round(ha_finaal_werk, 2), "ha\n"))
cat("=========================================================\n\n")
```

```{r waarnemingen inlezen en ARPL}
# ==============================================================================
# STAP 12: ACTUELE VERSPREIDING & ARPL OP BASIS VAN FINALE CLUSTERS (>= 5 HA)
# ==============================================================================
message("-> Start ARPL en Actuele Verspreiding analyse met waarnemingen voor boomkikker...")

# 1. Pad naar het waarnemingsbestand van de boomkikker
waarnemingen_path <- here("data/input/Waarnemingen_Soorten/Waarnemingen_Boomkikker.csv")

# 2. Inlezen en bufferen van waarnemingen
if (file.exists(waarnemingen_path)) {
  waarnemingen_raw    <- read_delim(waarnemingen_path, delim = ";", escape_double = FALSE, trim_ws = TRUE)
  df_points_sf        <- st_as_sf(waarnemingen_raw, coords = c("x", "y"), crs = 31370)
  waarneming_smear_sf <- st_buffer(df_points_sf, dist = buffer_m) %>% st_union() %>% st_sf()
  waarneming_blobs_vect  <- vect(waarneming_smear_sf)
  waarneming_punten_vect <- vect(df_points_sf) 
  has_observations    <- TRUE
  message("-> Waarnemingsbestand succesvol geladen.")
} else {
  message(paste("⚠️ Waarschuwing: Geen waarnemingsbestand gevonden op pad:", waarnemingen_path))
  df_points_sf        <- st_sf(st_sfc(crs = 31370)) 
  waarneming_smear_sf <- st_sf(st_sfc(crs = 31370))
  waarneming_blobs_vect  <- vect(st_sf(st_sfc(crs = 31370)))
  waarneming_punten_vect <- vect(st_sf(st_sfc(crs = 31370)))
  has_observations    <- FALSE
}

# Initialiseer de finale rasters, tellers en hectares voor de boomkikker
boomkikker_arpl_opp        <- terra::rast(template_TV, vals = NA)
boomkikker_verspreiding_bin <- terra::rast(template_TV, vals = NA) 
arpl_netwerk_count         <- 0
verspreiding_netwerk_count <- 0
ha_verspreiding_bin        <- 0

ha_pot_bin <- ha_pot_buit <- ha_werk_bin <- ha_werk_buit <- ha_arpl <- 0
ids_pot_bin <- ids_pot_buit <- ids_werk_bin <- ids_werk_buit <- ids_actueel_relevant <- c()

if (!all(is.na(terra::values(boomkikker_leefgebied_max, mat=FALSE)))) {
  
  message("-> Metapopulatie-netwerkwolken bouwen (500m fuzzy koppeling)...")
  # 1. Vectoriseer het ruwe leefgebied
  p_ruw_max <- terra::as.polygons(boomkikker_leefgebied_max, aggregate = TRUE) %>% terra::disagg()
  
  # 2. Bouw de netwerkwolken via 250m buffer-smeltmethode (zorgt voor de 500m fuzzy brug)
  p_buf_max        <- terra::buffer(p_ruw_max, width = 250)
  p_net_max_wolken <- terra::aggregate(p_buf_max) %>% terra::disagg()
  p_net_max_wolken$Netwerk_ID <- 1:nrow(p_net_max_wolken)
  
  # Koppel de Netwerk-IDs terug aan de originele scherpe deelelementen
  p_ruw_max$Netwerk_ID <- terra::relate(p_ruw_max, p_net_max_wolken, "within") %>% 
    apply(1, function(x) { if(any(x)) which(x)[1] else NA })
  
  if (nrow(p_ruw_max) > 0) {
    # 3. Maak het perfect sluitende netwerk-ID raster aan via onze kogelvrije methode
    r_opp_net_ids <- boomkikker_leefgebied_max * NA
    r_opp_net_ids <- terra::rasterize(p_ruw_max, r_opp_net_ids, field = "Netwerk_ID", touches = FALSE)
    
    missende_cellen <- !is.na(boomkikker_leefgebied_max) & is.na(r_opp_net_ids)
    if (terra::global(missende_cellen, "sum", na.rm = TRUE)[[1]] > 0) {
      cellen_indices <- which(terra::values(missende_cellen, mat = FALSE))
      extracted_ids <- terra::extract(p_ruw_max, terra::xyFromCell(r_opp_net_ids, cellen_indices))
      if (nrow(extracted_ids) > 0 && "Netwerk_ID" %in% colnames(extracted_ids)) {
        r_opp_net_ids[cellen_indices] <- extracted_ids$Netwerk_ID
      }
    }
    
    # --- ZUIVERE VECTOR-CHECK VOOR BINNEN/BUITEN STUDIEGEBIED ---
    relatie_matrix <- terra::relate(p_net_max_wolken, area_shape_proj, "intersects")
    ids_binnen <- p_net_max_wolken$Netwerk_ID[apply(relatie_matrix, 1, any)]
    ids_buiten <- setdiff(p_net_max_wolken$Netwerk_ID, ids_binnen)
    
    # --- EXTRACTION A: ARPL ---
    if (has_observations && nrow(df_points_sf) > 0) {
      extract_ids_arpl <- terra::extract(r_opp_net_ids, waarneming_blobs_vect)
      ids_netwerken_raken_arpl <- unique(na.omit(extract_ids_arpl[[2]]))
    } else {
      ids_netwerken_raken_arpl <- c()
    }
    ids_actueel_relevant <- intersect(ids_binnen, ids_netwerken_raken_arpl)
    
    if(length(ids_actueel_relevant) > 0) {
      m_arpl <- r_opp_net_ids %in% ids_actueel_relevant
      m_arpl_tv <- terra::crop(terra::ifel(m_arpl == 1, 1, NA), template_TV)
      boomkikker_arpl_opp <- terra::mask(terra::crop(boomkikker_leefgebied_opp, template_TV), m_arpl_tv)
      arpl_netwerk_count <- length(ids_actueel_relevant)
      ha_arpl <- calc_ha_exact(boomkikker_arpl_opp)
    }
    
    # --- EXTRACTION B: ACTUELE VERSPREIDING ---
    if (has_observations && nrow(df_points_sf) > 0) {
      extract_ids_verspr <- terra::extract(r_opp_net_ids, waarneming_punten_vect)
      ids_netwerken_raken_punten <- unique(na.omit(extract_ids_verspr[[2]]))
    } else {
      ids_netwerken_raken_punten <- c()
    }
    ids_verspreiding_clusters <- intersect(ids_binnen, ids_netwerken_raken_punten)
    
    if(length(ids_verspreiding_clusters) > 0) {
      m_verspr <- r_opp_net_ids %in% ids_verspreiding_clusters
      clusters_actueel <- terra::mask(boomkikker_leefgebied_opp, terra::ifel(m_verspr == 1, 1, NA))
      
      clusters_actueel_tv <- terra::crop(clusters_actueel, template_TV)
      boomkikker_verspreiding_bin <- terra::ifel(!is.na(clusters_actueel_tv) & clusters_actueel_tv > 0, 1, NA)
      verspreiding_netwerk_count <- length(ids_verspreiding_clusters)
      ha_verspreiding_bin <- calc_ha_exact(clusters_actueel_tv)
    }
    
    # --- TABEL METRICS BEREKENEN ---
    r_max_tv <- terra::crop(boomkikker_leefgebied_max, template_TV)
    r_opp_tv <- terra::crop(boomkikker_leefgebied_opp, template_TV)
    r_net_ids_tv <- terra::crop(r_opp_net_ids, template_TV)
    
    calc_ha_grutto_style <- function(ids, target_raster) {
      if(length(ids) == 0) return(0)
      masker <- r_net_ids_tv %in% ids
      r_gefilterd <- terra::mask(target_raster, terra::ifel(masker == 1, 1, NA))
      return(round(calc_ha_exact(r_gefilterd), 2))
    }
    
    ids_pot_bin  <- ids_binnen
    ids_pot_buit <- ids_buiten
    ids_werk_bin  <- ids_binnen
    ids_werk_buit <- ids_buiten
    
    ha_pot_bin   <- calc_ha_grutto_style(ids_pot_bin, r_max_tv)
    ha_pot_buit  <- calc_ha_grutto_style(ids_pot_buit, r_max_tv)
    ha_werk_bin  <- calc_ha_grutto_style(ids_werk_bin, r_opp_tv)
    ha_werk_buit <- calc_ha_grutto_style(ids_werk_buit, r_opp_tv)
    
    rm(p_ruw_max, p_buf_max, p_net_max_wolken, relatie_matrix)
  }
} else {
  stats_max_raw <- data.frame(value = c(), count = c())
}

# ==============================================================================
# PIXEL STATUSKAART OPBOUWEN
# ==============================================================================
message("-> Statuskaart opbouwen via stabiele raster-resampling...")
r_status <- terra::rast(template_TV, vals = NA)

if (exists("r_net_ids_tv") && !all(is.na(terra::values(r_net_ids_tv, mat=FALSE)))) {
  r_net_ids_tv_sync <- terra::resample(r_net_ids_tv, template_TV, method = "near")
  r_opp_tv_sync     <- terra::resample(r_opp_tv, template_TV, method = "near")
  
  if(length(ids_pot_bin) > 0 || length(ids_pot_buit) > 0) {
    m_pot_bin   <- !is.na(terra::match(r_net_ids_tv_sync, ids_pot_bin))
    m_pot_buit  <- !is.na(terra::match(r_net_ids_tv_sync, ids_pot_buit))
    
    r_status <- terra::ifel(m_pot_buit, 2, r_status)
    r_status <- terra::ifel(m_pot_bin, 1, r_status)
    r_status <- terra::ifel(m_pot_buit & r_opp_tv_sync > 0, 4, r_status)
    r_status <- terra::ifel(m_pot_bin & r_opp_tv_sync > 0, 3, r_status)
  }
  rm(r_net_ids_tv_sync, r_opp_tv_sync)
}

# ==============================================================================
# 4. PRINT DE INTEGRALE CRITERIA TABEL
# ==============================================================================
tabel_totaal <- data.frame(
  Type = c("Maximaal Potentieel (Binnen)", "Maximaal Potentieel (Buiten)", 
           "Werkelijk Habitat (Binnen)", "Werkelijk Habitat (Buiten)",
           "Actueel Relevant (ARPL - Reële ha)", "Actuele Verspreiding (Werkelijke ha)"),
  Clusters = c(length(ids_pot_bin), length(ids_pot_buit), 
               length(ids_werk_bin), length(ids_werk_buit), 
               arpl_netwerk_count, verspreiding_netwerk_count),
  Oppervlakte_ha = c(ha_pot_bin, ha_pot_buit, ha_werk_bin, ha_werk_buit, round(ha_arpl, 2), round(ha_verspreiding_bin, 2))
)

tabel_totaal %>% 
  knitr::kable(caption = paste("Totaaloverzicht habitat & verspreiding:", soort)) %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover"))
```

```{r, export}
# --- EXPORT MET KOGELVRIJE NUL-OPVULLING & CONTRACT FIX (BOOMKIKKER) ---
message("-> Start geformatteerde export met nul-opvulling voor boomkikker...")

base_dir <- here("data/output/Turnhouts_Vennegebied/Rasters_Soorten")
folders <- list(
  potentie     = file.path(base_dir, "01_Maximale_Potentie"),
  werkelijk    = file.path(base_dir, "02_Werkelijke_Oppervlakte"),
  arpl         = file.path(base_dir, "03_ARPL"),
  verspreiding = file.path(base_dir, "04_Actuele_Verspreiding")
)
purrr::walk(folders, ~if (!dir.exists(.x)) dir.create(.x, recursive = TRUE))

# 1. Spoor B: Werkelijke Oppervlakte formatteren naar 1/NA
if(!all(is.na(terra::values(boomkikker_leefgebied_opp, mat=FALSE)))) {
  werkelijk_export_rast <- terra::ifel(!is.na(boomkikker_leefgebied_opp) & boomkikker_leefgebied_opp > 0, 1, NA)
} else {
  werkelijk_export_rast <- terra::rast(template_TV, vals = NA)
}

# 2. ARPL formatteren naar 1/NA
if(exists("boomkikker_arpl_opp") && !all(is.na(terra::values(boomkikker_arpl_opp, mat=FALSE)))) {
  arpl_export_rast <- terra::ifel(boomkikker_arpl_opp > 0, 1, NA)
} else {
  arpl_export_rast <- terra::rast(template_TV, vals = NA)
}

# 3. Actuele Verspreiding formatteren naar 1/NA
if(!all(is.na(terra::values(boomkikker_verspreiding_bin, mat=FALSE)))) {
  verspreiding_export_rast <- terra::ifel(boomkikker_verspreiding_bin > 0, 1, NA)
} else {
  verspreiding_export_rast <- terra::rast(template_TV, vals = NA)
}

# 4. De vernieuwde export_config bundel voor boomkikker
export_config <- list(
  list(rast = boomkikker_leefgebied_max,   naam = "Maximale_Potentie",       folder = folders$potentie,     type = "INT1U"),
  list(rast = werkelijk_export_rast,       naam = "Werkelijke_Oppervlaktes", folder = folders$werkelijk,    type = "INT1U"), 
  list(rast = arpl_export_rast,            naam = "ARPL",                    folder = folders$arpl,         type = "INT1U"),
  list(rast = verspreiding_export_rast,    naam = "Actuele_Verspreiding",    folder = folders$verspreiding, type = "INT1U")
)

# De export-loop voegt netjes een nullen-omranding toe op de 50km buffer
for(item in export_config) {
  file_path <- file.path(item$folder, paste0("Habitat_", item$naam, "_", soort, ".tif"))
  
  export_rast <- terra::deepcopy(item$rast)
  export_rast <- terra::mask(export_rast, area_buffer_fix)
  
  if(all(is.na(terra::values(export_rast, mat=FALSE)))) {
    export_rast <- terra::ifel(is.na(export_rast), 0, 0)
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  } else {
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  }
  
  writeRaster(export_rast, 
              filename = file_path, 
              overwrite = TRUE, 
              gdal = c("COMPRESS=LZW"), 
              datatype = item$type)
              
  message(paste("   [OK] Geëxporteerd:", basename(file_path)))
}

# Finale grote schoonmaak van het RAM-geheugen om RStudio fris te houden
rm(
  werkelijk_export_rast, arpl_export_rast, verspreiding_export_rast,
  export_config, export_rast
)
gc()

message("🏁 SCRIPT EN EXPORT VOLLEDIG EN SUCCESVOL AFGEROND VOOR BOOMKIKKER!")
```

```{r Interactieve kaart}
# ==============================================================================
# STAP 14: INTERACTIEVE KAART (LEAFLET - INCLUSIEF VOORTPLANTINGSLAAAG BOOMKIKKER)
# ==============================================================================
if (exists("r_net_ids_tv") && !all(is.na(terra::values(r_net_ids_tv, mat=FALSE)))) {
  
  message("-> Clusterstatistieken berekenen op basis van het TOTALE, gedeelde netwerk voor boomkikker...")
  
  # We berekenen de frequentie en hectares op het BREDE, ongecroppte netwerkraster
  stats_global_freq <- terra::freq(r_opp_net_ids)
  colnames(stats_global_freq) <- c("layer", "ID", "count")
  
  stats_global_opp <- terra::zonal(boomkikker_leefgebied_opp * 0.01, r_opp_net_ids, fun = "sum", na.rm = TRUE)
  colnames(stats_global_opp) <- c("ID", "Totaal_Netwerk_Werk_ha")
  
  # Bereken ook hoeveel daarvan specifiek BINNEN TV ligt
  stats_tv_opp <- terra::zonal(r_opp_tv * 0.01, r_net_ids_tv, fun = "sum", na.rm = TRUE)
  colnames(stats_tv_opp) <- c("ID", "Binnen_TV_Werk_ha")
  
  cluster_info <- data.frame(
      ID = stats_global_freq$ID, 
      Tot_Pot_ha = stats_global_freq$count * 0.01
    ) %>%
    left_join(stats_global_opp, by = "ID") %>%
    left_join(stats_tv_opp, by = "ID") %>%
    mutate(
      Zone = "Binnen TV / Grensoverschrijdend",
      ARPL = ID %in% ids_actueel_relevant,
      Type = ifelse(ID %in% ids_pot_bin, "Werkelijk Habitat", "Potentieel (Niet Werkelijk)"),
      Popup = paste0(
        "<div style='font-family: Arial; min-width: 200px;'>",
        "<strong>Netwerk ID (Clusternummer):</strong> ", ID, "<br>",
        "<strong>Soort:</strong> ", tools::toTitleCase(soort), "<br>",
        "<strong>Zone:</strong> ", Zone, "<br><hr>",
        "<strong>Status:</strong> ", Type, "<br>",
        "<strong>Actueel Relevant (ARPL):</strong> ", 
        ifelse(ARPL, "<span style='color:green; font-weight:bold;'>JA (Populatielaag)</span>", 
                     "<span style='color:red;'>NEE (Geen waarneming)</span>"), "<br><br>",
        "<strong>Totaal Potentieel Netwerk (Gedeeld):</strong> ", round(Tot_Pot_ha, 2), " ha<br>",
        "<strong>Totaal Werkelijk Habitat (Gedeeld):</strong> ", round(coalesce(Totaal_Netwerk_Werk_ha, 0), 2), " ha<br><hr>",
        "<span style='font-size:11px; color:#555;'><strong>Waarvan binnen TV grens:</strong> ", 
        round(coalesce(Binnen_TV_Werk_ha, 0), 2), " ha netto</span>",
        "</div>"
      )
    )

  # 2. Vectoriseren van de exact gecroppte netwerk-ids naar Polygonen voor Leaflet
  cl_poly <- terra::as.polygons(r_net_ids_tv, aggregate = TRUE) %>% 
    st_as_sf() %>% 
    st_transform(4326)

  colnames(cl_poly)[1] <- "ID" 
  cl_poly_final <- cl_poly %>%
    inner_join(cluster_info, by = "ID")
    
} else {
  cl_poly_final <- st_sf(st_sfc(crs = 4326))
}

# 3. Projecteer rasters en vectoren naar WGS84 voor Leaflet
r_web_status <- terra::project(r_status, "EPSG:4326", method = "near")
bbox         <- st_bbox(grens_web)

# AANGEPAST VOOR BOOMKIKKER: Projecteer het goedgekeurde voortplantingsbiotoop (indien aanwezig)
if(exists("boomkikker_waterbiotoop1_finaal_max") && 
   !all(is.na(terra::values(boomkikker_waterbiotoop1_finaal_max, mat=FALSE)))) {
  r_web_vpt <- terra::project(boomkikker_waterbiotoop1_finaal_max, "EPSG:4326", method = "near")
} else {
  r_web_vpt <- NULL
}

# Projectie van waarnemingen van de boomkikker (indien aanwezig)
if(has_observations && nrow(df_points_sf) > 0) {
  waarneming_smear_web <- st_transform(waarneming_smear_sf, 4326)
  points_web           <- st_transform(df_points_sf, 4326)
} else {
  waarneming_smear_web <- st_sf(st_sfc(crs = 4326))
  points_web           <- st_sf(st_sfc(crs = 4326))
}

# 4. Kleurenpaletten definiëren
pal_status <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = c("Werkelijk Habitat", "Potentieel (Niet Werkelijk)"))
pal_4      <- colorFactor(palette = c("#FF00FF", "#FF8C00", "#4daf4a", "#377eb8"), levels = 1:4, na.color = "transparent")

# 5. Bouw de Leaflet Kaart
map <- leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

# Voeg status pixels toe
if(!all(is.na(terra::values(r_web_status, mat=FALSE)))) {
  map <- map %>% addRasterImage(terra::as.int(r_web_status), colors = pal_4, opacity = 0.4, group = "Habitat Status (Pixels)")
}

# VOEG VOORTPLANTINGSWATEREN BOOMKIKKER TOE (Felblauw raster)
if(!is.null(r_web_vpt)) {
  map <- map %>% addRasterImage(terra::as.int(r_web_vpt), colors = "#00BFFF", opacity = 0.8, group = "Voortplantingsbiotoop (Poelen/Waters)")
}

# Voeg Polygonen en Waarnemingen toe
map <- map %>%
  addPolygons(
    data = cl_poly_final,
    fillColor = ~pal_status(Type),
    fillOpacity = ~ifelse(ARPL, 0.6, 0.1), 
    color = ~pal_status(Type), 
    weight = ~ifelse(ARPL, 3, 0.5), 
    opacity = 1,
    popup = ~Popup,
    group = "Leefgebieden (Clusters)"
  )

if(has_observations && nrow(df_points_sf) > 0) {
  map <- map %>%
    addPolygons(
      data = waarneming_smear_web,  
      color = "#377eb8", weight = 2, fill = FALSE,
      dashArray = "10, 5", group = "Dispersiebereik (Smear)"
    ) %>%
    addCircles(
      data = points_web, 
      color = "black", weight = 1, fillColor = "yellow", fillOpacity = 1,
      radius = 15, group = "Waarnemingen"
    )
}

# Grens en Controls toevoegen (Aangepast met de nieuwe overlayGroup)
map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Leefgebieden (Clusters)", "Habitat Status (Pixels)", 
                      "Voortplantingsbiotoop (Poelen/Waters)", "Dispersiebereik (Smear)", 
                      "Waarnemingen", "Grens Studiegebied"),
    options = layersControlOptions(collapsed = FALSE)
  )
```