---
title: "Scenario Leefgebiedenscript: Gevlektewitsnuitlibel"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
params:
  scenario_rds_path: "data/input/Scenario_rds/TV_Scenario_bosbehoudss_ss31fix_tvg_vrij_2026.rds"
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE,
  include = FALSE
)

library(knitr)
library(here)
# Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
knitr::opts_knit$set(root.dir = here::here())

library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}
```

# Soort en scenario informatie ophalen
```{r Soort informatie}
soort <- "gevlektewitsnuitlibel"

# Strip eventuele relatieve klim-paden (../) als die toch worden meegegeven
p_raw <- gsub("^(\\.\\./)+", "", params$scenario_rds_path)
scenario_path <- here::here(p_raw)

if (!file.exists(scenario_path)) {
  stop(paste("❌ FOUT: Scenario RDS bestand NIET gevonden op:", scenario_path))
}

scen_volledig <- basename(scenario_path)
scenario_naam <- gsub("^TV_Scenario_|^Scenario_|.rds$", "", scen_volledig)

message(paste("Verwerken van soort:", soort, "binnen scenario:", scenario_naam))

df <- read_excel(here::here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
resultaat <- df %>% filter(tolower(trimws(Soort)) == soort) %>% select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

oppervlakte_ha <- resultaat$MinOpp_ha[1]
afstand_m      <- resultaat$AfstandBiotopen_m[1]
buffer_m       <- resultaat$Dispersiecap_m[1]

rm(df, resultaat)
```

```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Turnhouts_Vennegebied.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

# Inlezen van de CSV-sleutel (gebruikt om tabel-indices te mappen naar codes)
df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_TV <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

# ==============================================================================
# ULTRA-SNELLE METHODE: Direct naar data.frame met celnummers in C++
# ==============================================================================
id_raster_TV_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_TV_masked <- mask(id_raster_TV_globale_values, area_buffer_fix)

message("-> Vertaaltabel bliksemsnel opbouwen via C++ dataframe extractie...")

# 'cells = TRUE' zorgt ervoor dat terra direct een kolom 'cell' (lokale id) 
# en een kolom met de waarde (globale id) genereert voor alle niet-NA cellen.
df_extractie <- as.data.frame(id_raster_TV_masked, cells = TRUE)

# Direct omzetten naar data.table en de kolommen juist benoemen
vertaal_df <- as.data.table(df_extractie)

# De kolommen heten nu standaard 'cell' en de naam van je raster (lyr.1 of gelijkaardig)
# We hernoemen ze direct naar jouw vertrouwde structuur:
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))

# Filter eventuele resterende NA's eruit
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_TV) <- NA
template_TV          <- id_raster_TV * NA

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_TV_globale_values, id_raster_TV_masked)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(scenario_path)
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

# Initialiseer de lijsten voor de verschillende habitat-bouwstenen
lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE DYNAMISCH VIA DATA.TABLE
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    # 1. Ontsnap speciale regex-tekens (zoals haakjes) om syntaxfouten te voorkomen
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    
    # 2. CRUCIALE FIX: Plak een '^' voor elke individuele code. 
    # Dit dwingt grepl() om alleen te matchen als de BWK-code BEGINT met deze letters.
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    # Filteren: EXACT-matches (geen letters voor/na) OF codes die BEGINNEN met de bevat-termen
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_TV <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_cel_som <- tabel_TV[, .(Oppervlakte = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
  
  r_match_type <- id_raster_TV * NA
  r_opp_type   <- id_raster_TV * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

# Uitpakken naar de exacte losse variabelen (Naadloze match met je database)
voortplanting_bwk_max <- lijst_matches[["voortplanting_bwk"]]
voortplanting_bwk_opp <- lijst_oppervlaktes[["voortplanting_bwk"]]
land_nabij_max <- lijst_matches[["land_nabij"]]
land_nabij_opp <- lijst_oppervlaktes[["land_nabij"]]
land_ruim_max <- lijst_matches[["land_ruim"]]
land_ruim_opp <- lijst_oppervlaktes[["land_ruim"]]
struweel1_max <- lijst_matches[["struweel1"]]
struweel1_opp <- lijst_oppervlaktes[["struweel1"]]


# Een back-up maken voor functies verderop die refereren naar raster_simpel_final
raster_simpel_final <- id_raster_TV

# Grote eindschoonmaak (vertaal_df weggooien mag nu pas!)
rm(resultaten_gegroepeerd, df_nieuw, lijst_matches, lijst_oppervlaktes)
gc()
```

# Voortplantingsbiotoop
```{r Watervlakken en Kleine Waters}
message("-> Watervlakken shapefile lokaal inlezen...")

# 1. Lees de shapefile in als SpatVector
watervlakken_v <- vect(here("data/input/ASCI Files/watervlakken2024.shp"))

# 2. Zorg dat de projectie exact klopt met je mastergrid
watervlakken_proj <- project(watervlakken_v, crs(template_TV))

# --- STAP A: REPAREER JE BUFFER ---
message("-> Geometrie van de studiegebied-buffer herstellen...")
area_buffer_clean <- makeValid(area_buffer_fix)
area_buffer_clean <- aggregate(area_buffer_clean) 

# --- STAP B: REPAREER DE WATERVLAKKEN ---
message("-> Topologische fouten in de watervlakken herstellen...")
watervlakken_clean <- makeValid(watervlakken_proj)

# 3. GEOGRAFISCHE CROP: Nu met twee gegarandeerd schone vectorlagen!
message("-> Watervlakken uitsnijden op studiegebied...")
watervlakken_TV <- crop(watervlakken_clean, area_buffer_clean)

# ==============================================================================
# FUZZY CLUSTERING (10m zoekstraal) IN VECTOR-OMGEVING
# ==============================================================================
message("-> Fuzzy clustering uitvoeren via 10m vector-aggregatie...")

# Geef elke polygoon een tijdelijke buffer van 5 meter
watervlakken_buffered <- buffer(watervlakken_TV, width = 5)

# Voeg overlappende buffers samen
watervlakken_fuzzy_clusters <- aggregate(watervlakken_buffered, by = NULL)

# Bereken de exacte oppervlakte in m² van de clusters
watervlakken_fuzzy_clusters$fuzzy_area_m2 <- terra::expanse(watervlakken_fuzzy_clusters)

# Filter de clusters die in totaal kleiner of gelijk zijn aan 0.02 ha (200 m²)
kleine_clusters_v <- watervlakken_fuzzy_clusters[watervlakken_fuzzy_clusters$fuzzy_area_m2 <= 200, ]

# ==============================================================================
# RASTERISATIE & COMBINATIE (Nu foutveilig en parallel voor _max en _opp)
# ==============================================================================
message("-> Filterresultaat verrasteren en combineren met BWK...")

if (nrow(kleine_clusters_v) > 0) {
  
  # Snijd de oorspronkelijke, scherpe watervlakken uit op basis van deze clusters
  kleine_watervlakken_finaal <- crop(watervlakken_TV, kleine_clusters_v)
  
  if (nrow(kleine_watervlakken_finaal) > 0) {
    # Verraster de shapefile: open water telt als 1 (volledige fractie / 100% potentie)
    r_witsnuit_kleinwater <- terra::rasterize(kleine_watervlakken_finaal, template_TV, field = 1, background = NA)
    
    # 1. Combineren met de Maximale Potentie (binaire cover)
    r_witsnuit_waterbiotoop1 <- terra::cover(voortplanting_bwk_max, r_witsnuit_kleinwater)
    
    # 2. Combineren met de Werkelijke Oppervlakte (fractie cover)
    r_witsnuit_waterbiotoop1_opp <- terra::cover(voortplanting_bwk_opp, r_witsnuit_kleinwater)
  } else {
    message("⚠️ Waarschuwing: Crop van kleine waters leverde 0 rijen op.")
    r_witsnuit_waterbiotoop1     <- voortplanting_bwk_max
    r_witsnuit_waterbiotoop1_opp <- voortplanting_bwk_opp
  }
} else {
  message("⚠️ Waarschuwing: Geen kleine waters gevonden in deze zone.")
  r_witsnuit_waterbiotoop1     <- voortplanting_bwk_max
  r_witsnuit_waterbiotoop1_opp <- voortplanting_bwk_opp
}

# Netjes opruimen van het geheugen (veilig door suppressWarnings)
suppressWarnings(
  rm(watervlakken_v, watervlakken_proj, area_buffer_clean, watervlakken_clean, watervlakken_TV, 
     watervlakken_buffered, watervlakken_fuzzy_clusters, kleine_clusters_v, kleine_watervlakken_finaal, r_witsnuit_kleinwater)
)
gc()
```

```{r 3 Buren}
# ==============================================================================
# 1. CLUSTERS IDENTIFICEREN & FILTEREN (Bestaande binaire logiek)
# ==============================================================================
message("-> Unieke clusters identificeren voor het waterbiotoop...")
r_water_clusters <- terra::patches(r_witsnuit_waterbiotoop1, directions = 8)

# GEOPTIMALISEERD & VEILIG: omzeil trage R-functies in focal() via matrix-vermenigvuldiging.
# Dit voorkomt geheugen-crashes op grote 10m grids.
message("-> BLIKSEMSNEL: Unieke naburige clusters tellen via vector-centroids...")

# 1. Bereken de oppervlakte per cluster (om later de binaire '1' terug te zetten)
# en zet het cluster-raster om naar polygonen (1 polygoon per cluster)
watervlakken_poly <- terra::as.polygons(r_water_clusters, dissolve = TRUE)

# 2. Neem het exacte middelpunt (centroid) van elk cluster
centroids <- terra::centroids(watervlakken_poly)

# 3. Trek een buffer van 200 meter rondom de centroids
centroids_buffer <- terra::buffer(centroids, width = 200)

# 4. Tel hoeveel centroids er binnen elke 200m buffer vallen (intersect)
# 'terra::relate' geeft een matrix van welke buffers welke centroids raken
intersect_matrix <- terra::relate(centroids_buffer, centroids, "intersects")

# 5. Tel per rij hoeveel TRUE's er zijn (dit is het aantal unieke clusters binnen 500m)
cluster_counts <- rowSums(intersect_matrix)

# 6. Koppel deze counts terug aan de polygonen
watervlakken_poly$unieke_buren_count <- cluster_counts

# 7. Selecteer alleen de polygonen die aan de eis voldoen (count >= 4)
goedgekeurde_clusters_poly <- watervlakken_poly[watervlakken_poly$unieke_buren_count >= 4, ]

# ==============================================================================
# FINALE RASTERISATIE (Vervangt gevlektewitsnuitlibel_voortplanting_max)
# ==============================================================================
message("-> Goedgekeurde clusters terugzetten naar mastergrid...")

if (nrow(goedgekeurde_clusters_poly) > 0) {
  # Verraster de goedgekeurde polygonen direct terug naar een binaire '1'
  gevlektewitsnuitlibel_voortplanting_max <- terra::rasterize(
    goedgekeurde_clusters_poly, 
    template_TV, 
    field = 1, 
    background = NA
  )
} else {
  message("⚠️ Waarschuwing: Geen enkel cluster voldoet aan de connectiviteitseis.")
  gevlektewitsnuitlibel_voortplanting_max <- template_TV * NA
}

# Netjes opruimen van de vector-tussenstappen
rm(watervlakken_poly, centroids, centroids_buffer, intersect_matrix, cluster_counts, goedgekeurde_clusters_poly)
gc()
# ==============================================================================
# 2. PARALLEL: WERKELIJKE OPPERVLAKTE BEREKENEN (_opp)
# ==============================================================================
message("-> Werkelijke oppervlakte filteren op basis van de connectiviteitscheck...")

# We maskeren het samengestelde oppervlakteraster (BWK + watervlakken fracties)
gevlektewitsnuitlibel_voortplanting_opp <- terra::mask(
  r_witsnuit_waterbiotoop1_opp, 
  gevlektewitsnuitlibel_voortplanting_max
)

# ==============================================================================
# 3. HECTAREBEREKENING & PRINTEN
# ==============================================================================
# Bereken de exacte hectares met jouw eigen functie
ha_potentie  <- calc_ha_exact(gevlektewitsnuitlibel_voortplanting_max)
ha_werkelijk <- calc_ha_exact(gevlektewitsnuitlibel_voortplanting_opp)

# Netjes en overzichtelijk printen in de console/HTML output
cat("\n=========================================================\n")
cat("   RESULTATEN VOORPLANTINGSGEBIED GEVLEKTEWITSNUITLIBEL  \n")
cat("=========================================================\n")
cat(paste("Geschikt gebied (Maximale Potentie):  ", round(ha_potentie, 2), "ha\n"))
cat(paste("Geschikt gebied (Werkelijke Fractie): ", round(ha_werkelijk, 2), "ha\n"))
cat("=========================================================\n\n")

# Opruimen van de tussenstappen
rm(r_water_clusters, venster, r_unieke_clusters_count)
gc()
```

```{r Foerageergebied Afbakenen}
# ==============================================================================
# STAP 1: LAND_NABIJ CLUSTERS (MIN. 5 HA & MIN. 30% BOS/STRUWEEL)
# ==============================================================================
message("-> 1. Land_nabij clusters filteren op min. 5 ha en 10m overbrugging...")

# Spoor A: Maximale Potentie (land_nabij_max)
# Focal met 3x3 matrix (1 cel rand rondom = max 10m overbrugging tussen cellen)
r_grow_nabij_max <- terra::focal(land_nabij_max, w = 3, fun = "max", na.rm = TRUE)
cl_id_nabij_max  <- terra::patches(r_grow_nabij_max, directions = 8, zeroAsNA = TRUE)
cl_id_nabij_max  <- terra::mask(cl_id_nabij_max, land_nabij_max)

# Oppervlakteberekening per land_nabij cluster over de buffer
stats_nabij_max <- terra::zonal(land_nabij_max * 0.01, cl_id_nabij_max, fun = "sum", na.rm = TRUE)
colnames(stats_nabij_max) <- c("Cluster_ID", "Potentie_ha")

# Filter op minimaal 5 ha
ids_nabij_5ha_max <- stats_nabij_max$Cluster_ID[stats_nabij_max$Potentie_ha >= 5]

if(length(ids_nabij_5ha_max) > 0) {
  mask_nabij_5ha <- cl_id_nabij_max %in% ids_nabij_5ha_max
  land_nabij_5ha_max <- terra::mask(land_nabij_max, terra::ifel(mask_nabij_5ha, 1, NA))
  cl_id_5ha_max      <- terra::mask(cl_id_nabij_max, land_nabij_5ha_max)
  
  # --- BOSEIS CHECK (MIN. 30% BOS/STRUWEEL) ---
  message("-> Controle op minstens 30% bos/struweel binnen land_nabij clusters...")
  struweel_in_nabij <- terra::mask(struweel1_max, cl_id_5ha_max)
  
  stats_struweel <- terra::zonal(struweel_in_nabij * 0.01, cl_id_5ha_max, fun = "sum", na.rm = TRUE)
  colnames(stats_struweel) <- c("Cluster_ID", "Struweel_ha")
  
  df_bos_check <- merge(stats_nabij_max[stats_nabij_max$Cluster_ID %in% ids_nabij_5ha_max, ], 
                        stats_struweel, by = "Cluster_ID", all.x = TRUE)
  df_bos_check$Struweel_ha[is.na(df_bos_check$Struweel_ha)] <- 0
  df_bos_check$Pct_Bos <- (df_bos_check$Struweel_ha / df_bos_check$Potentie_ha) * 100
  
  ids_nabij_bos_ok <- df_bos_check$Cluster_ID[df_bos_check$Pct_Bos >= 30]
  
  if(length(ids_nabij_bos_ok) > 0) {
    land_nabij_gefilterd_max <- terra::mask(land_nabij_5ha_max, 
                                            terra::ifel(cl_id_5ha_max %in% ids_nabij_bos_ok, 1, NA))
  } else {
    message("⚠️ Geen enkel land_nabij cluster voldoet aan de 30% boseis.")
    land_nabij_gefilterd_max <- template_TV * NA
  }
} else {
  message("⚠️ Geen land_nabij clusters van >= 5 ha gevonden.")
  land_nabij_gefilterd_max <- template_TV * NA
}

# Clean-up tussenstappen
rm(r_grow_nabij_max, cl_id_nabij_max, stats_nabij_max, struweel_in_nabij, stats_struweel)
gc()

# ==============================================================================
# STAP 2: LAND_RUIM COMPLEXEN (MIN. 30 HA, MAX. 50M AFSTAND)
# ==============================================================================
message("-> 2. Land_ruim complexen opbouwen (min. 30 ha, 50m overbrugging)...")

# Focal met 11x11 window (5 cellen rand rondom = 50m overbrugging)
r_grow_ruim_max <- terra::focal(land_ruim_max, w = 11, fun = "max", na.rm = TRUE)
cl_id_ruim_max  <- terra::patches(r_grow_ruim_max, directions = 8, zeroAsNA = TRUE)
cl_id_ruim_max  <- terra::mask(cl_id_ruim_max, land_ruim_max)

stats_ruim_max <- terra::zonal(land_ruim_max * 0.01, cl_id_ruim_max, fun = "sum", na.rm = TRUE)
colnames(stats_ruim_max) <- c("Cluster_ID", "Potentie_ha")

ids_ruim_30ha_max <- stats_ruim_max$Cluster_ID[stats_ruim_max$Potentie_ha >= 30]

if(length(ids_ruim_30ha_max) > 0) {
  land_ruim_30ha_max <- terra::mask(land_ruim_max, terra::ifel(cl_id_ruim_max %in% ids_ruim_30ha_max, 1, NA))
  
  # --- CHECK: LIGGEN DE LAND_NABIJ CLUSTERS BINNEN DIT 30HA COMPLEX? ---
  message("-> Land_nabij clusters toetsen aan 30ha land_ruim complexen...")
  
  # Maak een buffer van 50m rond het 30ha ruim-complex om ruimtelijke overlap/nabijheid correct op te vangen
  ruim_complex_zone <- terra::focal(land_ruim_30ha_max, w = 11, fun = "max", na.rm = TRUE)
  
  land_nabij_in_ruim_max <- terra::mask(land_nabij_gefilterd_max, ruim_complex_zone)
} else {
  message("⚠️ Geen land_ruim complexen van >= 30 ha gevonden.")
  land_nabij_in_ruim_max <- template_TV * NA
}

rm(r_grow_ruim_max, cl_id_ruim_max, stats_ruim_max)
gc()

# ==============================================================================
# STAP 3: FINALE SYNTHESE & KOPPELING WATER <-> LAND (MAX 10M AFSTAND)
# ==============================================================================
message("-> 3. Finale koppeling: Water en Land_nabij moeten binnen 10m van elkaar liggen...")

if(terra::hasValues(gevlektewitsnuitlibel_voortplanting_max) && 
   terra::hasValues(land_nabij_in_ruim_max)) {
  
  # Patches maken om unieke water- en landclusters te kunnen selecteren
  cl_water <- terra::patches(gevlektewitsnuitlibel_voortplanting_max, directions = 8, zeroAsNA = TRUE)
  cl_land  <- terra::patches(land_nabij_in_ruim_max, directions = 8, zeroAsNA = TRUE)
  
  # Afstandsberekeningen
  dist_from_water <- terra::distance(cl_water)
  dist_from_land  <- terra::distance(cl_land)
  
  # Bepaal welke cellen binnen 10m van de ander liggen
  land_binnen_10m_water <- terra::ifel(dist_from_water <= 10, 1, 0)
  water_binnen_10m_land  <- terra::ifel(dist_from_land <= 10, 1, 0)
  
  # Cluster-niveau valideren (als minstens 1 pixel van het cluster raakt, bewaar het hele cluster)
  valide_land_ids  <- terra::zonal(land_binnen_10m_water, cl_land, fun = "max", na.rm = TRUE)
  valide_water_ids <- terra::zonal(water_binnen_10m_land, cl_water, fun = "max", na.rm = TRUE)
  
  colnames(valide_land_ids)  <- c("Cluster_ID", "Status")
  colnames(valide_water_ids) <- c("Cluster_ID", "Status")
  
  winnaars_land  <- valide_land_ids$Cluster_ID[valide_land_ids$Status == 1]
  winnaars_water <- valide_water_ids$Cluster_ID[valide_water_ids$Status == 1]
  
  if(length(winnaars_land) > 0 && length(winnaars_water) > 0) {
    
    gevlektewitsnuitlibel_land_finaal_max  <- terra::mask(land_nabij_in_ruim_max, 
                                                          terra::ifel(cl_land %in% winnaars_land, 1, NA))
    gevlektewitsnuitlibel_water_finaal_max <- terra::mask(gevlektewitsnuitlibel_voortplanting_max, 
                                                          terra::ifel(cl_water %in% winnaars_water, 1, NA))
    
    # FINALE COMBINATIE (Water + Land_nabij)
    gevlektewitsnuitlibel_leefgebied_max <- terra::cover(gevlektewitsnuitlibel_water_finaal_max, 
                                                         gevlektewitsnuitlibel_land_finaal_max)
    
    # Bijsnijden naar studiegebied voor rapportage
    gevlektewitsnuitlibel_leefgebied_max <- terra::crop(gevlektewitsnuitlibel_leefgebied_max, area_shape_proj, mask = TRUE)
    
  } else {
    message("⚠️ Geen water- en landclusters gevonden die binnen 10m van elkaar liggen.")
    gevlektewitsnuitlibel_leefgebied_max <- template_TV * NA
  }
} else {
  gevlektewitsnuitlibel_leefgebied_max <- template_TV * NA
}

# --- PARALLEL SPOOR B (WERKELIJKE OPPERVLAKTE FRACTIES) ---
if(exists("land_nabij_opp") && !all(is.na(terra::values(gevlektewitsnuitlibel_leefgebied_max, mat=FALSE)))) {
  # Masker het werkelijke fractieraster met de goedgekeurde ruimtelijke footprint uit Spoor A
  vpt_opp_tv  <- terra::crop(gevlektewitsnuitlibel_voortplanting_opp, area_shape_proj, mask = TRUE)
  land_opp_tv <- terra::crop(land_nabij_opp, area_shape_proj, mask = TRUE)
  
  leef_opp_ruw <- terra::cover(vpt_opp_tv, land_opp_tv)
  gevlektewitsnuitlibel_leefgebied_opp <- terra::mask(leef_opp_ruw, gevlektewitsnuitlibel_leefgebied_max)
} else {
  gevlektewitsnuitlibel_leefgebied_opp <- template_TV * NA
}

# Schoonmaak
rm(cl_water, cl_land, dist_from_water, dist_from_land, land_binnen_10m_water, water_binnen_10m_land)
gc()
```


# Export & Transitiekaart
```{r Export}
scenario_raster_dir <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", scenario_naam)
if (!dir.exists(scenario_raster_dir)) dir.create(scenario_raster_dir, recursive = TRUE)

final_opp_scen <- NULL

if (exists("resB_strikt") && !is.null(resB_strikt)) {
  final_opp_scen <- max(c(resB_strikt$kern, resB_strikt$bouw), na.rm = TRUE)
} else if (exists("final_opp")) {
  final_opp_scen <- final_opp
} else if (exists("bruinekiekendief_leefgebied1_opp")) {
  final_opp_scen <- bruinekiekendief_leefgebied1_opp
} else {
  mogelijke_vars <- ls(pattern = "(_final_opp|_leefgebied1_opp|_finaal_opp)$")
  if (length(mogelijke_vars) > 0) {
    final_opp_scen <- get(mogelijke_vars[1])
  } else {
    final_opp_scen <- terra::rast(template_TV, vals = NA)
  }
}

if (!all(is.na(terra::values(final_opp_scen, mat = FALSE)))) {
  werkelijk_scenario_rast <- terra::ifel(!is.na(final_opp_scen) & final_opp_scen > 0, 1, NA)
} else {
  werkelijk_scenario_rast <- terra::rast(template_TV, vals = NA)
}

bestandsnaam <- paste0("TV_", scenario_naam, "_", soort, ".tif")
file_path_scen <- file.path(scenario_raster_dir, bestandsnaam)

export_rast <- terra::deepcopy(werkelijk_scenario_rast)
export_rast <- terra::mask(export_rast, area_buffer_fix)

if (all(is.na(terra::values(export_rast, mat = FALSE)))) {
  export_rast <- terra::ifel(is.na(export_rast), 0, 0)
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
} else {
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
}

writeRaster(export_rast, 
            filename = file_path_scen, 
            overwrite = TRUE, 
            gdal = c("COMPRESS=LZW"), 
            datatype = "INT1U")

message(paste("✓ Scenario-raster succesvol weggeschreven naar:", file_path_scen))

referentie_file <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", "02_Werkelijke_Oppervlakte", paste0("Habitat_Werkelijke_Oppervlaktes_", soort, ".tif"))

if (file.exists(referentie_file)) {
  r_ref <- rast(referentie_file) %>% terra::resample(template_TV, method = "near")
} else {
  r_ref <- terra::rast(template_TV, vals = NA)
}

r_scen <- werkelijk_scenario_rast

r_rgb <- terra::ifel(!is.na(r_ref) & is.na(r_scen), 1, NA)
r_rgb <- terra::cover(r_rgb, terra::ifel(is.na(r_ref) & !is.na(r_scen), 2, NA))
r_rgb <- terra::cover(r_rgb, terra::ifel(!is.na(r_ref) & !is.na(r_scen), 3, NA))

r_rgb_web <- terra::project(terra::as.int(r_rgb), "EPSG:4326", method = "near")

kleuren_rgb <- c("#E74C3C", "#2ECC71", "#3498DB") 
pal_rgb <- colorFactor(palette = kleuren_rgb, levels = 1:3, na.color = "transparent")

leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  addRasterImage(r_rgb_web, colors = pal_rgb, opacity = 0.7, group = "Scenario Vergelijking") %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens TV") %>%
  addLegend(
    colors = kleuren_rgb,
    labels = c("Verloren gegaan leefgebied", "Nieuw bijgekomen leefgebied", "Status Quo (Behouden)"),
    title = paste("Scenario vs Referentie:", scenario_naam),
    position = "bottomright"
  ) %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Scenario Vergelijking", "Grens TV"),
    options = layersControlOptions(collapsed = FALSE)
  )
```
