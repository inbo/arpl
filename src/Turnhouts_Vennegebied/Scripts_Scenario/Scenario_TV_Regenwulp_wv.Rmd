---
title: "Scenario Leefgebiedenscript: Regenwulp"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
params:
  scenario_rds_path: "data/input/Scenario_rds/TV_Scenario_bosbehoudss_ss31fix_tvg_vrij_2026.rds"
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE,
  include = FALSE
)

library(knitr)
library(here)
# Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
knitr::opts_knit$set(root.dir = here::here())

library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  temp_mask <- masker
  if(dist_m > 0) temp_mask <- terra::buffer(masker, width = dist_m / 2)
  
  cl <- terra::patches(temp_mask, directions = 8, zeroAsNA = TRUE) %>% terra::mask(masker)
  
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    # CORRECTIE: Waarde is de som van fracties (1 pixel = 1.0). 
    # Vermenigvuldigen met 100 geeft de juiste Area in m2!
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100 # 1 binaire pixel = 100m2
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  masker_binair <- cl %in% voldoet_ids
  final_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  return(list(raster = terra::mask(masker, final_mask), clusters = cl))
}
```

# Soort en scenario informatie ophalen
```{r Soort informatie}
soort <- "regenwulp"

# Strip eventuele relatieve klim-paden (../) als die toch worden meegegeven
p_raw <- gsub("^(\\.\\./)+", "", params$scenario_rds_path)
scenario_path <- here::here(p_raw)

if (!file.exists(scenario_path)) {
  stop(paste("❌ FOUT: Scenario RDS bestand NIET gevonden op:", scenario_path))
}

scen_volledig <- basename(scenario_path)
scenario_naam <- gsub("^TV_Scenario_|^Scenario_|.rds$", "", scen_volledig)

message(paste("Verwerken van soort:", soort, "binnen scenario:", scenario_naam))

df <- read_excel(here::here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
resultaat <- df %>% filter(tolower(trimws(Soort)) == soort) %>% select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

oppervlakte_ha <- resultaat$MinOpp_ha[1]
afstand_m      <- resultaat$AfstandBiotopen_m[1]
buffer_m       <- 50000 #resultaat$Dispersiecap_m[1]

rm(df, resultaat)
```

```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Turnhouts_Vennegebied.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

# Inlezen van de CSV-sleutel (gebruikt om tabel-indices te mappen naar codes)
df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_TV <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

# ==============================================================================
# ULTRA-SNELLE METHODE: Direct naar data.frame met celnummers in C++
# ==============================================================================
id_raster_TV_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_TV_masked <- mask(id_raster_TV_globale_values, area_buffer_fix)

message("-> Vertaaltabel bliksemsnel opbouwen via C++ dataframe extractie...")

# 'cells = TRUE' zorgt ervoor dat terra direct een kolom 'cell' (lokale id) 
# en een kolom met de waarde (globale id) genereert voor alle niet-NA cellen.
df_extractie <- as.data.frame(id_raster_TV_masked, cells = TRUE)

# Direct omzetten naar data.table en de kolommen juist benoemen
vertaal_df <- as.data.table(df_extractie)

# De kolommen heten nu standaard 'cell' en de naam van je raster (lyr.1 of gelijkaardig)
# We hernoemen ze direct naar jouw vertrouwde structuur:
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))

# Filter eventuele resterende NA's eruit
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_TV) <- NA
# --- DE CRUCIALE TEMPLATE REPARATIE ---
# In plaats van alles op NA te zetten, maken we een binaire 'mal' van het studiegebied.
# We rasteriseren de shapefile van het Turnhouts Vennegebied (area_buffer_fix) op het uitgesneden raster.
template_TV <- terra::rasterize(area_buffer_fix, id_raster_TV, field = 1, background = 0)

# Controleer of het nu wél cellen bevat:
cat("Gecorrigeerd aantal pixels in template_TV: ", sum(terra::values(template_TV) == 1, na.rm=TRUE), "\n")

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_TV_globale_values, id_raster_TV_masked)
gc()
```

# Biotoopfiltering (BWK-codes uit Scenario RDS)
```{r Biotoopfiltering}
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# Lees de scenario-tabel in
tabel_vlaanderen <- readRDS(scenario_path)
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

lijst_oppervlaktes <- list()

for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  tabel_TV <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_TV_unique <- unique(tabel_TV, by = c("cel_id", "CODE"))
  tabel_cel_som <- tabel_TV_unique[, .(Oppervlakte = pmin(sum(BWK_FRAC, na.rm = TRUE), 1.0)), by = .(cel_id)]
  
  r_opp_type <- id_raster_TV * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    setnames(tabel_cel_som, "cel_id", "globale_id")
    
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

foerageer_opp      <- lijst_oppervlaktes[["foerageer"]]
foerageer_goed_opp <- lijst_oppervlaktes[["foerageer_goed"]]
plas_opp           <- lijst_oppervlaktes[["plas"]]
heide_opp          <- lijst_oppervlaktes[["heide"]]
heideplas_opp      <- lijst_oppervlaktes[["heideplas"]]
akkers_opp         <- lijst_oppervlaktes[["akkers"]]

rm(resultaten_gegroepeerd, df_nieuw, lijst_oppervlaktes, tabel_vlaanderen, tabel_gefilterd, tabel_TV, tabel_cel_som, vertaal_df)
gc()
```

```{r regenwulp spoor verwerking}
# ==============================================================================
# REGENWULP STAP 1: GRASLANDSPOOR (300 HA BRUTO, 100 HA GOED, PLAS, 50 HA CLUSTER)
# ==============================================================================
message("-> 1. Graslandspoor verwerken (reële oppervlaktes)...")

r_bin_foer <- terra::ifel(!is.na(foerageer_opp) & foerageer_opp > 0, 1, NA)
r_foer_buffered <- terra::buffer(r_bin_foer, width = 25)

cl_bruto_vlakken <- terra::patches(r_foer_buffered, directions = 8, zeroAsNA = TRUE) %>% 
  terra::mask(r_bin_foer)

opp_per_cluster <- terra::zonal(foerageer_opp, cl_bruto_vlakken, fun = "sum", na.rm = TRUE)
colnames(opp_per_cluster) <- c("ClusterID", "Bruto_ha")

grote_cluster_ids <- opp_per_cluster$ClusterID[opp_per_cluster$Bruto_ha >= 300]

if (length(grote_cluster_ids) > 0) {
  r_grote_compartimenten_ids <- cl_bruto_vlakken %in% grote_cluster_ids
  r_grote_compartimenten_ids <- terra::ifel(r_grote_compartimenten_ids == 1, cl_bruto_vlakken, NA)
  
  goed_per_groot_cluster <- terra::zonal(foerageer_goed_opp, r_grote_compartimenten_ids, fun = "sum", na.rm = TRUE)
  colnames(goed_per_groot_cluster) <- c("ClusterID", "Goed_ha")
  
  kwalitatieve_cluster_ids <- goed_per_groot_cluster$ClusterID[goed_per_groot_cluster$Goed_ha >= 100]
  
  if (length(kwalitatieve_cluster_ids) > 0) {
    r_grasland_gecertificeerd_ids <- r_grote_compartimenten_ids %in% kwalitatieve_cluster_ids
    r_grasland_gecertificeerd_ids <- terra::ifel(r_grasland_gecertificeerd_ids == 1, r_grote_compartimenten_ids, NA)
  } else {
    r_grasland_gecertificeerd_ids <- template_TV * NA
  }
} else {
  r_grasland_gecertificeerd_ids <- template_TV * NA
}

# --- SLAAPPLAATSCHECK GRASLAND (PLASSEN >= 0.5 HA BINNEN 50M) ---
r_grasland_slaapplaats <- template_TV * NA

if (!all(is.na(terra::values(r_grasland_gecertificeerd_ids, mat = FALSE))) && !is.null(plas_opp) && !all(is.na(terra::values(plas_opp, mat = FALSE)))) {
  r_bin_plas <- terra::ifel(!is.na(plas_opp) & plas_opp > 0, 1, NA)
  cl_plas <- cluster_filter_compleet(r_bin_plas, plas_opp, 0.5 * 10000, 50, TRUE)
  r_plas_05ha <- cl_plas$raster
  
  if (!all(is.na(terra::values(r_plas_05ha, mat = FALSE)))) {
    r_plas_zone <- terra::buffer(terra::ifel(!is.na(r_plas_05ha) & r_plas_05ha > 0, 1, NA), width = 50)
    
    plas_check_per_compartiment <- terra::zonal(r_plas_zone, r_grasland_gecertificeerd_ids, fun = "max", na.rm = TRUE)
    colnames(plas_check_per_compartiment) <- c("ClusterID", "Heeft_Slaapplaats")
    
    compartimenten_met_plas_ids <- plas_check_per_compartiment$ClusterID[plas_check_per_compartiment$Heeft_Slaapplaats == 1]
    
    if (length(compartimenten_met_plas_ids) > 0) {
      r_grasland_slaapplaats <- r_grasland_gecertificeerd_ids %in% compartimenten_met_plas_ids
      r_grasland_slaapplaats <- terra::ifel(r_grasland_slaapplaats == 1, foerageer_opp, NA)
    }
  }
}

# --- FINALE CLUSTERING GRASLAND (>= 50 HA) ---
if (!all(is.na(terra::values(r_grasland_slaapplaats, mat = FALSE)))) {
  r_bin_slp <- terra::ifel(!is.na(r_grasland_slaapplaats) & r_grasland_slaapplaats > 0, 1, NA)
  cl_grasland_final <- cluster_filter_compleet(r_bin_slp, r_grasland_slaapplaats, 50 * 10000, 50, TRUE)
  r_grasland_leefgebied_finaal <- terra::mask(r_grasland_slaapplaats, cl_grasland_final$raster)
} else {
  r_grasland_leefgebied_finaal <- template_TV * NA
}

# ==============================================================================
# REGENWULP STAP 2: HEIDESPOOR (50 HA HEIDE + VENNETOETS >= 0.5 HA BINNEN 100M)
# ==============================================================================
message("-> 2. Heidespoor verwerken (reële oppervlaktes)...")

r_heide_leefgebied_finaal <- template_TV * NA

if (!is.null(heide_opp) && !all(is.na(terra::values(heide_opp, mat = FALSE)))) {
  r_bin_heide <- terra::ifel(!is.na(heide_opp) & heide_opp > 0, 1, NA)
  r_heide_buffered <- terra::buffer(r_bin_heide, width = 25)
  cl_heide_vlakken <- terra::patches(r_heide_buffered, directions = 8, zeroAsNA = TRUE) %>% terra::mask(r_bin_heide)
  
  opp_heide_per_cluster <- terra::zonal(heide_opp, cl_heide_vlakken, fun = "sum", na.rm = TRUE)
  colnames(opp_heide_per_cluster) <- c("ClusterID", "Heide_ha")
  
  grote_heide_ids <- opp_heide_per_cluster$ClusterID[opp_heide_per_cluster$Heide_ha >= 50]
  
  if (length(grote_heide_ids) > 0) {
    r_heide_gecertificeerd_ids <- cl_heide_vlakken %in% grote_heide_ids
    r_heide_gecertificeerd_ids <- terra::ifel(r_heide_gecertificeerd_ids == 1, cl_heide_vlakken, NA)
    
    if (!is.null(heideplas_opp) && !all(is.na(terra::values(heideplas_opp, mat = FALSE)))) {
      r_bin_hplas <- terra::ifel(!is.na(heideplas_opp) & heideplas_opp > 0, 1, NA)
      cl_heideplas <- cluster_filter_compleet(r_bin_hplas, heideplas_opp, 0.5 * 10000, 50, TRUE)
      r_hplas_05ha <- cl_heideplas$raster
      
      if (!all(is.na(terra::values(r_hplas_05ha, mat = FALSE)))) {
        r_hplas_zone <- terra::buffer(terra::ifel(!is.na(r_hplas_05ha) & r_hplas_05ha > 0, 1, NA), width = 100)
        
        ven_check_per_heide <- terra::zonal(r_hplas_zone, r_heide_gecertificeerd_ids, fun = "max", na.rm = TRUE)
        colnames(ven_check_per_heide) <- c("ClusterID", "Heeft_Ven")
        
        heide_met_ven_ids <- ven_check_per_heide$ClusterID[ven_check_per_heide$Heeft_Ven == 1]
        
        if (length(heide_met_ven_ids) > 0) {
          m_h_ven <- r_heide_gecertificeerd_ids %in% heide_met_ven_ids
          r_heide_leefgebied_finaal <- terra::mask(heide_opp, terra::ifel(m_h_ven, 1, NA))
        }
      }
    }
  }
}

# ==============================================================================
# REGENWULP STAP 3: COMBINATIE & AKKER EXCLUSIE
# ==============================================================================
message("-> 3. Sporen combineren en akkers uitsluiten...")

r_leefgebied_gecombineerd_opp <- terra::cover(r_grasland_leefgebied_finaal, r_heide_leefgebied_finaal)

if (!all(is.na(terra::values(r_leefgebied_gecombineerd_opp, mat = FALSE)))) {
  r_bin_akkers <- terra::ifel(!is.na(akkers_opp) & akkers_opp > 0, 1, NA)
  regenwulp_leefgebied_opp <- terra::mask(r_leefgebied_gecombineerd_opp, r_bin_akkers, inverse = TRUE) %>% terra::crop(template_TV)
} else {
  regenwulp_leefgebied_opp <- template_TV * NA
}

cat("\n--- AUDIT: SCENARIO LEEFGEBIED REGENWULP (WERKELIJKE OPPERVLAKTE) ---\n")
ha_finaal_werk <- calc_ha_exact(regenwulp_leefgebied_opp)
cat("Werkelijke oppervlakte leefgebied Regenwulp in scenario:", round(ha_finaal_werk, 2), "ha\n")

suppressWarnings(rm(r_bin_foer, r_foer_buffered, cl_bruto_vlakken, opp_per_cluster, grote_cluster_ids, 
   r_grote_compartimenten_ids, goed_per_groot_cluster, kwalitatieve_cluster_ids, r_grasland_gecertificeerd_ids, 
   r_grasland_slaapplaats, r_bin_plas, cl_plas, r_plas_05ha, r_plas_zone, plas_check_per_compartiment, 
   r_grasland_leefgebied_finaal, r_heide_leefgebied_finaal, r_bin_heide, r_heide_buffered, cl_heide_vlakken, 
   opp_heide_per_cluster, grote_heide_ids, r_heide_gecertificeerd_ids, r_bin_hplas, cl_heideplas, r_hplas_05ha, 
   r_hplas_zone, ven_check_per_heide, r_leefgebied_gecombineerd_opp, r_bin_akkers))
gc()
```

# Export & Transitiekaart
```{r Export}
scenario_raster_dir <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", scenario_naam)
if (!dir.exists(scenario_raster_dir)) dir.create(scenario_raster_dir, recursive = TRUE)

# 1. Binariseren naar 1 / NA voor export
if (exists("regenwulp_leefgebied_opp") && !all(is.na(terra::values(regenwulp_leefgebied_opp, mat = FALSE)))) {
  werkelijk_scenario_rast <- terra::ifel(!is.na(regenwulp_leefgebied_opp) & regenwulp_leefgebied_opp > 0, 1, NA)
} else {
  werkelijk_scenario_rast <- terra::rast(template_TV, vals = NA)
}

bestandsnaam <- paste0("TV_", scenario_naam, "_", soort, ".tif")
file_path_scen <- file.path(scenario_raster_dir, bestandsnaam)

export_rast <- terra::deepcopy(werkelijk_scenario_rast)
export_rast <- terra::mask(export_rast, area_buffer_fix)

if (all(is.na(terra::values(export_rast, mat = FALSE)))) {
  export_rast <- terra::ifel(is.na(export_rast), 0, 0)
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
} else {
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
}

writeRaster(export_rast, 
            filename = file_path_scen, 
            overwrite = TRUE, 
            gdal = c("COMPRESS=LZW"), 
            datatype = "INT1U")

message(paste("✓ Scenario-raster succesvol weggeschreven naar:", file_path_scen))

# 2. Vergelijking met Referentie-raster uit 02_Werkelijke_Oppervlakte (check op _wv suffix)
referentie_file_wv  <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", "02_Werkelijke_Oppervlakte", paste0("Habitat_Werkelijke_Oppervlaktes_", soort, "_wv.tif"))
referentie_file_std <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", "02_Werkelijke_Oppervlakte", paste0("Habitat_Werkelijke_Oppervlaktes_", soort, ".tif"))

referentie_file <- if (file.exists(referentie_file_wv)) referentie_file_wv else referentie_file_std

if (file.exists(referentie_file)) {
  r_ref_raw <- rast(referentie_file) %>% terra::resample(template_TV, method = "near")
  r_ref <- terra::ifel(r_ref_raw == 1, 1, NA)
} else {
  r_ref <- terra::rast(template_TV, vals = NA)
}

r_scen <- werkelijk_scenario_rast

# Bepaal verschillen:
# 1 = Verloren (was 1 in ref, nu NA in scen)
# 2 = Nieuw (was NA in ref, nu 1 in scen)
# 3 = Behouden (was 1 in ref, is 1 in scen)
r_rgb <- terra::ifel(!is.na(r_ref) & is.na(r_scen), 1, NA)
r_rgb <- terra::cover(r_rgb, terra::ifel(is.na(r_ref) & !is.na(r_scen), 2, NA))
r_rgb <- terra::cover(r_rgb, terra::ifel(!is.na(r_ref) & !is.na(r_scen), 3, NA))

r_rgb_web <- terra::project(terra::as.int(r_rgb), "EPSG:4326", method = "near")

kleuren_rgb <- c("#E74C3C", "#2ECC71", "#3498DB") 
pal_rgb <- colorFactor(palette = kleuren_rgb, levels = 1:3, na.color = "transparent")

leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  addRasterImage(r_rgb_web, colors = pal_rgb, opacity = 0.7, group = "Scenario Vergelijking") %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens TV") %>%
  addLegend(
    colors = kleuren_rgb,
    labels = c("Verloren gegaan leefgebied", "Nieuw bijgekomen leefgebied", "Status Quo (Behouden)"),
    title = paste("Scenario vs Referentie:", scenario_naam),
    position = "bottomright"
  ) %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Scenario Vergelijking", "Grens TV"),
    options = layersControlOptions(collapsed = FALSE)
  )
```
