---
title: "Scenario Leefgebiedenscript: Sierlijke Witsnuitlibel"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
params:
  scenario_rds_path: "data/input/Scenario_rds/TV_Scenario_bosbehoudss_ss31fix_tvg_vrij_2026.rds"
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE,
  include = FALSE
)

library(knitr)
library(here)
knitr::opts_knit$set(root.dir = here::here())

library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(dplyr::first)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  temp_mask <- masker
  if(dist_m > 0) temp_mask <- terra::buffer(masker, width = dist_m / 2)
  
  cl <- terra::patches(temp_mask, directions = 8, zeroAsNA = TRUE) %>% terra::mask(masker)
  
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  masker_binair <- cl %in% voldoet_ids
  final_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  return(list(raster = terra::mask(masker, final_mask), clusters = cl))
}
```

# Soort informatie ophalen
```{r Soort informatie}
soort <- "sierlijkewitsnuitlibel"

p_raw <- gsub("^(\\.\\./)+", "", params$scenario_rds_path)
scenario_path <- here::here(p_raw)

if (!file.exists(scenario_path)) {
  stop(paste("❌ FOUT: Scenario RDS bestand NIET gevonden op:", scenario_path))
}

scen_volledig <- basename(scenario_path)
scenario_naam <- gsub("^TV_Scenario_|^Scenario_|.rds$", "", scen_volledig)

message(paste("Verwerken van soort:", soort, "binnen scenario:", scenario_naam))

df <- read_excel(here::here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
resultaat <- df %>% filter(tolower(trimws(Soort)) == soort) %>% select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

oppervlakte_ha <- resultaat$MinOpp_ha[1]
afstand_m      <- resultaat$AfstandBiotopen_m[1]
buffer_m       <- resultaat$Dispersiecap_m[1]

rm(df, resultaat)
```

# Gebied voorbereiden (Turnhouts Vennegebied)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Turnhouts_Vennegebied.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_TV <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

id_raster_TV_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_TV_masked <- mask(id_raster_TV_globale_values, area_buffer_fix)

df_extractie <- as.data.frame(id_raster_TV_masked, cells = TRUE)
vertaal_df <- as.data.table(df_extractie)
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_TV) <- NA
template_TV <- terra::rasterize(area_buffer_fix, id_raster_TV, field = 1, background = 0)

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_TV_globale_values, id_raster_TV_masked)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

tabel_vlaanderen <- readRDS(scenario_path)
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

lijst_oppervlaktes <- list()

for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  tabel_TV <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_TV_unique <- unique(tabel_TV, by = c("cel_id", "CODE"))
  tabel_cel_som <- tabel_TV_unique[, .(Oppervlakte = pmin(sum(BWK_FRAC, na.rm = TRUE), 1.0)), by = .(cel_id)]
  
  r_opp_type <- id_raster_TV * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    setnames(tabel_cel_som, "cel_id", "globale_id")
    
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

voortplanting_bwk_opp  <- lijst_oppervlaktes[["voortplanting_bwk"]]
foerageer0_notbwk_opp  <- lijst_oppervlaktes[["foerageer0"]]
struweel1_opp          <- lijst_oppervlaktes[["struweel1"]]

rm(resultaten_gegroepeerd, df_nieuw, lijst_oppervlaktes, tabel_vlaanderen, tabel_gefilterd, tabel_TV, tabel_TV_unique, tabel_cel_som, vertaal_df)
gc()
```

```{r Watervlakken en Kleine Waters}
# ==============================================================================
# STAP 1 & 2: WATERVLAKKEN, BWK-VENNEN & CONNECTIVITEITSCHECK (500M)
# ==============================================================================
message("-> Watervlakken shapefile inlezen en kleine waters (<= 0.4 ha) filteren...")

watervlakken_v <- vect(here("data/input/ASCI Files/watervlakken2024.shp"))
watervlakken_proj <- project(watervlakken_v, crs(template_TV))

area_buffer_clean <- aggregate(makeValid(area_buffer_fix))
watervlakken_clean <- makeValid(watervlakken_proj)
watervlakken_TV <- crop(watervlakken_clean, area_buffer_clean)

watervlakken_buffered <- buffer(watervlakken_TV, width = 5)
watervlakken_fuzzy_clusters <- aggregate(watervlakken_buffered, by = NULL)
watervlakken_fuzzy_clusters$fuzzy_area_m2 <- terra::expanse(watervlakken_fuzzy_clusters)

kleine_clusters_v <- watervlakken_fuzzy_clusters[watervlakken_fuzzy_clusters$fuzzy_area_m2 <= 4000, ]

if (nrow(kleine_clusters_v) > 0) {
  kleine_watervlakken_finaal <- crop(watervlakken_TV, kleine_clusters_v)
  if (nrow(kleine_watervlakken_finaal) > 0) {
    r_kleinwater_opp <- terra::rasterize(kleine_watervlakken_finaal, template_TV, field = 1, background = NA)
    w_clean <- terra::ifel(is.na(voortplanting_bwk_opp), 0, voortplanting_bwk_opp)
    k_clean <- terra::ifel(is.na(r_kleinwater_opp), 0, r_kleinwater_opp)
    r_witsnuit_waterbiotoop1_opp <- terra::clamp(w_clean + k_clean, upper = 1.0)
    r_witsnuit_waterbiotoop1_opp <- terra::ifel(r_witsnuit_waterbiotoop1_opp > 0, r_witsnuit_waterbiotoop1_opp, NA)
  } else {
    r_witsnuit_waterbiotoop1_opp <- voortplanting_bwk_opp
  }
} else {
  r_witsnuit_waterbiotoop1_opp <- voortplanting_bwk_opp
}

# Poelennetwerk connectiviteit (minstens 3 buren binnen 500m)
message("-> Poelennetwerk connectiviteit controleren...")
if (!is.null(r_witsnuit_waterbiotoop1_opp) && !all(is.na(terra::values(r_witsnuit_waterbiotoop1_opp, mat = FALSE)))) {
  r_bin_water1 <- terra::ifel(!is.na(r_witsnuit_waterbiotoop1_opp) & r_witsnuit_waterbiotoop1_opp > 0, 1, NA)
  r_water_clusters <- terra::patches(r_bin_water1, directions = 8, zeroAsNA = TRUE)
  
  r_poel_kernen <- r_water_clusters * NA
  df_cells <- terra::as.data.frame(r_water_clusters, cells = TRUE)
  colnames(df_cells) <- c("cell", "ID")
  
  df_kernen <- df_cells %>% dplyr::group_by(ID) %>% dplyr::summarise(cell = dplyr::first(cell))
  r_poel_kernen[df_kernen$cell] <- df_kernen$ID
  
  xy_kernen <- terra::xyFromCell(r_poel_kernen, df_kernen$cell)
  n_plassen <- nrow(xy_kernen)
  
  if (n_plassen >= 4) {
    dist_matrix <- stats::dist(xy_kernen, method = "euclidean") %>% as.matrix()
    cluster_counts <- rowSums(dist_matrix <= 500)
    
    df_scores <- data.table(ID = df_kernen$ID, Buren_Count = cluster_counts)
    goedgekeurde_poel_ids <- df_scores[Buren_Count >= 4, ID]
    
    if (length(goedgekeurde_poel_ids) > 0) {
      masker_water_bin <- r_water_clusters %in% goedgekeurde_poel_ids
      witsnuit_water_finaal_opp <- terra::mask(r_witsnuit_waterbiotoop1_opp, terra::ifel(masker_water_bin, 1, NA))
    } else {
      witsnuit_water_finaal_opp <- template_TV * NA
    }
  } else {
    witsnuit_water_finaal_opp <- template_TV * NA
  }
} else {
  witsnuit_water_finaal_opp <- template_TV * NA
}

# ==============================================================================
# STAP 3: STRUWEEL / BESCHUTTING GEBIED (MIN 50 HA, >= 70% STRUWEEL BINNEN 50M)
# ==============================================================================
message("-> Struweel/bos-omgeving evalueren op reële fracties...")

if (!is.null(struweel1_opp) && !all(is.na(terra::values(struweel1_opp, mat = FALSE)))) {
  r_bin_struw <- terra::ifel(!is.na(struweel1_opp) & struweel1_opp > 0, 1, NA)
  
  struw_clusters <- cluster_filter_compleet(
    masker     = r_bin_struw,
    opp_laag   = struweel1_opp,
    drempel_m2 = 50 * 10000, # 50 ha
    dist_m     = 50,
    werkelijk  = TRUE
  )
  
  cl_foerageer <- struw_clusters$clusters
  
  if (!all(is.na(terra::values(cl_foerageer, mat = FALSE)))) {
    w_50m <- terra::focalMat(cl_foerageer, 50, type = 'circle')
    w_50m[w_50m > 0] <- 1
    cl_foerageer_buf <- terra::focal(cl_foerageer, w = w_50m, fun = "max", na.rm = TRUE)
    
    struweel_in_zone <- terra::mask(struweel1_opp, cl_foerageer_buf)
    zonal_struweel   <- terra::zonal(struweel_in_zone, cl_foerageer_buf, fun = "sum", na.rm = TRUE)
    colnames(zonal_struweel) <- c("ID", "Struweel_Fractie_Sum")
    
    area_raster <- terra::cellSize(cl_foerageer, unit = "ha")
    foer_ha_raster <- struw_clusters$raster * area_raster
    stats_foer <- terra::zonal(foer_ha_raster, cl_foerageer, fun = "sum", na.rm = TRUE)
    colnames(stats_foer) <- c("ID", "Foer_ha")
    
    df_criteria <- merge(stats_foer, zonal_struweel, by = "ID", all.x = TRUE)
    df_criteria[is.na(df_criteria)] <- 0
    df_criteria$Struweel_ha <- df_criteria$Struweel_Fractie_Sum * 0.01
    df_criteria$Percentage_Struweel <- (df_criteria$Struweel_ha / df_criteria$Foer_ha) * 100
    
    goedgekeurde_foerageer_ids <- df_criteria$ID[df_criteria$Foer_ha >= 50 & df_criteria$Percentage_Struweel >= 70]
    
    if (length(goedgekeurde_foerageer_ids) > 0) {
      masker_foerageer_bin <- cl_foerageer %in% goedgekeurde_foerageer_ids
      witsnuit_foerageer_finaal_opp <- terra::mask(struweel1_opp, terra::ifel(masker_foerageer_bin, 1, NA))
    } else {
      witsnuit_foerageer_finaal_opp <- template_TV * NA
    }
  } else {
    witsnuit_foerageer_finaal_opp <- template_TV * NA
  }
} else {
  witsnuit_foerageer_finaal_opp <- template_TV * NA
}

# ==============================================================================
# STAP 4 & 5: RUIMTELIJKE KOPPELING (800M) & FINALE DREMPEL (35 HA)
# ==============================================================================
message("-> Wederzijdse 800m koppeling en finale 35 ha complexdrempel berekenen...")

if (!all(is.na(terra::values(witsnuit_water_finaal_opp, mat = FALSE))) && 
    !all(is.na(terra::values(witsnuit_foerageer_finaal_opp, mat = FALSE)))) {
  
  r_bin_wat_fin  <- terra::ifel(!is.na(witsnuit_water_finaal_opp) & witsnuit_water_finaal_opp > 0, 1, NA)
  r_bin_foer_fin <- terra::ifel(!is.na(witsnuit_foerageer_finaal_opp) & witsnuit_foerageer_finaal_opp > 0, 1, NA)
  
  water_buffer_800m <- terra::buffer(r_bin_wat_fin, width = 800)
  foer_buffer_800m  <- terra::buffer(r_bin_foer_fin, width = 800)
  
  witsnuit_water_relatie_opp <- terra::mask(witsnuit_water_finaal_opp, foer_buffer_800m == 1)
  witsnuit_foer_relatie_opp  <- terra::mask(witsnuit_foerageer_finaal_opp, water_buffer_800m == 1)
  
  w_rel_clean <- terra::ifel(is.na(witsnuit_water_relatie_opp), 0, witsnuit_water_relatie_opp)
  f_rel_clean <- terra::ifel(is.na(witsnuit_foer_relatie_opp), 0, witsnuit_foer_relatie_opp)
  
  witsnuit_leefgebied1_opp <- terra::clamp(w_rel_clean + f_rel_clean, upper = 1.0)
  witsnuit_leefgebied1_opp <- terra::ifel(witsnuit_leefgebied1_opp > 0, witsnuit_leefgebied1_opp, NA)
  
  if (!all(is.na(terra::values(witsnuit_leefgebied1_opp, mat = FALSE)))) {
    r_bin_leef1 <- terra::ifel(!is.na(witsnuit_leefgebied1_opp) & witsnuit_leefgebied1_opp > 0, 1, NA)
    
    finaal_clusters <- cluster_filter_compleet(
      masker     = r_bin_leef1,
      opp_laag   = witsnuit_leefgebied1_opp,
      drempel_m2 = 35 * 10000, # 35 ha
      dist_m     = 50,
      werkelijk  = TRUE
    )
    sierlijkewitsnuitlibel_leefgebied_opp <- terra::mask(witsnuit_leefgebied1_opp, finaal_clusters$raster) %>% terra::crop(template_TV)
  } else {
    sierlijkewitsnuitlibel_leefgebied_opp <- template_TV * NA
  }
} else {
  sierlijkewitsnuitlibel_leefgebied_opp <- template_TV * NA
}

cat("\n--- AUDIT: SCENARIO LEEFGEBIED SIERLIJKE WITSNUITLIBEL (WERKELIJKE OPPERVLAKTE) ---\n")
ha_finaal_werk <- calc_ha_exact(sierlijkewitsnuitlibel_leefgebied_opp)
cat("Werkelijke oppervlakte leefgebied Sierlijke Witsnuitlibel in scenario:", round(ha_finaal_werk, 2), "ha\n")

suppressWarnings(rm(watervlakken_v, watervlakken_proj, area_buffer_clean, watervlakken_clean, watervlakken_TV, 
   watervlakken_buffered, watervlakken_fuzzy_clusters, kleine_clusters_v, kleine_watervlakken_finaal, r_kleinwater_opp, 
   w_clean, k_clean, r_witsnuit_waterbiotoop1_opp, r_bin_water1, r_water_clusters, r_poel_kernen, df_cells, df_kernen, 
   xy_kernen, dist_matrix, df_scores, goedgekeurde_poel_ids, masker_water_bin, witsnuit_water_finaal_opp, 
   r_bin_struw, struw_clusters, cl_foerageer, w_50m, cl_foerageer_buf, struweel_in_zone, zonal_struweel, 
   area_raster, foer_ha_raster, stats_foer, df_criteria, goedgekeurde_foerageer_ids, masker_foerageer_bin, 
   witsnuit_foerageer_finaal_opp, r_bin_wat_fin, r_bin_foer_fin, water_buffer_800m, foer_buffer_800m, 
   witsnuit_water_relatie_opp, witsnuit_foer_relatie_opp, w_rel_clean, f_rel_clean, witsnuit_leefgebied1_opp, 
   r_bin_leef1, finaal_clusters))
gc()
```

```{r Export}
scenario_raster_dir <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", scenario_naam)
if (!dir.exists(scenario_raster_dir)) dir.create(scenario_raster_dir, recursive = TRUE)

# 1. Binariseren naar 1 / NA voor export
if (exists("sierlijkewitsnuitlibel_leefgebied_opp") && !all(is.na(terra::values(sierlijkewitsnuitlibel_leefgebied_opp, mat = FALSE)))) {
  werkelijk_scenario_rast <- terra::ifel(!is.na(sierlijkewitsnuitlibel_leefgebied_opp) & sierlijkewitsnuitlibel_leefgebied_opp > 0, 1, NA)
} else {
  werkelijk_scenario_rast <- terra::rast(template_TV, vals = NA)
}

bestandsnaam <- paste0("TV_", scenario_naam, "_", soort, ".tif")
file_path_scen <- file.path(scenario_raster_dir, bestandsnaam)

export_rast <- terra::deepcopy(werkelijk_scenario_rast)
export_rast <- terra::mask(export_rast, area_buffer_fix)

if (all(is.na(terra::values(export_rast, mat = FALSE)))) {
  export_rast <- terra::ifel(is.na(export_rast), 0, 0)
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
} else {
  export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
}

writeRaster(export_rast, 
            filename = file_path_scen, 
            overwrite = TRUE, 
            gdal = c("COMPRESS=LZW"), 
            datatype = "INT1U")

message(paste("✓ Scenario-raster succesvol weggeschreven naar:", file_path_scen))

# 2. Vergelijking met Referentie-raster uit 02_Werkelijke_Oppervlakte
referentie_file <- here::here("data/output/Turnhouts_Vennegebied/Rasters_Soorten", "02_Werkelijke_Oppervlakte", paste0("Habitat_Werkelijke_Oppervlaktes_", soort, ".tif"))

if (file.exists(referentie_file)) {
  r_ref_raw <- rast(referentie_file) %>% terra::resample(template_TV, method = "near")
  r_ref <- terra::ifel(r_ref_raw == 1, 1, NA)
} else {
  r_ref <- terra::rast(template_TV, vals = NA)
}

r_scen <- werkelijk_scenario_rast

# Bepaal verschillen:
# 1 = Verloren (was 1 in ref, nu NA in scen)
# 2 = Nieuw (was NA in ref, nu 1 in scen)
# 3 = Behouden (was 1 in ref, is 1 in scen)
r_rgb <- terra::ifel(!is.na(r_ref) & is.na(r_scen), 1, NA)
r_rgb <- terra::cover(r_rgb, terra::ifel(is.na(r_ref) & !is.na(r_scen), 2, NA))
r_rgb <- terra::cover(r_rgb, terra::ifel(!is.na(r_ref) & !is.na(r_scen), 3, NA))

r_rgb_web <- terra::project(terra::as.int(r_rgb), "EPSG:4326", method = "near")

kleuren_rgb <- c("#E74C3C", "#2ECC71", "#3498DB") 
pal_rgb <- colorFactor(palette = kleuren_rgb, levels = 1:3, na.color = "transparent")

leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  addRasterImage(r_rgb_web, colors = pal_rgb, opacity = 0.7, group = "Scenario Vergelijking") %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens TV") %>%
  addLegend(
    colors = kleuren_rgb,
    labels = c("Verloren gegaan leefgebied", "Nieuw bijgekomen leefgebied", "Status Quo (Behouden)"),
    title = paste("Scenario vs Referentie:", scenario_naam),
    position = "bottomright"
  ) %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Scenario Vergelijking", "Grens TV"),
    options = layersControlOptions(collapsed = FALSE)
  )
```

