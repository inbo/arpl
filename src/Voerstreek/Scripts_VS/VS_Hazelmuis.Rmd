---
title: "Leefgebiedenscript in R: Voerstreek"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)

library(here)
# CRUCIALE FIX: Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
# Hierdoor werken relatieve paden met hier() overal hetzelfde.
knitr::opts_knit$set(root.dir = here())

library(knitr)
library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(dplyr::first)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

# Exacte hectare-berekening behouden (cellSize voor 100% precisie)
calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

maak_kaart_laag <- function(res_list) {
  if(is.null(res_list)) return(NULL)
  kernen <- terra::ifel(!is.na(res_list$kern), 1, NA)
  if(!is.null(res_list$bouw)) {
    bouw <- terra::ifel(!is.na(res_list$bouw), 2, NA)
    finaal <- terra::cover(kernen, bouw)
  } else {
    finaal = kernen
  }
  return(finaal)
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  temp_mask <- masker
  if(dist_m > 0) temp_mask <- terra::buffer(masker, width = dist_m / 2)
  
  cl <- terra::patches(temp_mask, directions = 8, zeroAsNA = TRUE) %>% terra::mask(masker)
  
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    # CORRECTIE: Waarde is de som van fracties (1 pixel = 1.0). 
    # Vermenigvuldigen met 100 geeft de juiste Area in m2!
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100 # 1 binaire pixel = 100m2
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  masker_binair <- cl %in% voldoet_ids
  final_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  return(list(raster = terra::mask(masker, final_mask), clusters = cl))
}
```

# Soort informatie ophalen
```{r Soort informatie}
df <- read_excel(here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
soort <- "hazelmuis"

resultaat <- df %>%
filter(Soort == soort) %>%
select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

# Variabelen definiëren
buffer_m <- resultaat$Dispersiecap_m

print(resultaat)
rm(df, resultaat)
```

# Gebied voorbereiden (Voerstreek)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Voerstreek.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_VS <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

id_raster_VS_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_VS_masked <- mask(id_raster_VS_globale_values, area_buffer_fix)

message("-> Vertaaltabel bliksemsnel opbouwen via C++ dataframe extractie...")

df_extractie <- as.data.frame(id_raster_VS_masked, cells = TRUE)
vertaal_df <- as.data.table(df_extractie)
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))

vertaal_df <- vertaal_df[!is.na(globale_id)]
studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_VS) <- NA
template_VS <- terra::rasterize(area_buffer_fix, id_raster_VS, field = 1, background = 0)

cat("Gecorrigeerd aantal pixels in template_VS: ", sum(terra::values(template_VS) == 1, na.rm=TRUE), "\n")

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_VS_globale_values, id_raster_VS_masked, df_extractie)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(here("data/input/Raster_Vlaanderen/BWK_Tabel_Voerstreek_Plus_50km.rds"))
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

# Initialiseer de lijsten voor de verschillende habitat-bouwstenen
lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE DYNAMISCH VIA DATA.TABLE
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    # 1. Ontsnap speciale regex-tekens (zoals haakjes) om syntaxfouten te voorkomen
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    
    # 2. CRUCIALE FIX: Plak een '^' voor elke individuele code. 
    # Dit dwingt grepl() om alleen te matchen als de BWK-code BEGINT met deze letters.
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    # Filteren: EXACT-matches (geen letters voor/na) OF codes die BEGINNEN met de bevat-termen
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_VS <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_VS_unique <- unique(tabel_VS, by = c("cel_id", "CODE"))
  tabel_cel_som <- tabel_VS_unique[, .(Oppervlakte = pmin(sum(BWK_FRAC, na.rm = TRUE), 1.0)), by = .(cel_id)]
  
  r_match_type <- id_raster_VS * NA
  r_opp_type   <- id_raster_VS * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

# Uitpakken naar de exacte losse variabelen (Naadloze match met je database)
voortplanting_max <- lijst_matches[["voortplanting"]]
voortplanting_opp <- lijst_oppervlaktes[["voortplanting"]]
open_max <- lijst_matches[["open"]]
open_opp <- lijst_oppervlaktes[["open"]]
voortplanting_lineair_max <- lijst_matches[["voortplanting_lineair"]]
voortplanting_lineair_opp <- lijst_oppervlaktes[["voortplanting_lineair"]]

# Grote eindschoonmaak (vertaal_df weggooien mag nu pas!)
rm(resultaten_gegroepeerd, df_nieuw, lijst_matches, lijst_oppervlaktes)
gc()
```

```{r Voortplantingsbiotoop}
# ------------------------------------------------------------------------------
# STAP 1: Voortplantingsbiotoop filteren en clusteren (Parallel)
# ------------------------------------------------------------------------------
message("-> Berekenen voortplantingsclusters (Drempel: 50 ha)...")

# Spoor A: MAX
vp_geclusterd_max <- cluster_filter_compleet(
  masker = voortplanting_max, 
  opp_laag = voortplanting_opp, 
  drempel_m2 = 50 * 10000, 
  dist_m = 20, 
  werkelijk = FALSE
)
hazelmuis_voortplanting1_max <- vp_geclusterd_max$raster

# Spoor B: OPP
r_binair_vp_opp <- terra::ifel(!is.na(voortplanting_opp) & voortplanting_opp > 0, 1, NA)
vp_geclusterd_opp <- cluster_filter_compleet(
  masker = r_binair_vp_opp, 
  opp_laag = voortplanting_opp, 
  drempel_m2 = 50 * 10000, 
  dist_m = 20, 
  werkelijk = TRUE
)
hazelmuis_voortplanting1_opp <- vp_geclusterd_opp$raster

rm(vp_geclusterd_max, vp_geclusterd_opp, r_binair_vp_opp)
gc()
```


```{r Open gebieden clusteren}
# ------------------------------------------------------------------------------
# STAP 2: Open gebieden clusteren (Parallel)
# ------------------------------------------------------------------------------
message("-> Berekenen open gebieden nabij bos (Drempel: 5 ha)...")

# Spoor A: MAX
open_geclusterd_max <- cluster_filter_compleet(
  masker = open_max, 
  opp_laag = open_opp, 
  drempel_m2 = 5 * 10000, 
  dist_m = 100, 
  werkelijk = FALSE
)
hazelmuis_open1_max <- open_geclusterd_max$raster

# Spoor B: OPP
r_binair_open_opp <- terra::ifel(!is.na(open_opp) & open_opp > 0, 1, NA)
open_geclusterd_opp <- cluster_filter_compleet(
  masker = r_binair_open_opp, 
  opp_laag = open_opp, 
  drempel_m2 = 5 * 10000, 
  dist_m = 100, 
  werkelijk = TRUE
)
hazelmuis_open1_opp <- open_geclusterd_opp$raster

rm(open_geclusterd_max, open_geclusterd_opp, r_binair_open_opp)
gc()
```


```{r Bosranden bepalen}
# ------------------------------------------------------------------------------
# STAP 3: Bosranden bepalen (Parallel)
# ------------------------------------------------------------------------------
message("-> Genereren van bosranden (expert-correctie)...")

# Spoor A: MAX
if(!is.null(hazelmuis_voortplanting1_max) && !is.null(hazelmuis_open1_max)) {
  bos_buffer_max <- terra::buffer(hazelmuis_voortplanting1_max, width = 10)
  hazelmuis_bosrand_max <- terra::mask(bos_buffer_max, hazelmuis_open1_max)
} else {
  hazelmuis_bosrand_max <- template_VS * NA
}

bosrand_geclusterd_max <- cluster_filter_compleet(
  masker = hazelmuis_bosrand_max,
  opp_laag = hazelmuis_bosrand_max,
  drempel_m2 = 0.2 * 10000,
  dist_m = 100,
  werkelijk = FALSE
)
hazelmuis_bosrand_finaal_max <- bosrand_geclusterd_max$raster

# Spoor B: OPP
if(!is.null(hazelmuis_voortplanting1_opp) && !is.null(hazelmuis_open1_opp)) {
  r_binair_vp1_opp <- terra::ifel(!is.na(hazelmuis_voortplanting1_opp) & hazelmuis_voortplanting1_opp > 0, 1, NA)
  r_binair_open1_opp <- terra::ifel(!is.na(hazelmuis_open1_opp) & hazelmuis_open1_opp > 0, 1, NA)
  
  bos_buffer_opp <- terra::buffer(r_binair_vp1_opp, width = 10)
  r_bosrand_mask_opp <- terra::mask(bos_buffer_opp, r_binair_open1_opp)
  hazelmuis_bosrand_opp <- terra::mask(hazelmuis_voortplanting1_opp, r_bosrand_mask_opp)
} else {
  hazelmuis_bosrand_opp <- template_VS * NA
}

r_binair_bosrand_opp <- terra::ifel(!is.na(hazelmuis_bosrand_opp) & hazelmuis_bosrand_opp > 0, 1, NA)
bosrand_geclusterd_opp <- cluster_filter_compleet(
  masker = r_binair_bosrand_opp,
  opp_laag = hazelmuis_bosrand_opp,
  drempel_m2 = 0.2 * 10000,
  dist_m = 100,
  werkelijk = TRUE
)
hazelmuis_bosrand_finaal_opp <- bosrand_geclusterd_opp$raster

rm(bosrand_geclusterd_max, bosrand_geclusterd_opp)
gc()
```


```{r Lineaire voortplantingsgebieden}
# ------------------------------------------------------------------------------
# STAP 4: Lineaire voortplantingsgebieden (Parallel)
# ------------------------------------------------------------------------------
message("-> Berekenen lineaire elementen (Drempel: 0.2 ha) + Bos-aansluiting...")

# Spoor A: MAX
lineair_geclusterd_max <- cluster_filter_compleet(
  masker = voortplanting_lineair_max,
  opp_laag = voortplanting_lineair_opp,
  drempel_m2 = 0.2 * 10000,
  dist_m = 100,
  werkelijk = FALSE
)
hazelmuis_lineair_gefilterd_max <- lineair_geclusterd_max$raster

if(!is.null(hazelmuis_lineair_gefilterd_max) && !is.null(hazelmuis_voortplanting1_max)) {
  bos_aansluiting_zone_max <- terra::buffer(hazelmuis_voortplanting1_max, width = 20)
  hazelmuis_leefgebied_lineair1_max <- terra::mask(hazelmuis_lineair_gefilterd_max, bos_aansluiting_zone_max)
} else {
  hazelmuis_leefgebied_lineair1_max <- template_VS * NA
}

# Spoor B: OPP
r_binair_lin_opp <- terra::ifel(!is.na(voortplanting_lineair_opp) & voortplanting_lineair_opp > 0, 1, NA)
lineair_geclusterd_opp <- cluster_filter_compleet(
  masker = r_binair_lin_opp,
  opp_laag = voortplanting_lineair_opp,
  drempel_m2 = 0.2 * 10000,
  dist_m = 100,
  werkelijk = TRUE
)
hazelmuis_lineair_gefilterd_opp <- lineair_geclusterd_opp$raster

if(!is.null(hazelmuis_lineair_gefilterd_opp) && !is.null(hazelmuis_voortplanting1_opp)) {
  r_binair_vp1_opp <- terra::ifel(!is.na(hazelmuis_voortplanting1_opp) & hazelmuis_voortplanting1_opp > 0, 1, NA)
  bos_aansluiting_zone_opp <- terra::buffer(r_binair_vp1_opp, width = 20)
  hazelmuis_leefgebied_lineair1_opp <- terra::mask(hazelmuis_lineair_gefilterd_opp, bos_aansluiting_zone_opp)
} else {
  hazelmuis_leefgebied_lineair1_opp <- template_VS * NA
}

rm(lineair_geclusterd_max, lineair_geclusterd_opp, r_binair_lin_opp)
gc()
```

```{r Combinatie}
# ------------------------------------------------------------------------------
# STAP 5: Combinatie tot Finaal Leefgebied (Parallel)
# ------------------------------------------------------------------------------
message("-> Combineren en finaliseren leefgebied (Minimaal 0.5 ha)...")

# Spoor A: MAX
hazelmuis_leefgebied1_max <- terra::cover(hazelmuis_bosrand_finaal_max, hazelmuis_leefgebied_lineair1_max)

leefgebied_finaal_geclusterd_max <- cluster_filter_compleet(
  masker = hazelmuis_leefgebied1_max,
  opp_laag = hazelmuis_leefgebied1_max,
  drempel_m2 = 0.5 * 10000,
  dist_m = 100,
  werkelijk = FALSE
)
hazelmuis_leefgebied_max <- leefgebied_finaal_geclusterd_max$raster

# Spoor B: OPP
b_opp_nul <- terra::ifel(is.na(hazelmuis_bosrand_finaal_opp), 0, hazelmuis_bosrand_finaal_opp)
l_opp_nul <- terra::ifel(is.na(hazelmuis_leefgebied_lineair1_opp), 0, hazelmuis_leefgebied_lineair1_opp)

hazelmuis_leefgebied1_opp_raw <- terra::clamp(b_opp_nul + l_opp_nul, upper = 1.0)
hazelmuis_leefgebied1_opp     <- terra::ifel(hazelmuis_leefgebied1_opp_raw > 0, hazelmuis_leefgebied1_opp_raw, NA)
r_binair_leef1_opp            <- terra::ifel(!is.na(hazelmuis_leefgebied1_opp) & hazelmuis_leefgebied1_opp > 0, 1, NA)

leefgebied_finaal_geclusterd_opp <- cluster_filter_compleet(
  masker = r_binair_leef1_opp,
  opp_laag = hazelmuis_leefgebied1_opp,
  drempel_m2 = 0.5 * 10000,
  dist_m = 100,
  werkelijk = TRUE
)
hazelmuis_leefgebied_opp <- leefgebied_finaal_geclusterd_opp$raster

# Finale toewijzingen
final_max <- hazelmuis_leefgebied_max
final_opp <- hazelmuis_leefgebied_opp

totaal_ha <- calc_ha_exact(final_opp)
cat("--- MODELLERING AFGEROND ---\n")
cat("Totaal berekend leefgebied Hazelmuis in studiegebied (ha):", totaal_ha, "\n")

rm(b_opp_nul, l_opp_nul, hazelmuis_leefgebied1_opp_raw, r_binair_leef1_opp)
gc()
```

# Resultaten en Statistiek
```{r Resultaten en statistiek}
# --- ECOLOOG-MAPPING: KOPPEL GENERIEKE VARIABELEN AAN HAZELMUIS RASTERS ---
cl_max         <- leefgebied_finaal_geclusterd$clusters
bwk_opp        <- hazelmuis_leefgebied1
final_max      <- hazelmuis_leefgebied
final_opp      <- hazelmuis_leefgebied

# EXPERT-EISSEN: Drempels kogelvrij instellen op 0.5 ha
oppervlakte_ha <- 0.5
drempel_m2     <- 5000

# Binnen masker is de binaire template van Voerstreek
binnen_masker  <- template_VS

# --- BEREKENING EN STATISTIEK ---
soort_format <- paste0(toupper(substr(soort, 1, 1)), tolower(substr(soort, 2, nchar(soort))))
soort_format <- gsub(" ", "_", soort_format)

waarnemingen_path <- paste0(here("data/input/Waarnemingen_Soorten/Voerstreek"), "/Waarnemingen_", soort_format, ".csv")

if (file.exists(waarnemingen_path)) {
  waarnemingen_raw <- read_delim(waarnemingen_path, delim = ";", escape_double = FALSE, trim_ws = TRUE)
  df_points_sf <- st_as_sf(waarnemingen_raw, coords = c("x", "y"), crs = 31370)
  
  waarneming_smear_sf <- st_buffer(df_points_sf, dist = buffer_m) %>% 
    st_union() %>% 
    st_sf() 
  
  waarneming_blobs_vect  <- vect(waarneming_smear_sf)
  waarneming_punten_vect <- vect(df_points_sf)
  has_observations <- TRUE
} else {
  message(paste("⚠️ Waarschuwing: Geen waarnemingsbestand gevonden op pad:", waarnemingen_path))
  df_points_sf <- st_sf(st_sfc(crs = 31370)) 
  waarneming_smear_sf <- st_sf(st_sfc(crs = 31370))
  waarneming_blobs_vect  <- vect(st_sf(st_sfc(crs = 31370)))
  waarneming_punten_vect <- vect(st_sf(st_sfc(crs = 31370)))
  has_observations <- FALSE
}

verspreiding_raster <- terra::rast(template_VS, vals = NA)
verspreiding_netwerk_count <- 0

if (!is.null(cl_max) && !all(is.na(terra::values(cl_max, mat=FALSE)))) {
  
  # 1. Haal de binaire frequenties/oppervlaktes op
  stats_max_raw <- terra::freq(cl_max)
  stats_max_raw <- as.data.frame(stats_max_raw)
  colnames(stats_max_raw) <- c("layer", "ID", "count")
  stats_max_raw$Pot_ha <- stats_max_raw$count * 0.01
  
  # 2. Haal de werkelijke habitat-oppervlaktes op
  zonal_raw <- terra::zonal(bwk_opp, cl_max, fun = "sum", na.rm = TRUE)
  zonal_raw <- as.data.frame(zonal_raw)
  colnames(zonal_raw) <- c("ID", "Werk_ha_raw")
  zonal_raw$Werk_ha <- zonal_raw$Werk_ha_raw * 0.01 
  
  stats_gecombineerd <- merge(stats_max_raw, zonal_raw, by = "ID", all.x = TRUE)
  stats_gecombineerd$Werk_ha[is.na(stats_gecombineerd$Werk_ha)] <- 0
  stats_gecombineerd$Werk_ha <- pmin(stats_gecombineerd$Werk_ha, stats_gecombineerd$Pot_ha)

  # 3. Filter nu pas op basis van de gekoppelde ID's
  ids_potentieel_totaal <- stats_gecombineerd$ID[stats_gecombineerd$Pot_ha >= oppervlakte_ha]
  ids_werkelijk_totaal  <- stats_gecombineerd$ID[(stats_gecombineerd$Werk_ha * 10000) >= drempel_m2]
  
  # 4. Bepaal welke clusters het Voerstreek (binnen) raken
  zonal_zone <- terra::zonal(binnen_masker, cl_max, fun = "max", na.rm = TRUE)
  zonal_zone <- as.data.frame(zonal_zone)
  colnames(zonal_zone) <- c("ID", "In_VS")
  ids_die_VS_raken <- zonal_zone$ID[!is.na(zonal_zone$In_VS) & zonal_zone$In_VS > 0]
  
  # 5. De opsplitsing
  ids_pot_bin   <- intersect(ids_potentieel_totaal, ids_die_VS_raken)
  ids_pot_buit  <- setdiff(ids_potentieel_totaal, ids_die_VS_raken)
  
  ids_werk_bin  <- intersect(ids_werkelijk_totaal, ids_die_VS_raken)
  ids_werk_buit <- setdiff(ids_werkelijk_totaal, ids_die_VS_raken)
  
  # A. ARPL Extractie
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_max <- terra::extract(cl_max, waarneming_blobs_vect, ID = FALSE)
    ids_raken_waarneming <- if(nrow(extract_ids_max) > 0) unique(na.omit(extract_ids_max[[1]])) else c()
  } else {
    ids_raken_waarneming <- c()
  }
  ids_actueel_relevant <- intersect(ids_werkelijk_totaal, ids_raken_waarneming)

  # B. Actuele Verspreiding Extractie
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_verspr <- terra::extract(cl_max, waarneming_punten_vect, ID = FALSE)
    ids_raken_punten <- if(nrow(extract_ids_verspr) > 0) unique(na.omit(extract_ids_verspr[[1]])) else c()
  } else {
    ids_raken_punten <- c()
  }
  ids_verspreiding_clusters <- intersect(ids_werkelijk_totaal, ids_raken_punten)
  
  if(length(ids_verspreiding_clusters) > 0) {
    masker_verspr <- cl_max %in% ids_verspreiding_clusters
    clusters_actueel <- terra::mask(final_max, masker_verspr)
    verspreiding_raster <- terra::ifel(!is.na(clusters_actueel), 1, NA)
    verspreiding_netwerk_count <- length(ids_verspreiding_clusters)
  }

  ha_pot_bin      <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_bin]), 2)
  ha_pot_buit     <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_buit]), 2)
  ha_werk_bin     <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_bin]), 2)
  ha_werk_buit    <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_buit]), 2)
  ha_arpl         <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_actueel_relevant]), 2)
  ha_verspreiding <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_verspreiding_clusters]), 2)

  if(length(ids_actueel_relevant) > 0) {
    arpl_raster <- terra::match(cl_max, ids_actueel_relevant)
    arpl_raster <- (!is.na(arpl_raster)) * 1
  } else {
    arpl_raster <- terra::rast(cl_max, vals = NA)
  }

} else {
  ids_pot_bin <- ids_pot_buit <- ids_werk_bin <- ids_werk_buit <- ids_actueel_relevant <- c()
  ha_pot_bin <- ha_pot_buit <- ha_werk_bin <- ha_werk_buit <- ha_arpl <- ha_verspreiding <- 0
  arpl_raster <- terra::rast(template_VS, vals = NA)
  stats_gecombineerd <- data.frame(ID = integer(), Pot_ha = numeric(), Werk_ha = numeric())
  zonal_zone <- data.frame(ID = integer(), In_VS = numeric())
}

tabel_totaal <- data.frame(
  Type = c("Maximaal Potentieel (Binnen)", "Maximaal Potentieel (Buiten)", 
           "Werkelijk Habitat (Binnen)", "Werkelijk Habitat (Buiten)",
           "Actueel Relevant (ARPL - Werkelijke ha)", "Actuele Verspreiding (Werkelijke ha)"),
  Clusters = c(length(ids_pot_bin), length(ids_pot_buit), 
               length(ids_werk_bin), length(ids_werk_buit), 
               length(ids_actueel_relevant), verspreiding_netwerk_count),
  Oppervlakte_ha = c(ha_pot_bin, ha_pot_buit, ha_werk_bin, ha_werk_buit, ha_arpl, ha_verspreiding)
)

tabel_totaal %>% 
  knitr::kable(caption = paste("Totaaloverzicht habitat & verspreiding:", soort)) %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover"))
```

# Interactieve kaart
```{r Interactieve kaart}
# --- INTERACTIEVE KAART: METADATA EN PARALLELLE GEGEVENSKOPPELING ---

if (has_observations && nrow(df_points_sf) > 0) {
  waarneming_smear_web <- st_transform(waarneming_smear_sf, 4326)
  points_web           <- st_transform(df_points_sf, 4326)
} else {
  waarneming_smear_web <- st_sf(st_sfc(crs = 4326))
  points_web           <- st_sf(st_sfc(crs = 4326))
}

# --- GEFIXT: DATA EN POPUPS RECHTSTREEKS GEKOPPELD AAN DE HAZELMUIS CLUSTERS ---
if (!is.null(cl_max) && !all(is.na(terra::values(cl_max, mat=FALSE))) && nrow(stats_gecombineerd) > 0) {
  cluster_info_A <- stats_gecombineerd %>%
    left_join(zonal_zone, by = "ID") %>% 
    mutate(
      Zone = ifelse(!is.na(In_VS) & In_VS > 0, "Binnen VS", "Buiten VS (Buffer)"),
      ARPL = ID %in% ids_actueel_relevant,
      Type = ifelse(ID %in% ids_werkelijk_totaal, "Werkelijk Habitat", "Potentieel (Niet Werkelijk)"),
      Popup = paste0(
        "<div style='font-family: Arial; min-width: 180px;'>",
        "<strong>Hazelmuis Leefgebied Netwerk</strong><br>",
        "<strong>Cluster ID:</strong> ", ID, "<br>",
        "<strong>Zone:</strong> ", Zone, "<br><hr>",
        "<strong>Status:</strong> ", Type, "<br>",
        "<strong>Actueel Relevant (ARPL):</strong> ", ifelse(ARPL, "<span style='color:green;font-weight:bold;'>JA</span>", "<span style='color:red;'>NEE</span>"), "<br>",
        "<strong>Potentieel Oppervlak:</strong> ", round(Pot_ha, 2), " ha<br>",
        "<strong>Werkelijk Habitat binnen dit netwerk:</strong> ", round(coalesce(Werk_ha, 0), 2), " ha",
        "</div>"
      )
    )

  cl_poly_A <- terra::as.polygons(cl_max, aggregate = TRUE) %>% st_as_sf() %>% st_transform(4326)
  cl_poly_final_A <- cl_poly_A %>% inner_join(cluster_info_A, by = setNames("ID", names(cl_poly_A)[1]))
} else {
  cl_poly_final_A <- st_sf(st_sfc(crs = 4326))
}

# --- GEFIXT: R_STATUS PIXEL-LAAG OPBOUW ZONDER SCRIPT-MISMATCH ---
if (!is.null(cl_max) && !all(is.na(terra::values(cl_max, mat=FALSE))) && nrow(stats_gecombineerd) > 0) {
  bin_w  <- if(length(ids_werk_bin) > 0) ids_werk_bin else -999
  buit_w <- if(length(ids_werk_buit) > 0) ids_werk_buit else -999
  bin_p  <- if(length(ids_pot_bin) > 0) ids_pot_bin else -999
  buit_p <- if(length(ids_pot_buit) > 0) ids_pot_buit else -999
  
  r_status <- terra::ifel(cl_max %in% bin_w, 3,
              terra::ifel(cl_max %in% buit_w, 4,
              terra::ifel(cl_max %in% bin_p, 1,
              terra::ifel(cl_max %in% buit_p, 2, NA))))
  
  r_web_status <- terra::project(r_status, "EPSG:4326", method = "near")
} else {
  r_web_status <- terra::rast(template_VS, vals = NA) %>% terra::project("EPSG:4326")
}

grens_web <- st_transform(st_as_sf(area_shape_proj), 4326)
bbox      <- st_bbox(grens_web)

pal_status <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = c("Werkelijk Habitat", "Potentieel (Niet Werkelijk)"))
pal_4 <- colorFactor(
  palette = c("#FF00FF", "#FF8C00", "#4daf4a", "#377eb8"), 
  levels = c("1", "2", "3", "4"),
  na.color = "transparent"
)

# --- LEAFLET INTERACTIEVE KAART OPBOUW ---
map <- leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

if(!all(is.na(terra::values(r_web_status, mat=FALSE)))) {
  map <- map %>% addRasterImage(terra::as.int(r_web_status), colors = pal_4, opacity = 0.5, group = "Habitat Status (Pixels)")
}

if(nrow(cl_poly_final_A) > 0) {
  map <- map %>% addPolygons(
    data = cl_poly_final_A,
    fillColor = ~pal_status(Type),
    fillOpacity = ~ifelse(ARPL, 0.6, 0.1), 
    color = ~pal_status(Type), 
    weight = ~ifelse(ARPL, 3, 0.5), 
    opacity = 1,
    popup = ~Popup,
    group = "Leefgebieden (Netwerk)"
  )
}

if(has_observations && nrow(df_points_sf) > 0) {
  map <- map %>%
    addPolygons(
      data = waarneming_smear_web,  
      color = "#377eb8", weight = 2, fill = FALSE, dashArray = "10, 5", 
      group = "Dispersiebereik (Smear)"
    ) %>%
    addCircles(
      data = points_web,  
      color = "black", weight = 1, fillColor = "yellow", fillOpacity = 1,
      radius = 15, group = "Waarnemingen"
    )
}

map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Leefgebieden (Netwerk)", "Habitat Status (Pixels)", 
                      "Dispersiebereik (Smear)", "Waarnemingen", "Grens Studiegebied"),
    options = layersControlOptions(collapsed = FALSE)
  )
```

```{r export}
base_dir <- here("data/output/Voerstreek/Rasters_Soorten")
folders <- list(
  potentie     = file.path(base_dir, "01_Maximale_Potentie"),
  werkelijk    = file.path(base_dir, "02_Werkelijke_Oppervlakte"),
  arpl         = file.path(base_dir, "03_ARPL"),
  verspreiding = file.path(base_dir, "04_Actuele_Verspreiding")
)

purrr::walk(folders, ~if (!dir.exists(.x)) dir.create(.x, recursive = TRUE))

if(length(ids_actueel_relevant) > 0) {
  arpl_export_rast <- cl_max %in% ids_actueel_relevant
  arpl_export_rast <- terra::ifel(arpl_export_rast, 1, NA)
} else {
  arpl_export_rast <- terra::rast(template_VS, vals = NA)
}

if(!all(is.na(terra::values(final_opp, mat=FALSE)))) {
  werkelijk_export_rast <- terra::ifel(!is.na(final_opp) & final_opp > 0, 1, NA)
} else {
  werkelijk_export_rast <- terra::rast(template_VS, vals = NA)
}

export_config <- list(
  list(rast = final_max,              naam = "Maximale_Potentie",        folder = folders$potentie,     type = "INT1U"),
  list(rast = werkelijk_export_rast, naam = "Werkelijke_Oppervlaktes", folder = folders$werkelijk,    type = "INT1U"), 
  list(rast = arpl_export_rast,      naam = "ARPL",                     folder = folders$arpl,         type = "INT1U"),
  list(rast = verspreiding_raster,   naam = "Actuele_Verspreiding",    folder = folders$verspreiding, type = "INT1U")
)

for(item in export_config) {
  file_path <- file.path(item$folder, paste0("Habitat_", item$naam, "_", soort, ".tif"))
  
  export_rast <- terra::deepcopy(item$rast)
  export_rast <- terra::mask(export_rast, area_buffer_fix)
  
  if(all(is.na(terra::values(export_rast, mat=FALSE)))) {
    export_rast <- terra::ifel(is.na(export_rast), 0, 0)
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  } else {
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  }
  
  writeRaster(export_rast, 
              filename = file_path, 
              overwrite = TRUE, 
              gdal = c("COMPRESS=LZW"), 
              datatype = item$type)
}
```
