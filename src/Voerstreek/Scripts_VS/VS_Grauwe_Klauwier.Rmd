---
title: "Leefgebiedenscript in R: Voerstreek"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)

library(here)
# CRUCIALE FIX: Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
# Hierdoor werken relatieve paden met hier() overal hetzelfde.
knitr::opts_knit$set(root.dir = here())

library(knitr)
library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(dplyr::first)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

# Exacte hectare-berekening behouden (cellSize voor 100% precisie)
calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

maak_kaart_laag <- function(res_list) {
  if(is.null(res_list)) return(NULL)
  kernen <- terra::ifel(!is.na(res_list$kern), 1, NA)
  if(!is.null(res_list$bouw)) {
    bouw <- terra::ifel(!is.na(res_list$bouw), 2, NA)
    finaal <- terra::cover(kernen, bouw)
  } else {
    finaal = kernen
  }
  return(finaal)
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  temp_mask <- masker
  if(dist_m > 0) temp_mask <- terra::buffer(masker, width = dist_m / 2)
  
  cl <- terra::patches(temp_mask, directions = 8, zeroAsNA = TRUE) %>% terra::mask(masker)
  
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    # CORRECTIE: Waarde is de som van fracties (1 pixel = 1.0). 
    # Vermenigvuldigen met 100 geeft de juiste Area in m2!
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100 # 1 binaire pixel = 100m2
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  masker_binair <- cl %in% voldoet_ids
  final_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  return(list(raster = terra::mask(masker, final_mask), clusters = cl))
}
```

# Soort informatie ophalen
```{r Soort informatie}
df <- read_excel(here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
soort <- "grauweklauwier"

resultaat <- df %>%
filter(Soort == soort) %>%
select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

# Variabelen definiëren
oppervlakte_ha <- resultaat$MinOpp_ha
afstand_m <- resultaat$AfstandBiotopen_m
buffer_m <- 10000 #resultaat$Dispersiecap_m[1]

print(resultaat)
rm(df, resultaat)
```

# Gebied voorbereiden (Voerstreek)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Voerstreek.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

# Inlezen van de CSV-sleutel (gebruikt om tabel-indices te mappen naar codes)
df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

# 2. Wijs handmatig het Lambert 72 coördinatensysteem toe (EPSG: 31370)
crs(area_shape) <- "EPSG:31370"

# 3. Projecteer nu naar het master_grid
area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_VS <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

# ==============================================================================
# ULTRA-SNELLE METHODE: Direct naar data.frame met celnummers in C++
# ==============================================================================
id_raster_VS_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_VS_masked <- mask(id_raster_VS_globale_values, area_buffer_fix)

message("-> Vertaaltabel bliksemsnel opbouwen via C++ dataframe extractie...")

# 'cells = TRUE' zorgt ervoor dat terra direct een kolom 'cell' (lokale id) 
# en een kolom met de waarde (globale id) genereert voor alle niet-NA cellen.
df_extractie <- as.data.frame(id_raster_VS_masked, cells = TRUE)

# Direct omzetten naar data.table en de kolommen juist benoemen
vertaal_df <- as.data.table(df_extractie)

# De kolommen heten nu standaard 'cell' en de naam van je raster (lyr.1 of gelijkaardig)
# We hernoemen ze direct naar jouw vertrouwde structuur:
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))

# Filter eventuele resterende NA's eruit
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_VS) <- NA
# --- DE CRUCIALE TEMPLATE REPARATIE ---
# In plaats van alles op NA te zetten, maken we een binaire 'mal' van het studiegebied.
# We rasteriseren de shapefile van het Voerstreek (area_buffer_fix) op het uitgesneden raster.
template_VS <- terra::rasterize(area_buffer_fix, id_raster_VS, field = 1, background = 0)

# Controleer of het nu wél cellen bevat:
cat("Gecorrigeerd aantal pixels in template_VS: ", sum(terra::values(template_VS) == 1, na.rm=TRUE), "\n")

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_VS_globale_values, id_raster_VS_masked)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(here("data/input/Raster_Vlaanderen/BWK_Tabel_Voerstreek_Plus_50km.rds"))
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

# Initialiseer de lijsten voor de verschillende habitat-bouwstenen
lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE DYNAMISCH VIA DATA.TABLE
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    # 1. Ontsnap speciale regex-tekens (zoals haakjes) om syntaxfouten te voorkomen
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    
    # 2. CRUCIALE FIX: Plak een '^' voor elke individuele code. 
    # Dit dwingt grepl() om alleen te matchen als de BWK-code BEGINT met deze letters.
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    # Filteren: EXACT-matches (geen letters voor/na) OF codes die BEGINNEN met de bevat-termen
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_VS <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_cel_som <- tabel_VS[, .(Oppervlakte = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
  
  r_match_type <- id_raster_VS * NA
  r_opp_type   <- id_raster_VS * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

# Uitpakken naar de exacte losse variabelen (Naadloze match met je database)
voortplanting_bwk_max <- lijst_matches[["voortplanting_bwk"]]
voortplanting_bwk_opp <- lijst_oppervlaktes[["voortplanting_bwk"]]
foerageerbiotoop_max <- lijst_matches[["foerageerbiotoop"]]
foerageerbiotoop_opp <- lijst_oppervlaktes[["foerageerbiotoop"]]
bebouwing_max <- lijst_matches[["bebouwing"]]
bebouwing_opp <- lijst_oppervlaktes[["bebouwing"]]

# Grote eindschoonmaak (vertaal_df weggooien mag nu pas!)

```

```{r infrastructurele stoorfactoren}
# ==============================================================================
# STAP 1: KOGELVRIJE BEBOUWINGS- EN WEGENFILTER CONFORM GRUTTO-METHODE
# ==============================================================================
message("-> GDX-specifieke bebouwingslagen isoleren...")

# De exacte GDX-lijst voor grauwe klauwier bebouwing
gdx_bebouw_codes <- c("u", "ua", "ua-", "uc", "ud", "ui", "ui-", "un", "un-", "ur", "ur-", "ur+", "uv", "uv-", "uv+")

# Filter de tabel op deze specifieke codes binnen jouw studiegebied
tabel_bebouw_VS <- tabel_vlaanderen[CODE %in% gdx_bebouw_codes & cel_id %in% studiegebied_globale_ids]
tabel_bebouw_som <- tabel_bebouw_VS[, .(Present = ifelse(sum(BWK_FRAC, na.rm = TRUE) > 0, 1, 0)), by = .(cel_id)]

r_bebouwing_raw <- id_raster_VS * NA

if(nrow(tabel_bebouw_som) > 0) {
  setnames(tabel_bebouw_som, "cel_id", "globale_id")
  tabel_bebouw_mapping <- merge(tabel_bebouw_som, vertaal_df, by = "globale_id", all.x = TRUE)
  tabel_bebouw_mapping <- tabel_bebouw_mapping[!is.na(lokale_id) & Present == 1]
  
  if(nrow(tabel_bebouw_mapping) > 0) {
    r_bebouwing_raw[tabel_bebouw_mapping$lokale_id] <- 1
  }
}

message("-> Grote bebouwingskernen (>= 10 ha) identificeren via 50m fuzzy clustering...")
# GDX: fuzzy clustering binnen 50m (in R: buffer van 50m om te koppelen)
r_bebouw_dik <- terra::buffer(r_bebouwing_raw, width = 50)
r_patches    <- terra::patches(r_bebouw_dik, directions = 8, zeroAsNA = TRUE)

# Isoleer de clusters groter dan of gelijk aan 10 ha (1000 pixels van 10x10m)
patch_freq         <- terra::freq(r_patches)
grote_clusters_ids <- patch_freq$value[patch_freq$count >= 1000]

r_masker_clusters           <- r_patches %in% grote_clusters_ids
r_bebouw_finaal_voor_buffer <- terra::mask(r_bebouwing_raw, r_masker_clusters)

# 100 meter afstandsberekening rond deze grote kernen
dist_bebouw_groot   <- terra::distance(r_bebouw_finaal_voor_buffer)
masker_bebouw_stoor <- dist_bebouw_groot <= 100

# --- WEGENSTORING INTEGREREN ---
message("-> Wegenstoring (100m) berekenen...")
if (file.exists(here("data/input/Raster_Vlaanderen/GRB_Wegverbinding_at.tif"))) {
  r_wegen_raw <- terra::rast(here("data/input/Raster_Vlaanderen/GRB_Wegverbinding_at.tif"))
  r_wegen     <- terra::resample(r_wegen_raw, id_raster_VS, method = "near") %>% terra::crop(id_raster_VS)
  
  weg_cats <- terra::cats(r_wegen)[[1]]
  gewenste_weg_labels <- c("autosnelweg", "weg met gescheiden rijbanen die geen autosnelweg is")
  ids_wegen_ok <- weg_cats$value[tolower(trimws(weg_cats$Label)) %in% gewenste_weg_labels]
  
  if(length(ids_wegen_ok) == 0) {
    ids_wegen_ok <- weg_cats$value[grepl("autosnelweg|gescheiden rijbanen", tolower(weg_cats$Label))]
  }
  
  r_grote_wegen  <- r_wegen %in% ids_wegen_ok
  dist_wegen     <- terra::distance(terra::ifel(r_grote_wegen == 1, 1, NA))
  masker_weg_stoor <- dist_wegen <= 100
} else {
  message("⚠️ GRB_Wegverbinding_at.tif niet gevonden. Wegenstoring overgeslagen.")
  masker_weg_stoor <- id_raster_VS == -999 # Geeft overal FALSE/0
}

# --- STOORFACTOREN TOEPASSEN OP KERNBIOTOOP ---
message("-> Stoorfactoren toepassen op de bruto voortplantingsbiotoop...")
# Indien binnen 100m van weg of grote bebouwing -> NA, anders behouden we de biotooppixel
voortplanting_gezuiverd_max <- terra::ifel(masker_bebouw_stoor == 1 | masker_weg_stoor == 1, NA, voortplanting_bwk_max)
voortplanting_gezuiverd_opp <- terra::mask(voortplanting_bwk_opp, voortplanting_gezuiverd_max)

# Schoonmaken van zware objecten uit het geheugen
# rm(tabel_bebouw_VS, tabel_bebouw_som, tabel_bebouw_mapping, r_bebouwing_raw, 
#    r_bebouw_dik, r_patches, patch_freq, grote_clusters_ids, r_masker_clusters, 
#    r_bebouw_finaal_voor_buffer, dist_bebouw_groot, masker_bebouw_stoor, masker_weg_stoor)
# if (exists("r_wegen_raw")) rm(r_wegen_raw, r_wegen, weg_cats, ids_wegen_ok, r_grote_wegen, dist_wegen)
# gc()
```

```{r grauweklauwier debug - visuele inspectie kaarten}
# ==============================================================================
# IMPACTANALYSE FILTER 1: GEREDDE VS. VERLOREN BIOTOOP VISUALISEREN
# ==============================================================================
message("-> Impactanalyse voor Stap 1 opbouwen...")

# Bereken wat er verloren gaat door bebouwing en wegen binnen de biotoop
r_verlies_bouw  <- terra::ifel(masker_bebouw_stoor == 1 & voortplanting_bwk_max == 1, 1, NA)
r_verlies_wegen <- terra::ifel(masker_weg_stoor == 1 & voortplanting_bwk_max == 1, 1, NA)

# Projecteer de resultaten naar Web Mercator (EPSG:4326) voor Leaflet
r_web_gered   <- terra::project(voortplanting_gezuiverd_max, "EPSG:4326", method = "near")
r_web_v_bouw  <- terra::project(r_verlies_bouw, "EPSG:4326", method = "near")
r_web_v_wegen <- terra::project(r_verlies_wegen, "EPSG:4326", method = "near")

# Bepaal de bounds op basis van de studiegebied-grens
grens_web <- sf::st_transform(sf::st_as_sf(area_shape_proj), 4326)
bbox <- sf::st_bbox(grens_web)

# Bouw de interactieve Leaflet-kaart
impact_map <- leaflet() %>%
  addTiles(group = "OSM (Wegenkaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satellietbeeld") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

# 1. Toon de geredde biotoop (Groen)
if (!all(is.na(terra::values(r_web_gered, mat=FALSE)))) {
  impact_map <- impact_map %>%
    addRasterImage(terra::as.int(r_web_gered), colors = "#2ca25f", opacity = 0.8, group = "Geredde Biotoop (Groen)")
}

# 2. Toon de verloren biotoop door bebouwing (Rood)
if (!all(is.na(terra::values(r_web_v_bouw, mat=FALSE)))) {
  impact_map <- impact_map %>%
    addRasterImage(terra::as.int(r_web_v_bouw), colors = "#de2d26", opacity = 0.8, group = "Verlies door Bebouwing (Rood)")
}

# 3. Toon de verloren biotoop door grote wegen (Paars)
if (!all(is.na(terra::values(r_web_v_wegen, mat=FALSE)))) {
  impact_map <- impact_map %>%
    addRasterImage(terra::as.int(r_web_v_wegen), colors = "#756bb1", opacity = 0.8, group = "Verlies door Wegen (Paars)")
}

# Toon de grens van je studiegebied (Zwarte lijn)
impact_map <- impact_map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(
    baseGroups = c("OSM (Wegenkaart)", "Satellietbeeld"),
    overlayGroups = c("Grens Studiegebied", "Geredde Biotoop (Groen)", "Verlies door Bebouwing (Rood)", "Verlies door Wegen (Paars)"),
    options = layersControlOptions(collapsed = FALSE)
  )

impact_map
```

```{r netwerkgrootte kernbiotoop}
# ==============================================================================
# STAP 2: CLUSTERING EN FILTERING KERNBIOTOOP (50M FUZZY / MINIMAAL 10 HA)
# ==============================================================================
message("-> Kernbiotoop clusteren op 50m en zeven op minimaal 10 ha...")

cl_kern <- cluster_filter_compleet(
  masker     = voortplanting_gezuiverd_max,
  opp_laag   = voortplanting_gezuiverd_opp,
  drempel_m2 = 10 * 10000, # Expertgrens van 10 hectare
  dist_m     = 50,
  werkelijk  = FALSE
)

r_kern_10ha_max <- cl_kern$raster
r_kern_10ha_opp <- terra::mask(voortplanting_gezuiverd_opp, r_kern_10ha_max)

```

```{r kleine landschapselementen via groenkaart}
# ==============================================================================
# STAP 3: EVALUATIE VAN LINEAIRE KLE'S (MINSTENS 500 METER)
# ==============================================================================
message("-> Groenkaart inlezen en synchroniseren...")
r_groen_raw <- terra::rast(here("data/input/ASCI Files/Groenkaart_2021.tif"))
r_groen     <- terra::resample(r_groen_raw, id_raster_VS, method = "near") %>% terra::crop(id_raster_VS)

r_hooggroen <- r_groen == 1
r_laaggroen <- r_groen == 2

# Lijnvormig groen afleiden
focal_hoog <- terra::focal(r_hooggroen, w = 3, fun = "sum", na.rm = TRUE)
focal_laag <- terra::focal(r_laaggroen, w = 3, fun = "sum", na.rm = TRUE)

r_lijn_hooggroen <- r_hooggroen == 1 & (focal_hoog >= 1 & focal_hoog <= 6)
r_lijn_laaggroen <- r_laaggroen == 1 & (focal_laag >= 1 & focal_laag <= 6)
r_kle_groenkaart <- (r_lijn_hooggroen == 1) | (r_lijn_laaggroen == 1)
r_kle_binair     <- terra::ifel(r_kle_groenkaart, 1, 0)

# INITIALISATIE: Altijd een leeg raster klaarzetten voor de opvolgende stappen
r_voortplanting_finaal_max <- id_raster_VS * NA
r_voortplanting_finaal_opp <- id_raster_VS * NA

if (!all(is.na(terra::values(r_kern_10ha_max, mat=FALSE)))) {
  cl_patches <- terra::patches(r_kern_10ha_max, directions = 8, zeroAsNA = TRUE)
  kle_counts <- terra::zonal(r_kle_binair, cl_patches, fun = "sum", na.rm = TRUE)
  colnames(kle_counts) <- c("ID", "KLE_pixels")
  
  # 500m / 10m resolutie = 50 pixels
  goedgekeurde_patch_ids <- kle_counts$ID[kle_counts$KLE_pixels >= 50]
  
  if (length(goedgekeurde_patch_ids) > 0) {
    masker_kle_ok <- cl_patches %in% goedgekeurde_patch_ids
    r_voortplanting_finaal_max <- terra::ifel(masker_kle_ok == 1, 1, NA)
    r_voortplanting_finaal_opp <- terra::mask(r_kern_10ha_opp, r_voortplanting_finaal_max)
  }
}

# ==============================================================================
# STAP 4: FOERAGEERHABITAT KOPPELEN EN GRASLAND MONOTONITEITSTEST
# ==============================================================================
# INITIALISATIE: Voorkom 'object not found' errors in de diagnose
r_leefgebied_bruto_max        <- id_raster_VS * NA
grauweklauwier_leefgebied_max <- id_raster_VS * NA
grauweklauwier_leefgebied_opp <- id_raster_VS * NA

if (!all(is.na(terra::values(r_voortplanting_finaal_max, mat=FALSE)))) {
  buffer_100m_kern <- terra::buffer(r_voortplanting_finaal_max, width = 100)
  r_foerageer_geclipt_max <- terra::mask(foerageerbiotoop_max, buffer_100m_kern)
  
  r_leefgebied_bruto_max <- (!is.na(r_voortplanting_finaal_max) & r_voortplanting_finaal_max == 1) | 
                            (!is.na(r_foerageer_geclipt_max) & r_foerageer_geclipt_max == 1)
  r_leefgebied_bruto_max <- terra::ifel(r_leefgebied_bruto_max == 1, 1, NA)
  
  cl_leefgebied <- cluster_filter_compleet(
    masker     = r_leefgebied_bruto_max,
    opp_laag   = foerageerbiotoop_opp, 
    drempel_m2 = 15 * 10000, 
    dist_m     = 50,
    werkelijk  = FALSE
  )
  r_leefgebied_bruto_max <- cl_leefgebied$raster
  
  if (!all(is.na(terra::values(r_leefgebied_bruto_max, mat=FALSE)))) {
    cl_final_patches <- terra::patches(r_leefgebied_bruto_max, directions = 8, zeroAsNA = TRUE)
    opp_totaal <- terra::zonal(foerageerbiotoop_opp, cl_final_patches, fun = "sum", na.rm = TRUE)
    colnames(opp_totaal) <- c("ID", "Totaal_ha")
    
    tabel_hp <- tabel_vlaanderen[CODE %in% gouden_namenlijst[grepl("^hp", gouden_namenlijst)] & cel_id %in% studiegebied_globale_ids]
    tabel_hp_som <- tabel_hp[, .(Opp_HP = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
    
    r_hp_fractie <- id_raster_VS * NA
    if (nrow(tabel_hp_som) > 0) {
      setnames(tabel_hp_som, "cel_id", "globale_id")
      tabel_hp_mapping <- merge(tabel_hp_som, vertaal_df, by = "globale_id", all.x = TRUE)[!is.na(lokale_id)]
      if (nrow(tabel_hp_mapping) > 0) r_hp_fractie[tabel_hp_mapping$lokale_id] <- tabel_hp_mapping$Opp_HP
    }
    
    opp_hp_zonal <- terra::zonal(r_hp_fractie, cl_final_patches, fun = "sum", na.rm = TRUE)
    colnames(opp_hp_zonal) <- c("ID", "HP_ha")
    
    grasland_check <- merge(opp_totaal, opp_hp_zonal, by = "ID", all.x = TRUE)
    grasland_check$HP_ha[is.na(grasland_check$HP_ha)] <- 0
    grasland_check$GraslandAandeel <- grasland_check$HP_ha / grasland_check$Totaal_ha
    
    goedgekeurde_leefgebied_ids <- grasland_check$ID[grasland_check$GraslandAandeel < 0.90]
    
    if (length(goedgekeurde_leefgebied_ids) > 0) {
      masker_leefgebied_ok <- cl_final_patches %in% goedgekeurde_leefgebied_ids
      grauweklauwier_leefgebied_max <- terra::ifel(masker_leefgebied_ok == 1, 1, NA) %>% terra::crop(template_VS)
      grauweklauwier_leefgebied_opp <- terra::mask(foerageerbiotoop_opp, grauweklauwier_leefgebied_max) %>% terra::crop(template_VS)
    }
  }
}
```

```{r grauweklauwier diagnose - habitat per stap opgelost}
# ==============================================================================
# DIAGNOSTISCHE BOUNDARY-CHECK PER STAP (CORRECTE VARIABELEN)
# ==============================================================================
message("=== RECONSTRUCTIE EN STATISTIEK PER STAP ===")

calc_ha_binair <- function(r) {
  if(is.null(r) || all(is.na(terra::values(r, mat=FALSE)))) return(0)
  return(sum(terra::values(r) == 1, na.rm = TRUE) * 0.01)
}

# We meten hier nu direct de echte modelspecificaties
ha_stap_0  <- calc_ha_binair(voortplanting_bwk_max)
ha_stap_1  <- calc_ha_binair(voortplanting_gezuiverd_max) # Directe meting van de gezuiverde biotoop!
ha_stap_2  <- calc_ha_binair(r_kern_10ha_max)
ha_stap_3  <- calc_ha_binair(r_voortplanting_finaal_max)
ha_stap_4a <- calc_ha_binair(r_leefgebied_bruto_max)
ha_stap_4b <- calc_ha_binair(grauweklauwier_leefgebied_max)

diagnose_tabel <- data.frame(
  Modellatiestap = c(
    "0. Bruto voortplantingsbiotoop (BWK)",
    "1. Kernbiotoop buiten 100m buffers (Wegen & Bouw)",
    "2. Gecentreerde clusters van minimaal 10 ha",
    "3. Kernen met minstens 500m Groenkaart-KLE",
    "4a. Totaal netwerk inclusief foerageerbiotoop (min. 15 ha)",
    "4b. Finaal leefgebied na de <90% grasland-test"
  ),
  Resterend_Habitat_ha = c(ha_stap_0, ha_stap_1, ha_stap_2, ha_stap_3, ha_stap_4a, ha_stap_4b)
)

diagnose_tabel %>% 
  knitr::kable(caption = "🔍 Diagnose: Waar verliest de grauwe klauwier zijn leefgebied?") %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover", "condensed"))
```

```{r grauweklauwier stap 4 - foerageerkoppeling en graslandfilter}
# ==============================================================================
# STAP 4: FOERAGEERHABITAT KOPPELEN (100M BUFFER) EN GRASLAND MONOTONITEITSTEST
# ==============================================================================
message("-> Wederzijdse afstandsinteractie berekenen (100m foerageerbereik)...")

grauweklauwier_leefgebied_max <- id_raster_VS * NA
grauweklauwier_leefgebied_opp <- id_raster_VS * NA

if (!all(is.na(terra::values(r_voortplanting_finaal_max, mat=FALSE)))) {
  # Goedgekeurde kernen (die de 500m Groenkaart-KLE-test hebben overleefd) 100m bufferen
  buffer_100m_kern <- terra::buffer(r_voortplanting_finaal_max, width = 100)
  
  # Foerageerhabitat is geldig binnen 100m van een goedgekeurde kern
  r_foerageer_geclipt_max <- terra::mask(foerageerbiotoop_max, buffer_100m_kern)
  
  # Voeg kern en geclipt foerageergebied samen via logische OF
  r_leefgebied_bruto_max <- (!is.na(r_voortplanting_finaal_max) & r_voortplanting_finaal_max == 1) | 
                            (!is.na(r_foerageer_geclipt_max) & r_foerageer_geclipt_max == 1)
  r_leefgebied_bruto_max <- terra::ifel(r_leefgebied_bruto_max == 1, 1, NA)
  
  # Filter het geheel op de minimale totale leefgebiedsgrootte van GDX: 15 hectare
  cl_leefgebied <- cluster_filter_compleet(
    masker     = r_leefgebied_bruto_max,
    opp_laag   = foerageerbiotoop_opp, 
    drempel_m2 = 15 * 10000, # Totale oppervlakte van het netwerk moet >= 15 ha zijn
    dist_m     = 50,
    werkelijk  = FALSE
  )
  r_leefgebied_netwerk_max <- cl_leefgebied$raster
  
  # --- CRUCIALE MONOTONITEITSTEST: HP-GRASLAND FILTER < 90% ---
  if (!all(is.na(terra::values(r_leefgebied_netwerk_max, mat=FALSE)))) {
    cl_final_patches <- terra::patches(r_leefgebied_netwerk_max, directions = 8, zeroAsNA = TRUE)
    
    # Bereken de totale oppervlakte per cluster
    opp_totaal <- terra::zonal(foerageerbiotoop_opp, cl_final_patches, fun = "sum", na.rm = TRUE)
    colnames(opp_totaal) <- c("ID", "Totaal_ha")
    
    # Isoleer de 'hp%' codes (cultuurgrasland) uit de database om het aandeel te berekenen
    tabel_hp <- tabel_vlaanderen[CODE %in% gouden_namenlijst[grepl("^hp", gouden_namenlijst)] & cel_id %in% studiegebied_globale_ids]
    tabel_hp_som <- tabel_hp[, .(Opp_HP = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
    
    r_hp_fractie <- id_raster_VS * NA
    if (nrow(tabel_hp_som) > 0) {
      setnames(tabel_hp_som, "cel_id", "globale_id")
      tabel_hp_mapping <- merge(tabel_hp_som, vertaal_df, by = "globale_id", all.x = TRUE)[!is.na(lokale_id)]
      if (nrow(tabel_hp_mapping) > 0) r_hp_fractie[tabel_hp_mapping$lokale_id] <- tabel_hp_mapping$Opp_HP
    }
    
    opp_hp_zonal <- terra::zonal(r_hp_fractie, cl_final_patches, fun = "sum", na.rm = TRUE)
    colnames(opp_hp_zonal) <- c("ID", "HP_ha")
    
    # Bereken het graslandaandeel per cluster
    grasland_check <- merge(opp_totaal, opp_hp_zonal, by = "ID", all.x = TRUE)
    grasland_check$HP_ha[is.na(grasland_check$HP_ha)] <- 0
    grasland_check$GraslandAandeel <- grasland_check$HP_ha / grasland_check$Totaal_ha
    
    # Alleen clusters waar het aandeel intensief grasland STRICT minder dan 90% is overleven!
    goedgekeurde_leefgebied_ids <- grasland_check$ID[grasland_check$GraslandAandeel < 0.90]
    
    if (length(goedgekeurde_leefgebied_ids) > 0) {
      masker_leefgebied_ok <- cl_final_patches %in% goedgekeurde_leefgebied_ids
      grauweklauwier_leefgebied_max <- terra::ifel(masker_leefgebied_ok == 1, 1, NA) %>% terra::crop(template_VS)
      grauweklauwier_leefgebied_opp <- terra::mask(foerageerbiotoop_opp, grauweklauwier_leefgebied_max) %>% terra::crop(template_VS)
    }
  }
}

message("--- GRAUWE KLAUWIER MODEL SUCCESVOL AFGEROND ---")
cat("Totaal stabiel leefgebied Grauwe Klauwier via Groenkaart (ha):", round(calc_ha_exact(grauweklauwier_leefgebied_opp), 2), "\n")


```

# Resultaten en Statistiek
# Resultaten en Statistiek
```{r Resultaten en statistiek}
# ==============================================================================
# ECOLOOG-MAPPING: BRUG TUSSEN SCRIPT EN STATISTIEK (MET VEILIGE DREMPEL_M2)
# ==============================================================================
# Definieer de drempel globaal op basis van de ingelezen Excel-ondergrens (MinOpp_ha)
drempel_m2 <- oppervlakte_ha * 10000

cl_max    <- grauweklauwier_leefgebied_max
final_max <- grauweklauwier_leefgebied_max
bwk_opp   <- grauweklauwier_leefgebied_opp
final_opp <- grauweklauwier_leefgebied_opp
binnen_masker <- template_VS

# --- BEREKENING EN STATISTIEK ---
soort_format <- paste0(toupper(substr(soort, 1, 1)), tolower(substr(soort, 2, nchar(soort))))
soort_format <- gsub(" ", "_", soort_format)

waarnemingen_path <- paste0(here("data/input/Waarnemingen_Soorten/Voerstreek/"), "/Waarnemingen_", soort_format, ".csv")

if (file.exists(waarnemingen_path)) {
  waarnemingen_raw <- read_delim(waarnemingen_path, delim = ";", escape_double = FALSE, trim_ws = TRUE)
  df_points_sf <- st_as_sf(waarnemingen_raw, coords = c("x", "y"), crs = 31370)
  
  waarneming_smear_sf <- st_buffer(df_points_sf, dist = buffer_m) %>% 
    st_union() %>% 
    st_sf() 
  
  waarneming_blobs_vect  <- vect(waarneming_smear_sf)
  waarneming_punten_vect <- vect(df_points_sf)
  has_observations <- TRUE
} else {
  message(paste("⚠️ Waarschuwing: Geen waarnemingsbestand gevonden op pad:", waarnemingen_path))
  df_points_sf <- st_sf(st_sfc(crs = 31370)) 
  waarneming_smear_sf <- st_sf(st_sfc(crs = 31370))
  waarneming_blobs_vect  <- vect(st_sf(st_sfc(crs = 31370)))
  waarneming_punten_vect <- vect(st_sf(st_sfc(crs = 31370)))
  has_observations <- FALSE
}

verspreiding_raster <- terra::rast(template_VS, vals = NA)
verspreiding_netwerk_count <- 0

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  
  stats_max_raw <- terra::freq(cl_max)
  stats_max_raw <- as.data.frame(stats_max_raw)
  colnames(stats_max_raw) <- c("layer", "ID", "count")
  stats_max_raw$Pot_ha <- stats_max_raw$count * 0.01
  
  zonal_raw <- terra::zonal(bwk_opp, cl_max, fun = "sum", na.rm = TRUE)
  zonal_raw <- as.data.frame(zonal_raw)
  colnames(zonal_raw) <- c("ID", "Werk_ha_raw")
  zonal_raw$Werk_ha <- zonal_raw$Werk_ha_raw * 0.01 
  
  stats_gecombineerd <- merge(stats_max_raw, zonal_raw, by = "ID", all.x = TRUE)
  stats_gecombineerd$Werk_ha[is.na(stats_gecombineerd$Werk_ha)] <- 0
  stats_gecombineerd$Werk_ha <- pmin(stats_gecombineerd$Werk_ha, stats_gecombineerd$Pot_ha)

  ids_potentieel_totaal <- stats_gecombineerd$ID[stats_gecombineerd$Pot_ha >= oppervlakte_ha]
  ids_werkelijk_totaal  <- stats_gecombineerd$ID[(stats_gecombineerd$Werk_ha * 10000) >= drempel_m2]
  
  zonal_zone <- terra::zonal(binnen_masker, cl_max, fun = "max", na.rm = TRUE)
  zonal_zone <- as.data.frame(zonal_zone)
  colnames(zonal_zone) <- c("ID", "In_VS")
  ids_die_VS_raken <- zonal_zone$ID[!is.na(zonal_zone$In_VS) & zonal_zone$In_VS > 0]
  
  ids_pot_bin   <- intersect(ids_potentieel_totaal, ids_die_VS_raken)
  ids_pot_buit  <- setdiff(ids_potentieel_totaal, ids_die_VS_raken)
  
  ids_werk_bin  <- intersect(ids_werkelijk_totaal, ids_die_VS_raken)
  ids_werk_buit <- setdiff(ids_werkelijk_totaal, ids_die_VS_raken)
  
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_max <- terra::extract(cl_max, waarneming_blobs_vect, ID = FALSE)
    ids_raken_waarneming <- if(nrow(extract_ids_max) > 0) unique(na.omit(extract_ids_max[[1]])) else c()
  } else {
    ids_raken_waarneming <- c()
  }
  ids_actueel_relevant <- intersect(ids_werkelijk_totaal, ids_raken_waarneming)

  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_verspr <- terra::extract(cl_max, waarneming_punten_vect, ID = FALSE)
    ids_raken_punten <- if(nrow(extract_ids_verspr) > 0) unique(na.omit(extract_ids_verspr[[1]])) else c()
  } else {
    ids_raken_punten <- c()
  }
  ids_verspreiding_clusters <- intersect(ids_werkelijk_totaal, ids_raken_punten)
  
  if(length(ids_verspreiding_clusters) > 0) {
    masker_verspr <- cl_max %in% ids_verspreiding_clusters
    clusters_actueel <- terra::mask(final_max, masker_verspr)
    verspreiding_raster <- terra::ifel(!is.na(clusters_actueel), 1, NA)
    verspreiding_netwerk_count <- length(ids_verspreiding_clusters)
  }

  ha_pot_bin      <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_bin]), 2)
  ha_pot_buit     <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_buit]), 2)
  ha_werk_bin     <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_bin]), 2)
  ha_werk_buit    <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_buit]), 2)
  ha_arpl         <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_actueel_relevant]), 2)
  ha_verspreiding <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_verspreiding_clusters]), 2)

  if(length(ids_actueel_relevant) > 0) {
    arpl_raster <- terra::match(cl_max, ids_actueel_relevant)
    arpl_raster <- (!is.na(arpl_raster)) * 1
  } else {
    arpl_raster <- terra::rast(cl_max, vals = NA)
  }

} else {
  ids_pot_bin <- ids_pot_buit <- ids_werk_bin <- ids_werk_buit <- ids_actueel_relevant <- c()
  ha_pot_bin <- ha_pot_buit <- ha_werk_bin <- ha_werk_buit <- ha_arpl <- ha_verspreiding <- 0
  arpl_raster <- terra::rast(template_VS, vals = NA)
  stats_gecombineerd <- data.frame(ID = integer(), Pot_ha = numeric(), Werk_ha = numeric())
  zonal_zone <- data.frame(ID = integer(), In_VS = numeric())
}

tabel_totaal <- data.frame(
  Type = c("Maximaal Potentieel (Binnen)", "Maximaal Potentieel (Buiten)", 
           "Werkelijk Habitat (Binnen)", "Werkelijk Habitat (Buiten)",
           "Actueel Relevant (ARPL - Werkelijke ha)", "Actuele Verspreiding (Werkelijke ha)"),
  Clusters = c(length(ids_pot_bin), length(ids_pot_buit), 
               length(ids_werk_bin), length(ids_werk_buit), 
               length(ids_actueel_relevant), verspreiding_netwerk_count),
  Oppervlakte_ha = c(ha_pot_bin, ha_pot_buit, ha_werk_bin, ha_werk_buit, ha_arpl, ha_verspreiding)
)

tabel_totaal %>% 
  knitr::kable(caption = paste("Totaaloverzicht habitat & verspreiding:", soort)) %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover"))
```

# Interactieve kaart
```{r Interactieve kaart}
if (has_observations && nrow(df_points_sf) > 0) {
  waarneming_smear_web <- st_transform(waarneming_smear_sf, 4326)
  points_web           <- st_transform(df_points_sf, 4326)
} else {
  waarneming_smear_web <- st_sf(st_sfc(crs = 4326))
  points_web           <- st_sf(st_sfc(crs = 4326))
}

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  cluster_info <- stats_gecombineerd %>%
    left_join(zonal_zone, by = "ID") %>% 
    mutate(
      Zone = ifelse(!is.na(In_VS) & In_VS > 0, "Binnen VS", "Buiten VS (Buffer)"),
      ARPL = ID %in% ids_actueel_relevant,
      Type = ifelse(ID %in% ids_werkelijk_totaal, "Werkelijk Habitat", "Potentieel (Niet Werkelijk)"),
      Popup = paste0(
        "<div style='font-family: Arial; min-width: 180px;'>",
        "<strong>Cluster ID:</strong> ", ID, "<br>",
        "<strong>Zone:</strong> ", Zone, "<br><hr>",
        "<strong>Status:</strong> ", Type, "<br>",
        "<strong>Actueel Relevant (ARPL):</strong> ", 
        ifelse(ARPL, "<span style='color:green; font-weight:bold;'>JA</span>", 
                     "<span style='color:red;'>NEE (Geen waarneming of onder drempel)</span>"), "<br><br>",
        "<strong>Potentieel Oppervlak:</strong> ", round(Pot_ha, 2), " ha<br>",
        "<strong>Werkelijk Habitat:</strong> ", round(coalesce(Werk_ha, 0), 2), " ha",
        "</div>"
      )
    )

  cl_poly <- terra::as.polygons(cl_max, aggregate = TRUE) %>% 
    st_as_sf() %>% 
    st_transform(4326)

  cl_poly_final <- cl_poly %>%
    inner_join(cluster_info, by = setNames("ID", names(cl_poly)[1]))
} else {
  cl_poly_final <- st_sf(st_sfc(crs = 4326))
}

r_status <- terra::ifel(!is.na(final_opp), 1, terra::ifel(!is.na(final_max), 2, NA))
r_web_status         <- terra::project(r_status, "EPSG:4326", method = "near")
grens_web            <- st_transform(st_as_sf(area_shape_proj), 4326)
bbox                 <- st_bbox(grens_web)

pal_status <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = c("Werkelijk Habitat", "Potentieel (Niet Werkelijk)"))
pal_4 <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = 1:2, na.color = "transparent")

map <- leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

if(!all(is.na(terra::values(r_web_status, mat=FALSE)))) {
  map <- map %>% addRasterImage(terra::as.int(r_web_status), colors = pal_4, opacity = 0.5, group = "Habitat Status (Pixels)")
}

if(nrow(cl_poly_final) > 0) {
  map <- map %>% addPolygons(
    data = cl_poly_final,
    fillColor = ~pal_status(Type),
    fillOpacity = ~ifelse(ARPL, 0.6, 0.1), 
    color = ~pal_status(Type), 
    weight = ~ifelse(ARPL, 3, 0.5), 
    opacity = 1,
    popup = ~Popup,
    group = "Leefgebieden"
  )
}

if(has_observations && nrow(df_points_sf) > 0) {
  map <- map %>%
    addPolygons(
      data = waarneming_smear_web,  
      color = "#377eb8", 
      weight = 2, 
      fill = FALSE,       
      dashArray = "10, 5", 
      group = "Dispersiebereik (Smear)"
    ) %>%
    addCircles(
      data = points_web,  
      color = "black", weight = 1, fillColor = "yellow", fillOpacity = 1,
      radius = 15, group = "Waarnemingen"
    )
}

map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Leefgebieden", "Habitat Status (Pixels)", 
                      "Dispersiebereik (Smear)", "Waarnemingen", "Grens Studiegebied"),
    options = layersControlOptions(collapsed = FALSE)
  )
```

```{r export}
base_dir <- here("data/output/Voerstreek/Rasters_Soorten")
folders <- list(
  potentie     = file.path(base_dir, "01_Maximale_Potentie"),
  werkelijk    = file.path(base_dir, "02_Werkelijke_Oppervlakte"),
  arpl         = file.path(base_dir, "03_ARPL"),
  verspreiding = file.path(base_dir, "04_Actuele_Verspreiding")
)

purrr::walk(folders, ~if (!dir.exists(.x)) dir.create(.x, recursive = TRUE))

if(length(ids_actueel_relevant) > 0) {
  arpl_export_rast <- cl_max %in% ids_actueel_relevant
  arpl_export_rast <- terra::ifel(arpl_export_rast, 1, NA)
} else {
  arpl_export_rast <- terra::rast(template_VS, vals = NA)
}

if(!all(is.na(terra::values(final_opp, mat=FALSE)))) {
  werkelijk_export_rast <- terra::ifel(!is.na(final_opp) & final_opp > 0, 1, NA)
} else {
  werkelijk_export_rast <- terra::rast(template_VS, vals = NA)
}

export_config <- list(
  list(rast = final_max,              naam = "Maximale_Potentie",        folder = folders$potentie,     type = "INT1U"),
  list(rast = werkelijk_export_rast, naam = "Werkelijke_Oppervlaktes", folder = folders$werkelijk,    type = "INT1U"), 
  list(rast = arpl_export_rast,      naam = "ARPL",                     folder = folders$arpl,        type = "INT1U"),
  list(rast = verspreiding_raster,   naam = "Actuele_Verspreiding",    folder = folders$verspreiding, type = "INT1U")
)

for(item in export_config) {
  file_path <- file.path(item$folder, paste0("Habitat_", item$naam, "_", soort, ".tif"))
  
  export_rast <- terra::deepcopy(item$rast)
  export_rast <- terra::mask(export_rast, area_buffer_fix)
  
  if(all(is.na(terra::values(export_rast, mat=FALSE)))) {
    export_rast <- terra::ifel(is.na(export_rast), 0, 0)
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  } else {
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  }
  
  writeRaster(export_rast, 
              filename = file_path, 
              overwrite = TRUE, 
              gdal = c("COMPRESS=LZW"), 
              datatype = item$type)
}
```
