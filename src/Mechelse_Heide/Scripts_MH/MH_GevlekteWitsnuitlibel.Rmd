---
title: "Leefgebiedenscript in R: Mechelse Heide"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)

library(here)
# CRUCIALE FIX: Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
# Hierdoor werken relatieve paden met hier() overal hetzelfde.
knitr::opts_knit$set(root.dir = here())

library(knitr)
library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

# Exacte hectare-berekening behouden (cellSize voor 100% precisie)
calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

# Geoptimaliseerde get_stats helper
get_stats <- function(cat_id, label) {
  cid <- cat_id
  target_mask <- r_status == cid
  f_pix <- terra::freq(target_mask)
  n_pix <- if(nrow(f_pix) > 0) sum(f_pix$count[f_pix$value == 1], na.rm=TRUE) else 0
  area_ha <- (n_pix * 100) / 10000
  
  cl_src <- if(cid %in% c(1, 2)) cl_id_max else cl_id_opp
  cl_zone <- terra::mask(cl_src, target_mask)
  f_cl <- terra::freq(cl_zone)
  n_cl <- if(!is.null(f_cl) && nrow(f_cl) > 0) nrow(f_cl) else 0
  
  return(data.frame(Type = label, Clusters = n_cl, Oppervlakte_ha = round(area_ha, 2)))
}

maak_kaart_laag <- function(res_list) {
  if(is.null(res_list)) return(NULL)
  kernen <- terra::ifel(!is.na(res_list$kern), 1, NA)
  if(!is.null(res_list$bouw)) {
    bouw <- terra::ifel(!is.na(res_list$bouw), 2, NA)
    finaal <- terra::cover(kernen, bouw)
  } else {
    finaal = kernen
  }
  return(finaal)
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  # 1. Controleer of het raster leeg is
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  # ----------------------------------------------------------------------------
  # STAP 1: VEILIGE & SNELLER NETWERKVORMING ZONDER RAM-CRASH
  # ----------------------------------------------------------------------------
  if (dist_m > 0) {
    # Maak binaire kaart (1 = biotoop, NA = rest)
    r_binair <- terra::ifel(!is.na(masker) & masker > 0, 1, NA)
    
    # Buffer de BINAIR kaart (dit kost vrijwel geen geheugen!)
    # Volle afstand dist_m zorgt dat plukjes binnen dist_m gegarandeerd samensmelten
    r_buffered <- terra::buffer(r_binair, width = dist_m)
    
    # Maak unieke Netwerk-ID's op de gebufferde zones
    cl_network <- terra::patches(r_buffered, directions = 8, zeroAsNA = TRUE)
    
    # Snijd de Netwerk-ID's direct terug naar waar de originele biotooppixels liggen
    cl_biotoop_only <- terra::mask(cl_network, masker)
  } else {
    cl_network <- terra::patches(masker, directions = 8, zeroAsNA = TRUE)
    cl_biotoop_only <- cl_network
  }
  
  # ----------------------------------------------------------------------------
  # STAP 2: OPPERVLAKTE-OPTELSOM PER GEKOPPELD NETWERK
  # ----------------------------------------------------------------------------
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl_biotoop_only, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl_biotoop_only)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = masker * NA))
  
  # ----------------------------------------------------------------------------
  # STAP 3: FILTEREN OP TOTALE NETWERK-OPPERVLAKTE >= DREMPEL
  # ----------------------------------------------------------------------------
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = masker * NA))
  
  # Behaal alleen de winnende netwerk-ID's
  masker_binair <- cl_biotoop_only %in% voldoet_ids
  final_network_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  r_finaal  <- terra::mask(masker, final_network_mask)
  cl_finaal <- terra::mask(cl_biotoop_only, r_finaal) 
  
  return(list(raster = r_finaal, clusters = cl_finaal))
}
```

# Soort informatie ophalen
```{r Soort informatie}
# Hardgecodeerde parameters voor Gevlekte witsnuitlibel volgens de experttabel van Greet
soort <- "gevlektewitsnuitlibel"

# Parameters uit de tabel:
buffer_m            <- 20000  # Dispersiecapaciteit voor smear/buffer
straal_water_m      <- 200    # Afsnij-afstand van land rond het water (200m)

# Waterdrempels (Min. 0.02 ha EN Max. 1 ha)
drempel_water_min_m2 <- 200    # Min. 200 m² (0.02 ha)
drempel_water_max_m2 <- 10000  # Max. 10.000 m² (1.0 ha)
dist_water_m         <- 10     # 10m overbrugging voor water

# Landbiotoop drempels
drempel_land_m2     <- 50000   # Min. 5 ha land
dist_land_m          <- 10     # 10m overbrugging voor aanpalend land

drempel_ruim_m2     <- 300000  # Min. 30 ha ruim land
dist_ruim_m          <- 50     # 50m overbrugging voor ruim land
```

# Gebied voorbereiden (Mechelse Heide)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Mechelse_Heide.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Uitsnede maken en lokale/globale cel-IDs berekenen...")

id_raster_MH <- crop(master_grid, area_buffer_fix, snap = "near")
lokale_coords <- terra::xyFromCell(id_raster_MH, 1:ncell(id_raster_MH))
globale_ids   <- terra::cellFromXY(master_grid, lokale_coords)

vertaal_df <- data.table(
  lokale_id  = 1:ncell(id_raster_MH),
  globale_id = globale_ids
)

studiegebied_globale_ids <- unique(vertaal_df$globale_id[!is.na(vertaal_df$globale_id)])

template_MH <- terra::rasterize(area_buffer_fix, id_raster_MH, field = 1, background = 0)
grens_web   <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(lokale_coords, globale_ids)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

tabel_vlaanderen <- readRDS(here("data/input/Raster_Vlaanderen/BWK_Tabel_Mechelse_Heide_Plus_50km.rds"))
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

lijst_matches      <- list()
lijst_oppervlaktes <- list()

for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  tabel_MH <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  # 1. Ontdubbel op cel- EN code-niveau om dubbele matches in de crosswalk uit te sluiten
tabel_MH_unique <- unique(tabel_MH, by = c("cel_id", "CODE"))

# 2. Bereken de som per cel en cap de maximale bedekkingsgraad op 100% (1.0)
tabel_cel_som <- tabel_MH_unique[, .(
  Oppervlakte = pmin(sum(BWK_FRAC, na.rm = TRUE), 1.0)
), by = .(cel_id)]
  
  r_match_type <- id_raster_MH * NA
  r_opp_type   <- id_raster_MH * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

voortplanting_bwk_max <- lijst_matches[["voortplanting_bwk"]]
voortplanting_bwk_opp <- lijst_oppervlaktes[["voortplanting_bwk"]]
land_nabij_max        <- lijst_matches[["land_nabij"]]
land_nabij_opp        <- lijst_oppervlaktes[["land_nabij"]]
land_ruim_max         <- lijst_matches[["land_ruim"]]
land_ruim_opp         <- lijst_oppervlaktes[["land_ruim"]]
struweel1_max         <- lijst_matches[["struweel1"]]
struweel1_opp         <- lijst_oppervlaktes[["struweel1"]]

raster_simpel_final   <- id_raster_MH

rm(resultaten_gegroepeerd, df_nieuw, lijst_matches, lijst_oppervlaktes, tabel_vlaanderen)
gc()
```

# Voortplantingsbiotoop
```{r Watervlakken en Kleine Waters}
message("-> Watervlakken shapefile lokaal inlezen & bundelen met BWK-water...")

watervlakken_v <- vect(here("data/input/ASCI Files/watervlakken2024.shp"))
watervlakken_proj <- project(watervlakken_v, crs(template_MH))

area_buffer_clean  <- makeValid(area_buffer_fix) %>% aggregate()
watervlakken_clean <- makeValid(watervlakken_proj)
watervlakken_MH    <- crop(watervlakken_clean, area_buffer_clean)

# Fuzzy clustering op kleine wateren (<= 200m²)
watervlakken_buffered <- buffer(watervlakken_MH, width = 5)
watervlakken_fuzzy_clusters <- aggregate(watervlakken_buffered, by = NULL)
watervlakken_fuzzy_clusters$fuzzy_area_m2 <- terra::expanse(watervlakken_fuzzy_clusters)

kleine_clusters_v <- watervlakken_fuzzy_clusters[watervlakken_fuzzy_clusters$fuzzy_area_m2 <= 200, ]

if (nrow(kleine_clusters_v) > 0) {
  kleine_watervlakken_finaal <- crop(watervlakken_MH, kleine_clusters_v)
  if (nrow(kleine_watervlakken_finaal) > 0) {
    r_witsnuit_kleinwater        <- terra::rasterize(kleine_watervlakken_finaal, template_MH, field = 1, background = NA)
    r_witsnuit_waterbiotoop1     <- terra::cover(voortplanting_bwk_max, r_witsnuit_kleinwater)
    r_witsnuit_waterbiotoop1_opp <- terra::cover(voortplanting_bwk_opp, r_witsnuit_kleinwater)
  } else {
    r_witsnuit_waterbiotoop1     <- voortplanting_bwk_max
    r_witsnuit_waterbiotoop1_opp <- voortplanting_bwk_opp
  }
} else {
  r_witsnuit_waterbiotoop1     <- voortplanting_bwk_max
  r_witsnuit_waterbiotoop1_opp <- voortplanting_bwk_opp
}

suppressWarnings(
  rm(watervlakken_v, watervlakken_proj, area_buffer_clean, watervlakken_clean, watervlakken_MH, 
     watervlakken_buffered, watervlakken_fuzzy_clusters, kleine_clusters_v, kleine_watervlakken_finaal, r_witsnuit_kleinwater)
)
gc()
```

```{r 3 Buren}
message("-> Water filteren: 0.02 ha <= Opp <= 1 ha (10m netwerk) + 3-Buren dichtheidseis...")

# ==============================================================================
# SPOOR A: MAXIMALE POTENTIE WATER
# ==============================================================================
if (!is.null(r_witsnuit_waterbiotoop1) && !all(is.na(terra::values(r_witsnuit_waterbiotoop1, mat = FALSE)))) {
  
  # A. Vorm waterclusters via 10m overbrugging
  res_water_max <- cluster_filter_compleet(
    masker     = r_witsnuit_waterbiotoop1, 
    opp_laag   = r_witsnuit_waterbiotoop1, 
    drempel_m2 = drempel_water_min_m2, # Min 200 m² (0.02 ha)
    dist_m     = dist_water_m,          # 10m overbrugging
    werkelijk  = FALSE
  )
  r_water_cand_max  <- res_water_max$raster
  cl_water_cand_max <- res_water_max$clusters
  
  # B. MAXIMALE BOVENGRENS VAN 1 HA (10.000 m²)
  if (!all(is.na(terra::values(cl_water_cand_max, mat = FALSE)))) {
    f_water_max <- terra::freq(cl_water_cand_max)
    f_water_max$Area_m2 <- f_water_max$count * 100
    
    valide_opp_ids_max <- f_water_max$value[f_water_max$Area_m2 <= drempel_water_max_m2] # <= 1 ha
    
    if (length(valide_opp_ids_max) > 0) {
      r_water_cand_max <- terra::mask(r_water_cand_max, terra::ifel(cl_water_cand_max %in% valide_opp_ids_max, 1, NA))
    } else { r_water_cand_max <- template_MH * NA }
  } else { r_water_cand_max <- template_MH * NA }
  
  # C. ORIGINELE 3-BUREN CHECK (Minstens 3 naburige vennen binnen 200m)
  if (!all(is.na(terra::values(r_water_cand_max, mat = FALSE)))) {
    r_water_cl_max <- terra::patches(r_water_cand_max, directions = 8, zeroAsNA = TRUE)
    
    if (!all(is.na(terra::values(r_water_cl_max, mat = FALSE)))) {
      watervlakken_poly <- terra::as.polygons(r_water_cl_max, dissolve = TRUE)
      
      if (!is.null(watervlakken_poly) && nrow(watervlakken_poly) > 0) {
        centroids <- terra::centroids(watervlakken_poly)
        centroids_buffer <- terra::buffer(centroids, width = 200)
        intersect_matrix <- terra::relate(centroids_buffer, centroids, "intersects")
        cluster_counts   <- rowSums(intersect_matrix)
        
        watervlakken_poly$unieke_buren_count <- cluster_counts
        goedgekeurde_clusters_poly <- watervlakken_poly[watervlakken_poly$unieke_buren_count >= 4, ] # Self + 3 buren
        
        if (nrow(goedgekeurde_clusters_poly) > 0) {
          gevlektewitsnuitlibel_voortplanting_max <- terra::rasterize(goedgekeurde_clusters_poly, template_MH, field = 1, background = NA)
          gevlektewitsnuitlibel_water_finaal_max  <- gevlektewitsnuitlibel_voortplanting_max
        } else {
          gevlektewitsnuitlibel_voortplanting_max <- template_MH * NA
          gevlektewitsnuitlibel_water_finaal_max  <- template_MH * NA
        }
      } else {
        gevlektewitsnuitlibel_voortplanting_max <- template_MH * NA
        gevlektewitsnuitlibel_water_finaal_max  <- template_MH * NA
      }
    } else {
      gevlektewitsnuitlibel_voortplanting_max <- template_MH * NA
      gevlektewitsnuitlibel_water_finaal_max  <- template_MH * NA
    }
  } else {
    gevlektewitsnuitlibel_voortplanting_max <- template_MH * NA
    gevlektewitsnuitlibel_water_finaal_max  <- template_MH * NA
  }

} else {
  gevlektewitsnuitlibel_voortplanting_max <- template_MH * NA
  gevlektewitsnuitlibel_water_finaal_max  <- template_MH * NA
}


# ==============================================================================
# SPOOR B: WERKELIJKE OPPERVLAKTE WATER
# ==============================================================================
if (!is.null(r_witsnuit_waterbiotoop1_opp) && !all(is.na(terra::values(r_witsnuit_waterbiotoop1_opp, mat = FALSE)))) {
  r_binair_water_opp <- terra::ifel(!is.na(r_witsnuit_waterbiotoop1_opp) & r_witsnuit_waterbiotoop1_opp > 0, 1, NA)
  
  res_water_opp <- cluster_filter_compleet(
    masker     = r_binair_water_opp, 
    opp_laag   = r_witsnuit_waterbiotoop1_opp, 
    drempel_m2 = drempel_water_min_m2, 
    dist_m     = dist_water_m, # 10m overbrugging
    werkelijk  = TRUE
  )
  r_water_cand_opp  <- res_water_opp$raster
  cl_water_cand_opp <- res_water_opp$clusters
  
  # BOVENGRENS 1 HA TOEPASSEN OP WERKELIJKE OPPERVLAKTE
  if (!all(is.na(terra::values(cl_water_cand_opp, mat = FALSE)))) {
    stats_water_opp <- terra::zonal(r_water_cand_opp, cl_water_cand_opp, fun = "sum", na.rm = TRUE)
    colnames(stats_water_opp) <- c("ID", "Waarde")
    stats_water_opp$Area_m2 <- stats_water_opp$Waarde * 100
    
    valide_opp_ids_opp <- stats_water_opp$ID[stats_water_opp$Area_m2 <= drempel_water_max_m2]
    
    if (length(valide_opp_ids_opp) > 0) {
      r_water_cand_opp <- terra::mask(r_water_cand_opp, terra::ifel(cl_water_cand_opp %in% valide_opp_ids_opp, 1, NA))
    } else { r_water_cand_opp <- template_MH * NA }
  } else { r_water_cand_opp <- template_MH * NA }
  
  if (!all(is.na(terra::values(gevlektewitsnuitlibel_voortplanting_max, mat = FALSE)))) {
    gevlektewitsnuitlibel_voortplanting_opp <- terra::mask(r_water_cand_opp, gevlektewitsnuitlibel_voortplanting_max)
    gevlektewitsnuitlibel_water_finaal_opp <- gevlektewitsnuitlibel_voortplanting_opp
  } else {
    gevlektewitsnuitlibel_voortplanting_opp <- template_MH * NA
    gevlektewitsnuitlibel_water_finaal_opp <- template_MH * NA
  }
} else {
  gevlektewitsnuitlibel_voortplanting_opp <- template_MH * NA
  gevlektewitsnuitlibel_water_finaal_opp <- template_MH * NA
}

rm(r_water_clusters, r_witsnuit_waterbiotoop1, r_witsnuit_waterbiotoop1_opp)
gc()
```

```{r Foerageergebied Afbakenen}
# ==============================================================================
# WATER-FIRST LANDBIOTOOP FILTERING (GEVLEKTE WITSNUITLIBEL)
# ==============================================================================
message("-> Landbiotoop filteren (10m & 50m netwerken, 30% bos) & hard afsnijden op ", straal_water_m, "m van water...")

fix_bos_raster <- function(r_bos, template) {
  if (is.null(r_bos) || !inherits(r_bos, "SpatRaster")) {
    return(terra::rast(template, vals = NA))
  }
  return(terra::ifel(!is.na(r_bos) & r_bos > 0, 1, NA))
}

# --- STAP 1: RUIM LANDBIOTOOP FILTEREN (MIN. 30 HA MET 50M OVERBRUGGING) ---
if (exists("land_ruim_max") && !all(is.na(terra::values(land_ruim_max, mat = FALSE)))) {
  res_ruim_max <- cluster_filter_compleet(
    masker     = land_ruim_max,
    opp_laag   = land_ruim_max,
    drempel_m2 = drempel_ruim_m2, # 30 ha
    dist_m     = dist_ruim_m,     # 50m overbrugging
    werkelijk  = FALSE
  )
  land_ruim_ok_max <- res_ruim_max$raster
} else { land_ruim_ok_max <- template_MH * NA }

if (exists("land_ruim_opp") && !all(is.na(terra::values(land_ruim_opp, mat = FALSE)))) {
  r_bin_ruim_opp <- terra::ifel(!is.na(land_ruim_opp) & land_ruim_opp > 0, 1, NA)
  res_ruim_opp <- cluster_filter_compleet(
    masker     = r_bin_ruim_opp,
    opp_laag   = land_ruim_opp,
    drempel_m2 = drempel_ruim_m2, # 30 ha
    dist_m     = dist_ruim_m,     # 50m overbrugging
    werkelijk  = TRUE
  )
  land_ruim_ok_opp <- res_ruim_opp$raster
} else { land_ruim_ok_opp <- template_MH * NA }


# --- STAP 2: SPOOR A - MAXIMALE POTENTIE (200M WATER AFSNIJDING) ---
if (exists("gevlektewitsnuitlibel_voortplanting_max") && !all(is.na(terra::values(gevlektewitsnuitlibel_voortplanting_max, mat = FALSE)))) {
  
  poly_water_max  <- terra::as.polygons(gevlektewitsnuitlibel_voortplanting_max, aggregate = TRUE)
  poly_buffer_max <- terra::buffer(poly_water_max, width = straal_water_m) # 200m!
  
  if (exists("land_nabij_max") && !all(is.na(terra::values(land_nabij_max, mat = FALSE)))) {
    
    # 1. HARDE WATER-FIRST AFSNIJDING OP 200M
    land_afgesneden_max <- terra::mask(land_nabij_max, poly_buffer_max)
    
    # 2. Check 5 ha op het AFGESNEDEN land (met 10m overbrugging)
    res_land_5ha_max <- cluster_filter_compleet(
      masker     = land_afgesneden_max, 
      opp_laag   = land_afgesneden_max, 
      drempel_m2 = drempel_land_m2, # 5 ha
      dist_m     = dist_land_m,     # 10m overbrugging
      werkelijk  = FALSE
    )
    land_5ha_max  <- res_land_5ha_max$raster
    cl_id_5ha_max <- res_land_5ha_max$clusters
    
    # 3. ORIGINELE BOS/STRUWEEL CHECK (Minstens 30% bos/struweel)
    if (!all(is.na(terra::values(land_5ha_max, mat = FALSE)))) {
      r_struweel_max_clean <- fix_bos_raster(struweel1_max, template_MH)
      stats_totaal_max <- terra::zonal(land_5ha_max * 0.01, cl_id_5ha_max, fun = "sum", na.rm = TRUE)
      colnames(stats_totaal_max) <- c("Cluster_ID", "Totaal_ha")
      struweel_in_cl_max <- terra::mask(r_struweel_max_clean * 0.01, cl_id_5ha_max)
      stats_struweel_max <- terra::zonal(struweel_in_cl_max, cl_id_5ha_max, fun = "sum", na.rm = TRUE)
      colnames(stats_struweel_max) <- c("Cluster_ID", "Struweel_ha")
      
      df_check_max <- merge(stats_totaal_max, stats_struweel_max, by = "Cluster_ID", all.x = TRUE)
      df_check_max$Struweel_ha[is.na(df_check_max$Struweel_ha)] <- 0
      df_check_max$Pct_Bos <- (df_check_max$Struweel_ha / df_check_max$Totaal_ha) * 100
      valide_ids_max <- df_check_max$Cluster_ID[df_check_max$Pct_Bos >= 30] # 30% bos/struweel
      
      land_ok_max <- if(length(valide_ids_max) > 0) terra::mask(land_5ha_max, terra::ifel(cl_id_5ha_max %in% valide_ids_max, 1, NA)) else template_MH * NA
    } else { land_ok_max <- template_MH * NA }
    
    # 4. Koppel aan 30ha ruim landbiotoop
    if (!all(is.na(terra::values(land_ok_max, mat = FALSE))) && !all(is.na(terra::values(land_ruim_ok_max, mat = FALSE)))) {
      gevlektewitsnuitlibel_land_finaal_max <- terra::mask(land_ok_max, terra::ifel(!is.na(land_ruim_ok_max) & land_ruim_ok_max > 0, 1, NA))
    } else { gevlektewitsnuitlibel_land_finaal_max <- land_ok_max }
    
  } else { gevlektewitsnuitlibel_land_finaal_max <- template_MH * NA }
} else { gevlektewitsnuitlibel_land_finaal_max <- template_MH * NA }

water_finaal_max  <- if(exists("gevlektewitsnuitlibel_water_finaal_max")) gevlektewitsnuitlibel_water_finaal_max else template_MH * NA
leefgebied_max    <- terra::cover(water_finaal_max, gevlektewitsnuitlibel_land_finaal_max)
finaal_max_binair <- terra::ifel(!is.na(leefgebied_max) & leefgebied_max > 0, 1, NA)
cl_id_max         <- terra::patches(finaal_max_binair, directions = 8, zeroAsNA = TRUE)


# --- STAP 3: SPOOR B - WERKELIJKE OPPERVLAKTE (200M WATER AFSNIJDING) ---
if (exists("gevlektewitsnuitlibel_voortplanting_opp") && !all(is.na(terra::values(gevlektewitsnuitlibel_voortplanting_opp, mat = FALSE)))) {
  r_water_bin_opp <- terra::ifel(!is.na(gevlektewitsnuitlibel_voortplanting_opp) & gevlektewitsnuitlibel_voortplanting_opp > 0, 1, NA)
  poly_water_opp  <- terra::as.polygons(r_water_bin_opp, aggregate = TRUE)
  poly_buffer_opp <- terra::buffer(poly_water_opp, width = straal_water_m)
  
  if (exists("land_nabij_opp") && !all(is.na(terra::values(land_nabij_opp, mat = FALSE)))) {
    
    # 1. HARDE WATER-FIRST AFSNIJDING OP 200M
    land_afgesneden_opp <- terra::mask(land_nabij_opp, poly_buffer_opp)
    r_bin_land_afgesneden <- terra::ifel(!is.na(land_afgesneden_opp) & land_afgesneden_opp > 0, 1, NA)
    
    # 2. Check 5 ha op het AFGESNEDEN land (met 10m overbrugging)
    res_land_5ha_opp <- cluster_filter_compleet(
      masker     = r_bin_land_afgesneden, 
      opp_laag   = land_afgesneden_opp, 
      drempel_m2 = drempel_land_m2, # 5 ha
      dist_m     = dist_land_m,     # 10m overbrugging
      werkelijk  = TRUE
    )
    land_5ha_opp  <- res_land_5ha_opp$raster
    cl_id_5ha_opp <- res_land_5ha_opp$clusters
    
    # 3. ORIGINELE BOS/STRUWEEL CHECK (Minstens 30% bos/struweel)
    if (!all(is.na(terra::values(land_5ha_opp, mat = FALSE)))) {
      r_struweel_opp_clean <- fix_bos_raster(struweel1_opp, template_MH)
      stats_totaal_opp <- terra::zonal(land_5ha_opp, cl_id_5ha_opp, fun = "sum", na.rm = TRUE)
      colnames(stats_totaal_opp) <- c("Cluster_ID", "Totaal_m2")
      struweel_in_cl_opp <- terra::mask(r_struweel_opp_clean, cl_id_5ha_opp)
      stats_struweel_opp <- terra::zonal(struweel_in_cl_opp, cl_id_5ha_opp, fun = "sum", na.rm = TRUE)
      colnames(stats_struweel_opp) <- c("Cluster_ID", "Struweel_m2")
      
      df_check_opp <- merge(stats_totaal_opp, stats_struweel_opp, by = "Cluster_ID", all.x = TRUE)
      df_check_opp$Struweel_m2[is.na(df_check_opp$Struweel_m2)] <- 0
      df_check_opp$Pct_Bos <- (df_check_opp$Struweel_m2 / df_check_opp$Totaal_m2) * 100
      valide_ids_opp <- df_check_opp$Cluster_ID[df_check_opp$Pct_Bos >= 30] # 30% bos/struweel
      
      land_ok_opp <- if(length(valide_ids_opp) > 0) terra::mask(land_5ha_opp, terra::ifel(cl_id_5ha_opp %in% valide_ids_opp, 1, NA)) else template_MH * NA
    } else { land_ok_opp <- template_MH * NA }
    
    # 4. Koppel aan 30ha ruim landbiotoop
    if (!all(is.na(terra::values(land_ok_opp, mat = FALSE))) && !all(is.na(terra::values(land_ruim_ok_opp, mat = FALSE)))) {
      gevlektewitsnuitlibel_land_finaal_opp <- terra::mask(land_ok_opp, terra::ifel(!is.na(land_ruim_ok_opp) & land_ruim_ok_opp > 0, 1, NA))
    } else { gevlektewitsnuitlibel_land_finaal_opp <- land_ok_opp }
    
  } else { gevlektewitsnuitlibel_land_finaal_opp <- template_MH * NA }
} else { gevlektewitsnuitlibel_land_finaal_opp <- template_MH * NA }

water_finaal_opp  <- if(exists("gevlektewitsnuitlibel_voortplanting_opp")) gevlektewitsnuitlibel_voortplanting_opp else template_MH * NA
leefgebied_opp    <- terra::cover(water_finaal_opp, gevlektewitsnuitlibel_land_finaal_opp)
finaal_opp_binair <- terra::ifel(!is.na(leefgebied_opp) & leefgebied_opp > 0, 1, NA)
cl_id_opp         <- terra::patches(finaal_opp_binair, directions = 8, zeroAsNA = TRUE)

message("-> Water-First filtering met alle originele bronnen/checks succesvol doorlopen!")
```

```{r waarnemingen inlezen en ARPL}
# ==============================================================================
# STAP 12: ARPL & VERSPREIDING OVER DE VOLLEDIGE REGIOBUFFER
# ==============================================================================
message("-> Start ARPL en Actuele Verspreiding netwerkanalyse met waarnemingen...")

# 1. Pad naar het waarnemingsbestand van de libel
waarnemingen_path <- here("data/input/Waarnemingen_Soorten/Mechelse_Heide/Waarnemingen_GevlekteWitsnuitlibel.csv")

if (file.exists(waarnemingen_path)) {
  waarnemingen_raw    <- read_delim(waarnemingen_path, delim = ";", escape_double = FALSE, trim_ws = TRUE)
  df_points_sf        <- st_as_sf(waarnemingen_raw, coords = c("x", "y"), crs = 31370)
  waarneming_smear_sf <- st_buffer(df_points_sf, dist = buffer_m) %>% st_union() %>% st_sf()
  waarneming_blobs_vect  <- vect(waarneming_smear_sf)
  waarneming_punten_vect <- vect(df_points_sf) 
  has_observations    <- TRUE
} else {
  df_points_sf        <- st_sf(st_sfc(crs = 31370)) 
  waarneming_smear_sf <- st_sf(st_sfc(crs = 31370))
  waarneming_blobs_vect  <- vect(st_sf(st_sfc(crs = 31370)))
  waarneming_punten_vect <- vect(st_sf(st_sfc(crs = 31370)))
  has_observations    <- FALSE
}

verspreiding_raster <- terra::rast(template_MH, vals = NA)
verspreiding_netwerk_count <- 0

# FIX: Gebruik leefgebied_max en leefgebied_opp in plaats van final_max en final_opp
final_max_layer <- if(exists("leefgebied_max")) leefgebied_max else template_MH * NA
final_opp_layer <- if(exists("leefgebied_opp")) leefgebied_opp else template_MH * NA

cl_max   <- terra::patches(final_max_layer, directions = 8, zeroAsNA = TRUE)
opp_bron <- final_opp_layer

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  stats_max_raw <- terra::freq(cl_max)
  
  stats_act_raw <- terra::zonal(opp_bron, cl_max, fun = "sum", na.rm = TRUE)
  colnames(stats_act_raw) <- c("ID", "Werk_ha") 
  stats_act_raw$Werk_ha <- stats_act_raw$Werk_ha * 0.01

  # binnengrens MH controle
  r_opp_binnen_tv <- terra::mask(opp_bron, area_shape_proj)
  stats_tv_netto <- terra::zonal(r_opp_binnen_tv, cl_max, fun = "sum", na.rm = TRUE)
  colnames(stats_tv_netto) <- c("ID", "Binnen_MH_Werk_ha")
  stats_tv_netto$Binnen_MH_Werk_ha <- stats_tv_netto$Binnen_MH_Werk_ha * 0.01

  ids_binnen <- stats_tv_netto$ID[stats_tv_netto$Binnen_MH_Werk_ha > 0]
  
  # Neem drempelwaarde van min. leefgebied op (of stel in op 0 indien niet vooraf gedefinieerd)
  oppervlakte_ha_drempel <- if(exists("oppervlakte_ha")) oppervlakte_ha else 0
  ids_werkelijk_totaal <- stats_act_raw$ID[stats_act_raw$Werk_ha >= oppervlakte_ha_drempel]
  
  # ARPL / Verspreiding over het gehele dispersiebereik
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_max <- terra::extract(cl_max, waarneming_blobs_vect, ID = FALSE)
    ids_raken_waarneming <- if(nrow(extract_ids_max) > 0) unique(na.omit(extract_ids_max[[1]])) else c()
  } else {
    ids_raken_waarneming <- c()
  }
  ids_actueel_relevant <- intersect(ids_werkelijk_totaal, ids_raken_waarneming)

  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_verspr <- terra::extract(cl_max, waarneming_punten_vect, ID = FALSE)
    ids_raken_punten <- if(nrow(extract_ids_verspr) > 0) unique(na.omit(extract_ids_verspr[[1]])) else c()
  } else {
    ids_raken_punten <- c()
  }
  ids_verspreiding_clusters <- intersect(ids_werkelijk_totaal, ids_raken_punten)
  
  if(length(ids_verspreiding_clusters) > 0) {
    masker_verspr <- cl_max %in% ids_verspreiding_clusters
    clusters_actueel <- terra::mask(final_max_layer, masker_verspr)
    verspreiding_raster <- terra::ifel(!is.na(clusters_actueel), 1, NA)
    verspreiding_netwerk_count <- length(ids_verspreiding_clusters)
  }

  ids_pot_bin   <- ids_binnen
  ids_pot_buit  <- setdiff(stats_max_raw$value, ids_binnen)
  ids_werk_bin  <- intersect(ids_werkelijk_totaal, ids_binnen)
  ids_werk_buit <- setdiff(ids_werkelijk_totaal, ids_binnen)

  calc_ha <- function(ids, mode = "pot") {
    if(length(ids) == 0) return(0)
    if(mode == "pot") {
      sub_stats <- stats_max_raw[stats_max_raw$value %in% ids, ]
      return(round(sum(sub_stats$count) * 0.01, 2))
    } else {
      sub_stats <- stats_act_raw[stats_act_raw$ID %in% ids, ]
      return(round(sum(sub_stats$Werk_ha), 2))
    }
  }

  ha_pot_bin      <- calc_ha(ids_pot_bin, "pot")
  ha_pot_buit     <- calc_ha(ids_pot_buit, "pot")
  ha_werk_bin     <- calc_ha(ids_werk_bin, "werk")
  ha_werk_buit    <- calc_ha(ids_werk_buit, "werk")
  ha_arpl         <- calc_ha(ids_actueel_relevant, "werk")
  ha_verspreiding <- calc_ha(ids_verspreiding_clusters, "werk")

  if(length(ids_actueel_relevant) > 0) {
    arpl_raster <- cl_max %in% ids_actueel_relevant
    arpl_raster <- terra::ifel(arpl_raster, 1, NA)
  } else {
    arpl_raster <- terra::rast(cl_max, vals = NA)
  }

} else {
  ids_pot_bin <- ids_pot_buit <- ids_werk_bin <- ids_werk_buit <- ids_actueel_relevant <- c()
  ha_pot_bin <- ha_pot_buit <- ha_werk_bin <- ha_werk_buit <- ha_arpl <- ha_verspreiding <- 0
  arpl_raster <- terra::rast(template_MH, vals = NA)
}

tabel_totaal <- data.frame(
  Type = c("Maximaal Potentieel (Binnen)", "Maximaal Potentieel (Buiten)", 
           "Werkelijk Habitat (Binnen)", "Werkelijk Habitat (Buiten)",
           "Actueel Relevant (ARPL - Werkelijke ha)", "Actuele Verspreiding (Werkelijke ha)"),
  Clusters = c(length(ids_pot_bin), length(ids_pot_buit), 
               length(ids_werk_bin), length(ids_werk_buit), 
               length(ids_actueel_relevant), verspreiding_netwerk_count),
  Oppervlakte_ha = c(ha_pot_bin, ha_pot_buit, ha_werk_bin, ha_werk_buit, ha_arpl, ha_verspreiding)
)

tabel_totaal %>% 
  knitr::kable(caption = paste("Totaaloverzicht habitat & verspreiding:", soort)) %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover"))
```

```{r, export}
# --- EXPORT MET KOGELVRIJE NUL-OPVULLING & CONTRACT FIX ---
base_dir <- here("data/output/Mechelse_Heide/Rasters_Soorten")
folders <- list(
  potentie     = file.path(base_dir, "01_Maximale_Potentie"),
  werkelijk    = file.path(base_dir, "02_Werkelijke_Oppervlakte"),
  arpl         = file.path(base_dir, "03_ARPL"),
  verspreiding = file.path(base_dir, "04_Actuele_Verspreiding")
)
purrr::walk(folders, ~if (!dir.exists(.x)) dir.create(.x, recursive = TRUE))

if(!all(is.na(terra::values(leefgebied_opp, mat=FALSE)))) {
  werkelijk_export_rast <- terra::ifel(!is.na(leefgebied_opp) & leefgebied_opp > 0, 1, NA)
} else {
  werkelijk_export_rast <- terra::rast(template_MH, vals = NA)
}

if(exists("arpl_raster") && !all(is.na(terra::values(arpl_raster, mat=FALSE)))) {
  arpl_export_rast <- terra::ifel(arpl_raster > 0, 1, NA)
} else {
  arpl_export_rast <- terra::rast(template_MH, vals = NA)
}

if(!all(is.na(terra::values(verspreiding_raster, mat=FALSE)))) {
  verspreiding_export_rast <- terra::ifel(verspreiding_raster > 0, 1, NA)
} else {
  verspreiding_export_rast <- terra::rast(template_MH, vals = NA)
}

export_config <- list(
  list(rast = leefgebied_max,           naam = "Maximale_Potentie",        folder = folders$potentie,     type = "INT1U"),
  list(rast = werkelijk_export_rast,    naam = "Werkelijke_Oppervlaktes", folder = folders$werkelijk,    type = "INT1U"), 
  list(rast = arpl_export_rast,         naam = "ARPL",                    folder = folders$arpl,         type = "INT1U"),
  list(rast = verspreiding_export_rast, naam = "Actuele_Verspreiding",    folder = folders$verspreiding, type = "INT1U")
)

for(item in export_config) {
  file_path <- file.path(item$folder, paste0("Habitat_", item$naam, "_", soort, ".tif"))
  
  export_rast <- terra::deepcopy(item$rast)
  export_rast <- terra::mask(export_rast, area_buffer_fix)
  
  if(all(is.na(terra::values(export_rast, mat=FALSE)))) {
    export_rast <- terra::ifel(is.na(export_rast), 0, 0)
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  } else {
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  }
  
  writeRaster(export_rast, 
              filename = file_path, 
              overwrite = TRUE, 
              gdal = c("COMPRESS=LZW"), 
              datatype = item$type)
}
```

```{r Interactieve kaart}
# ==============================================================================
# STAP 14: INTERACTIEVE KAART (CORRECTE STATUSKLEURING & POPUPS)
# ==============================================================================
r_status <- maak_kaart_laag(list(kern = leefgebied_opp))
if (has_observations && nrow(df_points_sf) > 0) {
  waarneming_smear_web <- st_transform(waarneming_smear_sf, 4326)
  points_web           <- st_transform(df_points_sf, 4326)
} else {
  waarneming_smear_web <- st_sf(st_sfc(crs = 4326))
  points_web           <- st_sf(st_sfc(crs = 4326))
}

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  cluster_info <- data.frame(
      ID = stats_max_raw$value, 
      Pot_ha = stats_max_raw$count * 0.01 
    ) %>%
    left_join(stats_act_raw, by = "ID") %>%
    left_join(stats_tv_netto, by = "ID") %>% 
    mutate(
      Netto_MH_ha = coalesce(Binnen_MH_Werk_ha, 0),
      Zone = ifelse(Netto_MH_ha > 0, "Binnen MH / Grensoverschrijdend", "Buiten MH (Buffer)"),
      ARPL = ID %in% ids_actueel_relevant,
      Is_Werkelijk = ID %in% ids_werkelijk_totaal,
      
      # LOGISCHE EN EERLIJKE STATUSINDELING
      Status_Categorie = case_when(
        Is_Werkelijk & ARPL  ~ "Werkelijk Habitat (ARPL)",
        Is_Werkelijk & !ARPL ~ "Werkelijk Habitat (Onbeset)",
        TRUE                 ~ "Potentieel Leefgebied"
      ),
      
      Popup = paste0(
        "<div style='font-family: Arial; min-width: 210px;'>",
        "<strong>Cluster ID:</strong> ", ID, "<br>",
        "<strong>Zone:</strong> ", Zone, "<br><hr>",
        "<strong>Status:</strong> <span style='font-weight:bold;'>", Status_Categorie, "</span><br>",
        "<strong>Actueel Relevant (ARPL):</strong> ", 
        ifelse(ARPL, "<span style='color:green; font-weight:bold;'>JA (Populatielaag)</span>", 
                     "<span style='color:#555;'>Nee (Geen waarneming)</span>"), "<br><br>",
        "<strong>Bruto Potentieel Netwerk:</strong> ", round(Pot_ha, 2), " ha<br>",
        "<strong>Netto Werkelijk Habitat:</strong> ", round(coalesce(Werk_ha, 0), 2), " ha<br><hr>",
        "<span style='font-size:11px; color:#555;'><strong>Waarvan netto binnen MH grens:</strong> ", 
        round(Netto_MH_ha, 2), " ha</span>",
        "</div>"
      )
    )

  cl_poly <- terra::as.polygons(cl_max, aggregate = TRUE) %>% 
    st_as_sf() %>% 
    st_transform(4326)

  cl_poly_final <- cl_poly %>%
    inner_join(cluster_info, by = setNames("ID", names(cl_poly)[1]))
} else {
  cl_poly_final <- st_sf(st_sfc(crs = 4326))
}

r_web_status <- terra::project(r_status, "EPSG:4326", method = "near")
bbox         <- st_bbox(grens_web)

if(exists("gevlektewitsnuitlibel_water_finaal_max") && 
   !all(is.na(terra::values(gevlektewitsnuitlibel_water_finaal_max, mat=FALSE)))) {
  r_web_water <- terra::project(gevlektewitsnuitlibel_water_finaal_max, "EPSG:4326", method = "near")
} else {
  r_web_water <- NULL
}

# HIER ZIT DE GELEZEN PALET-LOGICA:
# Groen = ARPL, Oranje = Werkelijk zonder ARPL, Blauw/Paars = Puur Potentieel
pal_status_categorie <- colorFactor(
  palette = c("#2ecc71", "#f39c12", "#3498db"), 
  levels = c("Werkelijk Habitat (ARPL)", "Werkelijk Habitat (Onbeset)", "Potentieel Leefgebied")
)

pal_4 <- colorFactor(palette = c("#FF00FF", "#FF8C00", "#4daf4a", "#377eb8"), levels = 1:4, na.color = "transparent")

map <- leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

# Pixellag
if(!all(is.na(terra::values(r_web_status, mat=FALSE)))) {
  map <- map %>% addRasterImage(terra::as.int(r_web_status), colors = pal_4, opacity = 0.3, group = "Habitat Status (Pixels)")
}

# Goedgekeurde Poelen in Felblauw
if(!is.null(r_web_water)) {
  map <- map %>% addRasterImage(
    terra::as.int(r_web_water), 
    colors = "#0072B2", 
    opacity = 0.85, 
    group = "Goedgekeurde Poelen/Vennen"
  )
}

# Polygonen gekleurd op basis van de 3 logische categorieën
if(nrow(cl_poly_final) > 0) {
  map <- map %>% addPolygons(
    data = cl_poly_final,
    fillColor = ~pal_status_categorie(Status_Categorie),
    fillOpacity = ~case_when(
      Status_Categorie == "Werkelijk Habitat (ARPL)"    ~ 0.65,
      Status_Categorie == "Werkelijk Habitat (Onbeset)" ~ 0.45,
      TRUE                                              ~ 0.20
    ), 
    color = ~pal_status_categorie(Status_Categorie), 
    weight = ~ifelse(ARPL, 2.5, 1), 
    opacity = 0.9,
    popup = ~Popup,
    group = "Leefgebieden (Clusters)"
  )
}

# Waarnemingen & Smear
if(has_observations && nrow(df_points_sf) > 0) {
  map <- map %>%
    addPolygons(
      data = waarneming_smear_web,  
      color = "#377eb8", weight = 2, fill = FALSE,
      dashArray = "10, 5", group = "Dispersiebereik (Smear)"
    ) %>%
    addCircles(
      data = points_web, 
      color = "black", weight = 1, fillColor = "yellow", fillOpacity = 1,
      radius = 15, group = "Waarnemingen"
    )
}

map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Goedgekeurde Poelen/Vennen", 
                      "Leefgebieden (Clusters)", 
                      "Habitat Status (Pixels)", 
                      "Dispersiebereik (Smear)", 
                      "Waarnemingen", 
                      "Grens Studiegebied"),
    options = layersControlOptions(collapsed = FALSE)
  ) %>%
  addLegend(
    pal = pal_status_categorie, 
    values = cl_poly_final$Status_Categorie, 
    title = "Status Leefgebied", 
    position = "bottomright",
    group = "Leefgebieden (Clusters)"
  )
```
