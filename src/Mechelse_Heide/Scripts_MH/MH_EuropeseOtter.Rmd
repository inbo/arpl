---
title: "Leefgebiedenscript in R: Mechelse Heide"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)

library(here)
# CRUCIALE FIX: Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
# Hierdoor werken relatieve paden met hier() overal hetzelfde.
knitr::opts_knit$set(root.dir = here())

library(knitr)
library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(dplyr::first)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

# Exacte hectare-berekening behouden (cellSize voor 100% precisie)
calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

maak_kaart_laag <- function(res_list) {
  if(is.null(res_list)) return(NULL)
  kernen <- terra::ifel(!is.na(res_list$kern), 1, NA)
  if(!is.null(res_list$bouw)) {
    bouw <- terra::ifel(!is.na(res_list$bouw), 2, NA)
    finaal <- terra::cover(kernen, bouw)
  } else {
    finaal = kernen
  }
  return(finaal)
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  temp_mask <- masker
  if(dist_m > 0) temp_mask <- terra::buffer(masker, width = dist_m / 2)
  
  cl <- terra::patches(temp_mask, directions = 8, zeroAsNA = TRUE) %>% terra::mask(masker)
  
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    # CORRECTIE: Waarde is de som van fracties (1 pixel = 1.0). 
    # Vermenigvuldigen met 100 geeft de juiste Area in m2!
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100 # 1 binaire pixel = 100m2
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  masker_binair <- cl %in% voldoet_ids
  final_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  return(list(raster = terra::mask(masker, final_mask), clusters = cl))
}
```

# Soort informatie ophalen
```{r Soort informatie}
df <- read_excel(here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
soort <- "europeseotter"

resultaat <- df %>%
filter(Soort == soort) %>%
select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

# Variabelen definiëren
oppervlakte_ha <- resultaat$MinOpp_ha[1]
afstand_m <- resultaat$AfstandBiotopen_m[1]
buffer_m <- resultaat$Dispersiecap_m[1]

print(resultaat)
rm(df, resultaat)
```

# Gebied voorbereiden (Mechelse Heide)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/Mechelse_Heide.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Uitsnede maken en lokale/globale cel-IDs berekenen...")

# 1. Snijd de master_grid direct uit naar het studiegebied (met buffer)
id_raster_MH <- crop(master_grid, area_buffer_fix, snap = "near")

# 2. GEHEUGEN-FIX: Bereken globale cel-IDs op basis van xy-coördinaten van de uitsnede
# Dit voorkomt het laden van miljoenen cellen van heel Vlaanderen in het RAM
lokale_coords <- terra::xyFromCell(id_raster_MH, 1:ncell(id_raster_MH))
globale_ids   <- terra::cellFromXY(master_grid, lokale_coords)

# 3. Bouw de vertaaltabel efficiënt op met data.table
vertaal_df <- data.table(
  lokale_id  = 1:ncell(id_raster_MH),
  globale_id = globale_ids
)

# Enkel de unieke globale IDs bewaren voor de database query
studiegebied_globale_ids <- unique(vertaal_df$globale_id[!is.na(vertaal_df$globale_id)])

# Sjabloon opbouwen voor latere stappen
template_MH <- terra::rasterize(area_buffer_fix, id_raster_MH, field = 1, background = 0)
grens_web   <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

# Geheugen opschonen
rm(lokale_coords, globale_ids)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(here("data/input/Raster_Vlaanderen/BWK_Tabel_Mechelse_Heide_Plus_50km.rds"))
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE DYNAMISCH EN GEHEUGENZUINIG
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    bevat_codes_escaped  <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term           <- paste0(bevat_codes_anchored, collapse = "|")
    
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_MH      <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_cel_som <- tabel_MH[, .(Oppervlakte = sum(BWK_FRAC, na.rm = TRUE)), by = .(cel_id)]
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      
      # GEHEUGEN-OPTIMALISATIE:
      # Maak verse rasters op basis van het sjabloon
      r_opp_type   <- template_MH * NA
      r_match_type <- template_MH * NA
      
      # Vul waarden direct in een tijdelijke R-vector van de grootte van de uitsnede
      # Dit gebruikt 10x minder geheugen dan een C++ raster-subsetting
      vec_opp   <- rep(NA_real_, terra::ncell(id_raster_MH))
      vec_match <- rep(NA_real_, terra::ncell(id_raster_MH))
      
      vec_opp[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        vec_match[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      
      # Schrijf de vectoren in één keer weg naar het raster
      terra::values(r_opp_type)   <- vec_opp
      terra::values(r_match_type) <- vec_match
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
      
      rm(vec_opp, vec_match)
    }
  }
}

# Uitpakken naar de exacte losse variabelen
d_max <- lijst_matches[["d"]]; d_opp <- lijst_oppervlaktes[["d"]]
h_max <- lijst_matches[["h"]]; h_opp <- lijst_oppervlaktes[["h"]]
k_max <- lijst_matches[["k"]]; k_opp <- lijst_oppervlaktes[["k"]]
m_max <- lijst_matches[["m"]]; m_opp <- lijst_oppervlaktes[["m"]]
r_max <- lijst_matches[["r"]]; r_opp <- lijst_oppervlaktes[["r"]]
s_max <- lijst_matches[["s"]]; s_opp <- lijst_oppervlaktes[["s"]]
t_max <- lijst_matches[["t"]]; t_opp <- lijst_oppervlaktes[["t"]]
v_max <- lijst_matches[["v"]]; v_opp <- lijst_oppervlaktes[["v"]]

# Grote eindschoonmaak
rm(resultaten_gegroepeerd, df_nieuw, lijst_matches, lijst_oppervlaktes, tabel_vlaanderen)
gc()
```

```{r Otter Stap 1 - Waterbiotopen opbouwen}
# ==============================================================================
# OTTER STAP 1: INTEGRAL WATERNETWERK (HUETZON KLASSE 1 + BLAUWELAAG)
# ==============================================================================
message("-> Otter Spoor 1: Waterlopen en plassen inlezen en samenvoegen...")

# 1. Huetzon-raster inlezen voor waterlopen (Ge('rastert_huetzon1.asc', 1))
r_huetzon_raw <- terra::rast(here("data/input/Raster_Vlaanderen/vlaanderen_huetzon_10m.tif"))
r_huetzon     <- terra::resample(r_huetzon_raw, id_raster_MH, method = "near") %>% terra::crop(id_raster_MH)

# Selecteer alle cellen van klasse 1 (hoofdwaterlopen)
r_waterlopen1 <- r_huetzon == 1

# 2. BlauweLaag plassen inlezen (Ge('BlauweLaag_Plassen_06042017_20m_.asc', 1))
# Failsafe: mocht de specifieke plassen-asc niet aanwezig zijn, maak een leeg raster
bl_path <- here("data/input/ASCI Files/BlauweLaag_Plassen_06042017_20m_.asc")
if (file.exists(bl_path)) {
  r_bl_raw <- terra::rast(bl_path)
  r_bl     <- terra::resample(r_bl_raw, id_raster_MH, method = "near") %>% terra::crop(id_raster_MH)
  r_plassen <- r_bl == 1
} else {
  message("⚠️ Opmerking: BlauweLaag plassen-bestand niet gevonden, we rekenen puur op de Huetzon waterlopen.")
  r_plassen <- id_raster_MH * NA
}

# 3. Combineer alle water-elementen via logische OF (or)
otter_water_max <- (r_waterlopen1 == 1) | (r_plassen == 1)
otter_water_max <- terra::ifel(otter_water_max == 1, 1, NA)

message(paste("-> Totaal rauw waterbiotoop geïdentificeerd:", 
              round(calc_ha_exact(otter_water_max), 2), "ha"))

suppressWarnings(rm(r_huetzon_raw, r_huetzon, r_waterlopen1, r_bl_raw, r_bl, r_plassen))
gc()
```

```{r Otter Stap 2 - Landbiotopen smelten en zeven op 1000 ha}
# ==============================================================================
# OTTER STAP 2: LANDBIOTOPEN SMELTEN EN ZEVEN OP 1000 HA (PARALLEL & ONTAFHANKELIJK)
# ==============================================================================
message("-> Otter Spoor 2: Alle landbiotopen samenvoegen en opschonen...")

# 1. Hulpfunctie definiëren om NAs en nullen op te schalen
clean_max <- function(r) {
  if (is.null(r)) return(template_MH * NA)
  r[is.na(r) | r == 0] <- NA
  return(r)
}

# 2. Smelt alle binaire/fractionele landbiotopen in één snelle stap met pmax
opp_stack <- c(
  clean_max(d_opp), clean_max(h_opp), clean_max(k_opp), clean_max(m_opp),
  clean_max(r_opp), clean_max(s_opp), clean_max(t_opp), clean_max(v_opp)
)

otter_biotopen_opp <- sum(opp_stack, na.rm = TRUE)
otter_biotopen_opp <- terra::clamp(otter_biotopen_opp, upper = 1.0)
otter_biotopen_max <- terra::ifel(otter_biotopen_opp > 0, 1, NA)

rm(opp_stack)
gc()

# --- SPOOR A: MAXIMALE POTENTIE (Binair zeven op 1000 ha) ---
if (!all(is.na(terra::values(otter_biotopen_max, mat = FALSE)))) {
  message("-> Spoor A: Landbiotopen clusteren en zeven op 1000 ha (Binair)...")
  
  poly_land_max <- terra::as.polygons(otter_biotopen_max, aggregate = TRUE) %>% terra::disagg()
  poly_buf_max  <- terra::buffer(poly_land_max, width = 50)
  poly_net_max  <- terra::aggregate(poly_buf_max) %>% terra::disagg()
  poly_net_max$Cluster_ID <- 1:nrow(poly_net_max)
  
  poly_land_max$Cluster_ID <- terra::relate(poly_land_max, poly_net_max, "within") %>% 
    apply(1, function(x) { if(any(x)) which(x)[1] else NA })
  
  r_clusters_max <- terra::rasterize(poly_land_max, template_MH, field = "Cluster_ID", touches = FALSE)
  
  freq_max <- terra::freq(r_clusters_max)
  freq_max$Opp_ha <- freq_max$count * 0.01 # 1 pixel = 0.01 ha
  
  voldoet_ids_max <- freq_max$value[freq_max$Opp_ha >= 1000]
  
  if (length(voldoet_ids_max) > 0) {
    otter_biotopen1000_max <- terra::mask(otter_biotopen_max, r_clusters_max %in% voldoet_ids_max, maskvalues = FALSE)
  } else {
    otter_biotopen1000_max <- template_MH * NA
  }
  rm(poly_land_max, poly_buf_max, poly_net_max, r_clusters_max, freq_max)
} else {
  otter_biotopen1000_max <- template_MH * NA
}

# --- SPOOR B: WERKELIJKE OPPERVLAKTE (Parallel & Autonoom zeven op 1000 ha) ---
r_binair_land_opp <- terra::ifel(!is.na(otter_biotopen_opp) & otter_biotopen_opp > 0, 1, NA)

if (!all(is.na(terra::values(r_binair_land_opp, mat = FALSE)))) {
  message("-> Spoor B: Landbiotopen clusteren en zeven op 1000 ha (Fracties)...")
  
  poly_land_opp <- terra::as.polygons(r_binair_land_opp, aggregate = TRUE) %>% terra::disagg()
  poly_buf_opp  <- terra::buffer(poly_land_opp, width = 50)
  poly_net_opp  <- terra::aggregate(poly_buf_opp) %>% terra::disagg()
  poly_net_opp$Cluster_ID <- 1:nrow(poly_net_opp)
  
  poly_land_opp$Cluster_ID <- terra::relate(poly_land_opp, poly_net_opp, "within") %>% 
    apply(1, function(x) { if(any(x)) which(x)[1] else NA })
  
  r_clusters_opp <- terra::rasterize(poly_land_opp, template_MH, field = "Cluster_ID", touches = FALSE)
  
  zonal_ha_opp <- terra::zonal(otter_biotopen_opp, r_clusters_opp, fun = "sum", na.rm = TRUE)
  colnames(zonal_ha_opp) <- c("Cluster_ID", "Som_Pixels")
  zonal_ha_opp$Opp_ha    <- zonal_ha_opp$Som_Pixels * 0.01
  
  voldoet_ids_opp <- zonal_ha_opp$Cluster_ID[zonal_ha_opp$Opp_ha >= 1000]
  
  if (length(voldoet_ids_opp) > 0) {
    otter_biotopen1000_opp <- terra::mask(otter_biotopen_opp, r_clusters_opp %in% voldoet_ids_opp, maskvalues = FALSE)
  } else {
    otter_biotopen1000_opp <- template_MH * NA
  }
  rm(poly_land_opp, poly_buf_opp, poly_net_opp, r_clusters_opp, zonal_ha_opp)
} else {
  otter_biotopen1000_opp <- template_MH * NA
}

suppressWarnings(rm(otter_biotopen_max, otter_biotopen_opp, r_binair_land_opp))
gc()
```

```{r Otter Stap 3 - Ruimtelijke waterkoppeling en finalesering}
# ==============================================================================
# OTTER STAP 3: COMMUTER WATERKOPPELING (< 1000M) EN HABITAT-FINALESERING
# ==============================================================================
message("-> Otter Spoor 3: Wederzijdse afstandsmetingen uitvoeren (max. 1000m)...")

final_max <- template_MH * NA
final_opp <- template_MH * NA

if (exists("otter_water_max") && !all(is.na(terra::values(otter_water_max, mat=FALSE)))) {
  
  # --- SPOOR A: MAXIMALE POTENTIE ---
  if (exists("otter_biotopen1000_max") && !all(is.na(terra::values(otter_biotopen1000_max, mat=FALSE)))) {
    dist_tot_water_max <- terra::distance(otter_water_max)
    dist_tot_land_max  <- terra::distance(otter_biotopen1000_max)
    
    leefgebied3_max <- otter_biotopen1000_max == 1 & dist_tot_water_max <= 1000
    leefgebied4_max <- otter_water_max == 1 & dist_tot_land_max <= 1000
    
    otter_leefgebied_max <- leefgebied3_max | leefgebied4_max
    final_max <- terra::ifel(otter_leefgebied_max == 1, 1, NA) %>% terra::crop(template_MH)
    
    rm(dist_tot_water_max, dist_tot_land_max, leefgebied3_max, leefgebied4_max, otter_leefgebied_max)
  }
  
  # --- SPOOR B: WERKELIJKE OPPERVLAKTE (Parallel & Autonoom) ---
  r_binair_land1000_opp <- terra::ifel(!is.na(otter_biotopen1000_opp) & otter_biotopen1000_opp > 0, 1, NA)
  
  if (!all(is.na(terra::values(r_binair_land1000_opp, mat=FALSE)))) {
    dist_tot_water_opp <- terra::distance(otter_water_max)
    dist_tot_land_opp  <- terra::distance(r_binair_land1000_opp)
    
    leefgebied3_opp_mask <- r_binair_land1000_opp == 1 & dist_tot_water_opp <= 1000
    leefgebied3_opp      <- terra::mask(otter_biotopen1000_opp, leefgebied3_opp_mask)
    
    leefgebied4_opp_mask <- otter_water_max == 1 & dist_tot_land_opp <= 1000
    r_water_opp_full     <- terra::ifel(otter_water_max == 1, 1.0, NA)
    leefgebied4_opp      <- terra::mask(r_water_opp_full, leefgebied4_opp_mask)
    
    l3_clean <- terra::ifel(is.na(leefgebied3_opp), 0, leefgebied3_opp)
    l4_clean <- terra::ifel(is.na(leefgebied4_opp), 0, leefgebied4_opp)
    
    otter_leefgebied_opp_raw <- terra::clamp(l3_clean + l4_clean, upper = 1.0)
    otter_leefgebied_opp     <- terra::ifel(!is.na(otter_leefgebied_opp_raw) & otter_leefgebied_opp_raw > 0, otter_leefgebied_opp_raw, NA)
    
    final_opp <- terra::crop(otter_leefgebied_opp, template_MH)
    
    rm(dist_tot_water_opp, dist_tot_land_opp, leefgebied3_opp_mask, leefgebied3_opp, 
       leefgebied4_opp_mask, r_water_opp_full, l3_clean, l4_clean, otter_leefgebied_opp_raw, otter_leefgebied_opp)
  }
  
  rm(r_binair_land1000_opp)
  gc()
  
  # --- KOPPELING NAAR FINALE TABELLEN EN KAARTEN ---
  cl_max  <- final_max
  bwk_opp <- final_opp
  
  cat("\n=========================================================\n")
  cat("          RESULTATEN MODEL EUROPESE OTTER                 \n")
  cat("=========================================================\n")
  cat("Totaal stabiel leefgebied Otter (ha):", round(calc_ha_exact(final_opp), 2), "\n")
  cat("=========================================================\n\n")
  
} else {
  message("⚠️ Knelpunt: Er is geen landnetwerk >1000 ha óf geen water aanwezig in de actieve regio!")
  cl_max  <- final_max
  bwk_opp <- final_opp
}

r_status <- final_max

if (exists("otter_water_max")) {
  otter_waterbiotoop1_finaal_max <- otter_water_max 
}

suppressWarnings(rm(otter_biotopen1000_max, otter_biotopen1000_opp, otter_water_max))
gc()
```

# Resultaten en Statistiek
```{r Resultaten en statistiek gefixt voor otter}
# ==============================================================================
# ECO-STATISTISCHE FINALESERING EN KOPPELING NAAMGEVING otter
# ==============================================================================
message("-> Resultatensectie: otter-model-uitgangen koppelen aan de statistiek-mal...")

# BRONKOPPELING FIX: Wijs de succesvolle eindrasters uit je modelchunk rechtstreeks toe!
if (exists("final_max") && !all(is.na(terra::values(final_max, mat=FALSE)))) {
  final_max <- final_max
  final_opp <- final_opp
  opp_bron  <- final_opp
} else {
  message("⚠️ Knelpunt: final_max is volledig leeg of bestaat niet. Fallback naar NA-rasters.")
  final_max <- template_MH * NA
  final_opp <- template_MH * NA
  opp_bron  <- template_MH * NA
}

# Dynamisch pad naar de waarnemingen van de otter
waarnemingen_path <- here("data/input/Waarnemingen_Soorten/Mechelse_Heide/Waarnemingen_otter.csv")

# ==============================================================================
# ECO-VISUELE FIX: Clusters definiëren via de echte 500m model-zoekstraal!
# ==============================================================================
if (!all(is.na(terra::values(final_max, mat=FALSE)))) {
  # 1. Vectoriseer de scherpe leefgebied-pixels
  p_ruw_max <- terra::as.polygons(final_max, aggregate = TRUE) %>% terra::disagg()
  
  # 2. Smelt elementen binnen 500m (breedte = 250m buffer) samen tot grote netwerkwolken
  p_buf_max        <- terra::buffer(p_ruw_max, width = 250)
  p_net_max_wolken <- terra::aggregate(p_buf_max) %>% terra::disagg()
  p_net_max_wolken$Netwerk_ID <- 1:nrow(p_net_max_wolken)
  
  # 3. Koppel de Netwerk-IDs terug aan een schoon raster (dit bouwt cl_max op!)
  cl_max <- final_max * NA
  
  # Breng de unieke Netwerk-IDs aan op het raster via een ruimtelijke relate
  p_ruw_max$Netwerk_ID <- terra::relate(p_ruw_max, p_net_max_wolken, "within") %>% 
    apply(1, function(x) { if(any(x)) which(x)[1] else NA })
  
  cl_max <- terra::rasterize(p_ruw_max, final_max * NA, field = "Netwerk_ID", touches = FALSE)
  
  rm(p_ruw_max, p_buf_max, p_net_max_wolken)
} else {
  cl_max <- final_max * NA
}

# Maak een binaire mal van Mechelse Heide ZONDER de buffer (Gebruik de grens van Mechelse Heide)
binnen_masker <- terra::rasterize(area_shape_proj, id_raster_MH, field = 1, background = 0)

# --- WAARNEMINGEN INLEZEN ---
if (file.exists(waarnemingen_path)) {
  waarnemingen_raw <- read_delim(waarnemingen_path, delim = ";", escape_double = FALSE, trim_ws = TRUE)
  df_points_sf <- st_as_sf(waarnemingen_raw, coords = c("x", "y"), crs = 31370)
  waarneming_smear_sf <- st_buffer(df_points_sf, dist = buffer_m) %>% st_union() %>% st_sf() 
  waarneming_blobs_vect  <- vect(waarneming_smear_sf)
  waarneming_punten_vect <- vect(df_points_sf)
  has_observations <- TRUE
} else {
  df_points_sf <- st_sf(st_sfc(crs = 31370)) 
  waarneming_smear_sf <- st_sf(st_sfc(crs = 31370))
  waarneming_blobs_vect  <- vect(st_sf(st_sfc(crs = 31370)))
  waarneming_punten_vect <- vect(st_sf(st_sfc(crs = 31370)))
  has_observations <- FALSE
}

verspreiding_raster <- terra::rast(template_MH, vals = NA)
verspreiding_netwerk_count <- 0

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  
  # 1. Totale binaire grootte van het cluster
  stats_max_raw <- terra::freq(cl_max)
  stats_max_raw <- as.data.frame(stats_max_raw)
  colnames(stats_max_raw) <- c("layer", "ID", "count")
  stats_max_raw$Pot_ha <- stats_max_raw$count * 0.01 
  
  # 2. Totale werkelijke habitat-oppervlaktes binnen deze clusters
  zonal_raw <- terra::zonal(opp_bron, cl_max, fun = "sum", na.rm = TRUE)
  zonal_raw <- as.data.frame(zonal_raw)
  colnames(zonal_raw) <- c("ID", "Werk_ha_raw")
  zonal_raw$Werk_ha <- zonal_raw$Werk_ha_raw * 0.01 
  
  stats_gecombineerd <- merge(stats_max_raw, zonal_raw, by = "ID", all.x = TRUE)
  stats_gecombineerd$Werk_ha[is.na(stats_gecombineerd$Werk_ha)] <- 0
  stats_gecombineerd$Werk_ha <- pmin(stats_gecombineerd$Werk_ha, stats_gecombineerd$Pot_ha)

  # 3. Welke clusters raken het actieve studiegebied
  zonal_zone <- terra::zonal(binnen_masker, cl_max, fun = "max", na.rm = TRUE)
  zonal_zone <- as.data.frame(zonal_zone)
  colnames(zonal_zone) <- c("ID", "In_MH")
  ids_die_MH_raken <- zonal_zone$ID[!is.na(zonal_zone$In_MH) & zonal_zone$In_MH > 0]
  
  # 4. DYNAMISCHE FILTERING
  ids_potentieel_totaal <- stats_gecombineerd$ID
  ids_werkelijk_totaal  <- stats_gecombineerd$ID[stats_gecombineerd$Werk_ha >= oppervlakte_ha]
  
  # 5. Opsplitsing naar Binnen/Buiten Mechelse Heide
  ids_pot_bin   <- intersect(ids_potentieel_totaal, ids_die_MH_raken)
  ids_pot_buit  <- setdiff(ids_potentieel_totaal, ids_die_MH_raken)
  
  ids_werk_bin  <- intersect(ids_werkelijk_totaal, ids_die_MH_raken)
  ids_werk_buit <- setdiff(ids_werkelijk_totaal, ids_die_MH_raken)
  
  # A. ARPL Extractie
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_max <- terra::extract(cl_max, waarneming_blobs_vect, ID = FALSE)
    ids_raken_waarneming <- if(nrow(extract_ids_max) > 0) unique(na.omit(extract_ids_max[[1]])) else c()
  } else {
    ids_raken_waarneming <- c()
  }
  ids_actueel_relevant <- intersect(ids_werkelijk_totaal, ids_raken_waarneming)

  # B. Actuele Verspreiding Extractie
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_verspr <- terra::extract(cl_max, waarneming_punten_vect, ID = FALSE)
    ids_raken_punten <- if(nrow(extract_ids_verspr) > 0) unique(na.omit(extract_ids_verspr[[1]])) else c()
  } else {
    ids_raken_punten <- c()
  }
  ids_verspreiding_clusters <- intersect(ids_werkelijk_totaal, ids_raken_punten)
  
  if(length(ids_verspreiding_clusters) > 0) {
    masker_verspr <- cl_max %in% ids_verspreiding_clusters
    clusters_actueel <- terra::mask(final_max, masker_verspr)
    verspreiding_raster <- terra::ifel(!is.na(clusters_actueel), 1, NA)
    verspreiding_netwerk_count <- length(ids_verspreiding_clusters)
  }

  # --- BEREKENING HECTARES VOOR TABEL ---
  ha_pot_bin      <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_bin]), 2)
  ha_pot_buit     <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_buit]), 2)
  ha_werk_bin     <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_bin]), 2)
  ha_werk_buit    <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_buit]), 2)
  ha_arpl         <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_actueel_relevant]), 2)
  ha_verspreiding <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_verspreiding_clusters]), 2)

  if(length(ids_actueel_relevant) > 0) {
    arpl_raster <- terra::match(cl_max, ids_actueel_relevant)
    arpl_raster <- (!is.na(arpl_raster)) * 1
  } else {
    arpl_raster <- terra::rast(cl_max, vals = NA)
  }

} else {
  ids_pot_bin <- ids_pot_buit <- ids_werk_bin <- ids_werk_buit <- ids_actueel_relevant <- c()
  ha_pot_bin <- ha_pot_buit <- ha_werk_bin <- ha_werk_buit <- ha_arpl <- ha_verspreiding <- 0
  arpl_raster <- terra::rast(template_MH, vals = NA)
  stats_gecombineerd <- data.frame(ID = integer(), Pot_ha = numeric(), Werk_ha = numeric())
  zonal_zone <- data.frame(ID = integer(), In_MH = numeric())
}

tabel_totaal <- data.frame(
  Type = c("Maximaal Potentieel (Binnen)", "Maximaal Potentieel (Buiten)", 
           "Werkelijk Habitat (Binnen)", "Werkelijk Habitat (Buiten)",
           "Actueel Relevant (ARPL - Werkelijke ha)", "Actuele Verspreiding (Werkelijke ha)"),
  Clusters = c(length(ids_pot_bin), length(ids_pot_buit), 
               length(ids_werk_bin), length(ids_werk_buit), 
               length(ids_actueel_relevant), verspreiding_netwerk_count),
  Oppervlakte_ha = c(ha_pot_bin, ha_pot_buit, ha_werk_bin, ha_werk_buit, ha_arpl, ha_verspreiding)
)

tabel_totaal %>% 
  knitr::kable(caption = paste("Totaaloverzicht habitat & verspreiding voor de", soort)) %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover"))
```

# Interactieve kaart
```{r Interactieve kaart}
if (has_observations && nrow(df_points_sf) > 0) {
  waarneming_smear_web <- st_transform(waarneming_smear_sf, 4326)
  points_web           <- st_transform(df_points_sf, 4326)
} else {
  waarneming_smear_web <- st_sf(st_sfc(crs = 4326))
  points_web           <- st_sf(st_sfc(crs = 4326))
}

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  cluster_info <- stats_gecombineerd %>%
    left_join(zonal_zone, by = "ID") %>% 
    mutate(
      Zone = ifelse(!is.na(In_MH) & In_MH > 0, "Binnen MH", "Buiten MH (Buffer)"),
      ARPL = ID %in% ids_actueel_relevant,
      Type = ifelse(ID %in% ids_werkelijk_totaal, "Werkelijk Habitat", "Potentieel (Niet Werkelijk)"),
      Popup = paste0(
        "<div style='font-family: Arial; min-width: 180px;'>",
        "<strong>Cluster ID:</strong> ", ID, "<br>",
        "<strong>Zone:</strong> ", Zone, "<br><hr>",
        "<strong>Status:</strong> ", Type, "<br>",
        "<strong>Actueel Relevant (ARPL):</strong> ", 
        ifelse(ARPL, "<span style='color:green; font-weight:bold;'>JA</span>", 
                     "<span style='color:red;'>NEE (Geen waarneming of onder drempel)</span>"), "<br><br>",
        "<strong>Potentieel Oppervlak:</strong> ", round(Pot_ha, 2), " ha<br>",
        "<strong>Werkelijk Habitat:</strong> ", round(coalesce(Werk_ha, 0), 2), " ha",
        "</div>"
      )
    )

  cl_poly <- terra::as.polygons(cl_max, aggregate = TRUE) %>% 
    st_as_sf() %>% 
    st_transform(4326)

  cl_poly_final <- cl_poly %>%
    inner_join(cluster_info, by = setNames("ID", names(cl_poly)[1]))
} else {
  cl_poly_final <- st_sf(st_sfc(crs = 4326))
}

r_status <- terra::ifel(!is.na(final_opp), 1, terra::ifel(!is.na(final_max), 2, NA))
r_web_status         <- terra::project(r_status, "EPSG:4326", method = "near")
grens_web            <- st_transform(st_as_sf(area_shape_proj), 4326)
bbox                 <- st_bbox(grens_web)

pal_status <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = c("Werkelijk Habitat", "Potentieel (Niet Werkelijk)"))
pal_4 <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = 1:2, na.color = "transparent")

map <- leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

if(!all(is.na(terra::values(r_web_status, mat=FALSE)))) {
  map <- map %>% addRasterImage(terra::as.int(r_web_status), colors = pal_4, opacity = 0.5, group = "Habitat Status (Pixels)")
}

if(nrow(cl_poly_final) > 0) {
  map <- map %>% addPolygons(
    data = cl_poly_final,
    fillColor = ~pal_status(Type),
    fillOpacity = ~ifelse(ARPL, 0.6, 0.1), 
    color = ~pal_status(Type), 
    weight = ~ifelse(ARPL, 3, 0.5), 
    opacity = 1,
    popup = ~Popup,
    group = "Leefgebieden"
  )
}

if(has_observations && nrow(df_points_sf) > 0) {
  map <- map %>%
    addPolygons(
      data = waarneming_smear_web,  
      color = "#377eb8", 
      weight = 2, 
      fill = FALSE,       
      dashArray = "10, 5", 
      group = "Dispersiebereik (Smear)"
    ) %>%
    addCircles(
      data = points_web,  
      color = "black", weight = 1, fillColor = "yellow", fillOpacity = 1,
      radius = 15, group = "Waarnemingen"
    )
}

map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Leefgebieden", "Habitat Status (Pixels)", 
                      "Dispersiebereik (Smear)", "Waarnemingen", "Grens Studiegebied"),
    options = layersControlOptions(collapsed = FALSE)
  )
```

```{r export}
base_dir <- here("data/output/Mechelse_Heide/Rasters_Soorten")
folders <- list(
  potentie     = file.path(base_dir, "01_Maximale_Potentie"),
  werkelijk    = file.path(base_dir, "02_Werkelijke_Oppervlakte"),
  arpl         = file.path(base_dir, "03_ARPL"),
  verspreiding = file.path(base_dir, "04_Actuele_Verspreiding")
)

purrr::walk(folders, ~if (!dir.exists(.x)) dir.create(.x, recursive = TRUE))

if(length(ids_actueel_relevant) > 0) {
  arpl_export_rast <- cl_max %in% ids_actueel_relevant
  arpl_export_rast <- terra::ifel(arpl_export_rast, 1, NA)
} else {
  arpl_export_rast <- terra::rast(template_MH, vals = NA)
}

if(!all(is.na(terra::values(final_opp, mat=FALSE)))) {
  werkelijk_export_rast <- terra::ifel(!is.na(final_opp) & final_opp > 0, 1, NA)
} else {
  werkelijk_export_rast <- terra::rast(template_MH, vals = NA)
}

export_config <- list(
  list(rast = final_max,              naam = "Maximale_Potentie",        folder = folders$potentie,     type = "INT1U"),
  list(rast = werkelijk_export_rast, naam = "Werkelijke_Oppervlaktes", folder = folders$werkelijk,    type = "INT1U"), 
  list(rast = arpl_export_rast,      naam = "ARPL",                     folder = folders$arpl,         type = "INT1U"),
  list(rast = verspreiding_raster,   naam = "Actuele_Verspreiding",    folder = folders$verspreiding, type = "INT1U")
)

for(item in export_config) {
  file_path <- file.path(item$folder, paste0("Habitat_", item$naam, "_", soort, ".tif"))
  
  export_rast <- terra::deepcopy(item$rast)
  export_rast <- terra::mask(export_rast, area_buffer_fix)
  
  if(all(is.na(terra::values(export_rast, mat=FALSE)))) {
    export_rast <- terra::ifel(is.na(export_rast), 0, 0)
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  } else {
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  }
  
  writeRaster(export_rast, 
              filename = file_path, 
              overwrite = TRUE, 
              gdal = c("COMPRESS=LZW"), 
              datatype = item$type)
}
```
