---
title: "Leefgebiedenscript in R: De Maten"
author: "Bert Van Hecke"
date: "`r Sys.Date()`"
output: 
  html_document:
    self_contained: true
    toc: true
    theme: flatly
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)

library(here)
# CRUCIALE FIX: Dwing R Markdown om te werken vanaf de hoofdmap (arpl/)
# Hierdoor werken relatieve paden met hier() overal hetzelfde.
knitr::opts_knit$set(root.dir = here())

library(knitr)
library(tidyverse)
library(sf)
library(terra)
library(readxl)
library(tidyterra)
library(leaflet)
library(kableExtra)
library(data.table)

conflicted::conflicts_prefer(dplyr::filter)
conflicted::conflicts_prefer(dplyr::select)
conflicted::conflicts_prefer(dplyr::first)
conflicted::conflicts_prefer(terra::intersect)
conflicted::conflicts_prefer(terra::any)

# Exacte hectare-berekening behouden (cellSize voor 100% precisie)
calc_ha_exact <- function(r) {
  if(is.null(r)) return(0)
  if(all(is.na(terra::values(r, mat=FALSE)))) return(0)
  area_raster <- r * terra::cellSize(r, unit = "ha")
  val <- terra::global(area_raster, "sum", na.rm = TRUE)[[1]]
  return(as.numeric(val))
}

maak_kaart_laag <- function(res_list) {
  if(is.null(res_list)) return(NULL)
  kernen <- terra::ifel(!is.na(res_list$kern), 1, NA)
  if(!is.null(res_list$bouw)) {
    bouw <- terra::ifel(!is.na(res_list$bouw), 2, NA)
    finaal <- terra::cover(kernen, bouw)
  } else {
    finaal = kernen
  }
  return(finaal)
}

cluster_filter_compleet <- function(masker, opp_laag, drempel_m2, dist_m, werkelijk = FALSE) {
  if (terra::global(is.na(masker), "sum")[[1]] == terra::ncell(masker)) {
    return(list(raster = masker * NA, clusters = masker * NA))
  }
  
  temp_mask <- masker
  if(dist_m > 0) temp_mask <- terra::buffer(masker, width = dist_m / 2)
  
  cl <- terra::patches(temp_mask, directions = 8, zeroAsNA = TRUE) %>% terra::mask(masker)
  
  if(werkelijk) {
    stats_df <- terra::zonal(opp_laag, cl, fun = "sum", na.rm = TRUE)
    colnames(stats_df) <- c("ID", "Waarde")
    # CORRECTIE: Waarde is de som van fracties (1 pixel = 1.0). 
    # Vermenigvuldigen met 100 geeft de juiste Area in m2!
    stats_df$Area_m2 <- stats_df$Waarde * 100 
  } else {
    f <- terra::freq(cl)
    stats_df <- data.frame(ID = f$value, Waarde = f$count)
    stats_df$Area_m2 <- stats_df$Waarde * 100 # 1 binaire pixel = 100m2
  }
  
  stats_df <- stats_df[!is.na(stats_df$ID), ]
  if(nrow(stats_df) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  voldoet_ids <- stats_df$ID[stats_df$Area_m2 >= drempel_m2]
  if(length(voldoet_ids) == 0) return(list(raster = masker * NA, clusters = cl * NA))
  
  masker_binair <- cl %in% voldoet_ids
  final_mask <- terra::ifel(masker_binair == 1, 1, NA)
  
  return(list(raster = terra::mask(masker, final_mask), clusters = cl))
}
```

# Soort informatie ophalen
```{r Soort informatie}
df <- read_excel(here("data/input/Excel_files/Soorten_bwk_afstanden.xlsx"))
soort <- "watersnip"

resultaat <- df %>%
filter(Soort == soort) %>%
select(Type, MinOpp_ha, AfstandBiotopen_m, Dispersiecap_m)

# Variabelen definiëren
oppervlakte_ha <- resultaat$MinOpp_ha[1]
afstand_m <- resultaat$AfstandBiotopen_m[1]
buffer_m <- resultaat$Dispersiecap_m[1]

print(resultaat)
rm(df, resultaat)
```

# Gebied voorbereiden (De Maten)
```{r Gebied Voorbereiden}
area_shape  <- vect(here("data/input/De_Maten.shp"))
master_grid <- rast(here("data/input/Raster_Vlaanderen/Vlaanderen_MasterGrid_10m.tif"))[[1]]

# Inlezen van de CSV-sleutel (gebruikt om tabel-indices te mappen naar codes)
df_namen_sleutel <- read_csv(here("data/input/Excel_files/BWK_Laag_Namen_2025.csv"), show_col_types = FALSE)
gouden_namenlijst <- tolower(trimws(df_namen_sleutel$Laagnaam))

area_shape_proj <- project(area_shape, crs(master_grid))
area_buffer_fix <- buffer(area_shape_proj, width = buffer_m)

message("-> Vertaalraster voor globale/lokale cellen opbouwen via snelle MASK methode...")
id_raster_DM <- crop(master_grid, area_buffer_fix, snap = "near")

globale_id_raster <- master_grid
values(globale_id_raster) <- 1:ncell(globale_id_raster)

# ==============================================================================
# ULTRA-SNELLE METHODE: Direct naar data.frame met celnummers in C++
# ==============================================================================
id_raster_DM_globale_values <- crop(globale_id_raster, area_buffer_fix, snap = "near")
id_raster_DM_masked <- mask(id_raster_DM_globale_values, area_buffer_fix)

message("-> Vertaaltabel bliksemsnel opbouwen via C++ dataframe extractie...")

# 'cells = TRUE' zorgt ervoor dat terra direct een kolom 'cell' (lokale id) 
# en een kolom met de waarde (globale id) genereert voor alle niet-NA cellen.
df_extractie <- as.data.frame(id_raster_DM_masked, cells = TRUE)

# Direct omzetten naar data.table en de kolommen juist benoemen
vertaal_df <- as.data.table(df_extractie)

# De kolommen heten nu standaard 'cell' en de naam van je raster (lyr.1 of gelijkaardig)
# We hernoemen ze direct naar jouw vertrouwde structuur:
setnames(vertaal_df, c(1, 2), c("lokale_id", "globale_id"))

# Filter eventuele resterende NA's eruit
vertaal_df <- vertaal_df[!is.na(globale_id)]

studiegebied_globale_ids <- unique(vertaal_df$globale_id)

values(id_raster_DM) <- NA
# --- DE CRUCIALE TEMPLATE REPARATIE ---
# In plaats van alles op NA te zetten, maken we een binaire 'mal' van het studiegebied.
# We rasteriseren de shapefile van het De Maten (area_buffer_fix) op het uitgesneden raster.
template_DM <- terra::rasterize(area_buffer_fix, id_raster_DM, field = 1, background = 0)

# Controleer of het nu wél cellen bevat:
cat("Gecorrigeerd aantal pixels in template_DM: ", sum(terra::values(template_DM) == 1, na.rm=TRUE), "\n")

grens_web <- sf::st_as_sf(terra::project(area_shape, "EPSG:4326"))

rm(globale_id_raster, id_raster_DM_globale_values, id_raster_DM_masked)
gc()
```

# Biotoopfiltering (BWK-codes)
```{r Biotoopfiltering}
# 1. LAAD DE BRON-CROSSWALK/DICTIONARY IN
df_nieuw <- read_csv(here("data/input/Excel_files/Resultaten_Totaal_Samengevoegd.csv"), show_col_types = FALSE)

resultaten_gegroepeerd <- df_nieuw %>%
  mutate(Soort_clean = tolower(trimws(Soort))) %>%
  filter(Soort_clean == soort) %>%
  group_by(Type) %>%
  nest(Data = c(Code, Match))

# 2. LAAD DE REGIONALE TABEL IN
tabel_vlaanderen <- readRDS(here("data/input/Raster_Vlaanderen/BWK_Tabel_Turnhout_Plus_50km.rds"))
setDT(tabel_vlaanderen)
tabel_vlaanderen[, CODE := tolower(trimws(CODE))]

# Initialiseer de lijsten voor de verschillende habitat-bouwstenen
lijst_matches      <- list()
lijst_oppervlaktes <- list()

# 3. VERWERK ELK HABITAT-TYPE DYNAMISCH VIA DATA.TABLE
for(i in 1:nrow(resultaten_gegroepeerd)) {
  h_type <- resultaten_gegroepeerd$Type[i]
  h_data <- resultaten_gegroepeerd$Data[[i]]
  
  exact_codes <- tolower(trimws(h_data$Code[h_data$Match == "exact"]))
  bevat_codes <- tolower(trimws(h_data$Code[h_data$Match == "bevat"]))
  
  if(length(bevat_codes) > 0) {
    # 1. Ontsnap speciale regex-tekens (zoals haakjes) om syntaxfouten te voorkomen
    bevat_codes_escaped <- gsub("([\\.\\^\\$\\*\\+\\?\\(\\)\\[\\{\\\\\\|])", "\\\\\\1", bevat_codes)
    
    # 2. CRUCIALE FIX: Plak een '^' voor elke individuele code. 
    # Dit dwingt grepl() om alleen te matchen als de BWK-code BEGINT met deze letters.
    bevat_codes_anchored <- paste0("^", bevat_codes_escaped)
    regex_term <- paste0(bevat_codes_anchored, collapse = "|")
    
    # Filteren: EXACT-matches (geen letters voor/na) OF codes die BEGINNEN met de bevat-termen
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes | grepl(regex_term, CODE)]
  } else {
    tabel_gefilterd <- tabel_vlaanderen[CODE %in% exact_codes]
  }
  
  # Filter op studiegebied
  tabel_DM <- tabel_gefilterd[cel_id %in% studiegebied_globale_ids]
  tabel_TV_unique <- unique(tabel_TV, by = c("cel_id", "CODE"))
  tabel_cel_som <- tabel_TV_unique[, .(Oppervlakte = pmin(sum(BWK_FRAC, na.rm = TRUE), 1.0)), by = .(cel_id)]
  
  r_match_type <- id_raster_DM * NA
  r_opp_type   <- id_raster_DM * NA
  
  if(nrow(tabel_cel_som) > 0) {
    tabel_cel_som[, Oppervlakte := pmin(Oppervlakte, 1)]
    tabel_cel_som[, Match := ifelse(Oppervlakte >= 0.01, 1, 0)]
    
    setnames(tabel_cel_som, "cel_id", "globale_id")
    tabel_finaal_mapping <- merge(tabel_cel_som, vertaal_df, by = "globale_id", all.x = TRUE)
    tabel_finaal_mapping <- tabel_finaal_mapping[!is.na(lokale_id)]
    
    if(nrow(tabel_finaal_mapping) > 0) {
      tabel_matches_clean <- tabel_finaal_mapping[Match == 1]
      if(nrow(tabel_matches_clean) > 0) {
        r_match_type[tabel_matches_clean$lokale_id] <- tabel_matches_clean$Match
      }
      r_opp_type[tabel_finaal_mapping$lokale_id] <- tabel_finaal_mapping$Oppervlakte
      
      # Sla de gegenereerde rasters op
      lijst_matches[[h_type]]      <- r_match_type
      lijst_oppervlaktes[[h_type]] <- r_opp_type
    }
  }
}

# Uitpakken naar de exacte losse variabelen (Naadloze match met je database)
biotopen_max <- lijst_matches[["biotopen"]]
biotopen_opp <- lijst_oppervlaktes[["biotopen"]]

# Grote eindschoonmaak (vertaal_df weggooien mag nu pas!)
rm(resultaten_gegroepeerd, df_nieuw, lijst_matches, lijst_oppervlaktes)
gc()
```

```{r omgevingslagen watersnip}
# ==============================================================================
# STAP 1: OMGEVINGSFILTERS & PRIMAIRE CLUSTERING (10 HA, 100M BUFFER)
# ==============================================================================
message("-> Drainage en groen/waterkaarten inlezen en synchroniseren...")

r_drainage_raw <- rast(here("data/input/Raster_Vlaanderen/vlaanderen_drainage_10m.tif"))
r_groen_raw    <- rast(here("data/input/ASCI Files/Groenkaart_2021.tif"))
r_grb_raw      <- rast(here("data/input/Raster_Vlaanderen/vlaanderen_grb_water_10m.tif"))

# Extents en grids 100% matchen met het brongrid via resample
r_drainage <- terra::resample(r_drainage_raw, id_raster_DM, method = "near") %>% terra::crop(id_raster_DM)
r_groen    <- terra::resample(r_groen_raw, id_raster_DM, method = "near") %>% terra::crop(id_raster_DM)
r_grb      <- terra::resample(r_grb_raw, id_raster_DM, method = "near") %>% terra::crop(id_raster_DM)

# Drainagecorrectie (Drainageklasse d en natter, of code 0)
drain_cats           <- terra::cats(r_drainage)[[1]]
geselecteerde_letters <- c("d", "e", "f", "h", "i", "g", "e-f", "h-i", "e-i")
watersnip_drain_ids     <- drain_cats$value[drain_cats$Label %in% geselecteerde_letters]
watersnip_drainage_ok  <- r_drainage %in% watersnip_drain_ids

# Basisbiotoop combineren met de harde geografische randvoorwaarden
watersnip_basis_max <- (biotopen_max == 1) & watersnip_drainage_ok
watersnip_basis_max <- terra::ifel(watersnip_basis_max == 1, 1, NA)
watersnip_basis_opp <- terra::mask(biotopen_opp, watersnip_basis_max)

# Ruim direct de zware onbewerkte bronbestanden op
rm(r_drainage_raw, r_ecoregio_raw, r_groen_raw, r_grb_raw, watersnip_drainage_ok, watersnip_ecoregio_ok)
```


```{r primaire clustering watersnip}
# ==============================================================================
# STAP 2: PRIMAIRE CLUSTERING (10 HA, 100M BUFFER)
# ==============================================================================
message("-> Primaire clustering (100m fuzzy koppeling) en filteren op min. 10 ha...")

watersnip_clusters <- cluster_filter_compleet(
  masker       = watersnip_basis_max,
  opp_laag     = watersnip_basis_opp,
  drempel_m2   = 100000,   
  dist_m       = 100,       
  werkelijk    = FALSE      
)

r_cluster_ids <- watersnip_clusters$clusters
```

```{r kwaliteitscontrole watersnip}
# ==============================================================================
# STAP 3: LANDSCHAPSKWALITEIT CHECKS PER UNIEKE CLUSTER
# ==============================================================================
# Failsafe initialisatie van de tussentijdse ID-lijst
ids_kwaliteit_ok <- c()

if (!all(is.na(terra::values(r_cluster_ids, mat=FALSE)))) {
  
  message("-> Landschapschecks per cluster (openheid <75% bos & water <90%) berekenen...")
  
  # A. Totale oppervlakte (ha) per unieke cluster
  area_ha_raster <- r_cluster_ids * terra::cellSize(r_cluster_ids, unit = "ha")
  totale_opp_per_cluster <- terra::zonal(area_ha_raster, r_cluster_ids, fun = "sum", na.rm = TRUE)
  colnames(totale_opp_per_cluster) <- c("Cluster_ID", "Totaal_ha")
  
  # B. Aandeel bos/verbossing controleren (<75% verbossing)
  bos_masker <- terra::mask(r_groen == 1, r_cluster_ids)
  bos_ha_raster <- bos_masker * terra::cellSize(bos_masker, unit = "ha")
  bos_per_cluster <- terra::zonal(bos_ha_raster, r_cluster_ids, fun = "sum", na.rm = TRUE)
  colnames(bos_per_cluster) <- c("Cluster_ID", "Bos_ha")
  
  # C. Aandeel open water via GRB watergang evalueren (<90% open water)
  water_masker <- terra::mask(r_grb == 1, r_cluster_ids)
  water_ha_raster <- water_masker * terra::cellSize(water_masker, unit = "ha")
  water_per_cluster <- terra::zonal(water_ha_raster, r_cluster_ids, fun = "sum", na.rm = TRUE)
  colnames(water_per_cluster) <- c("Cluster_ID", "Water_ha")
  
  # Data samenvoegen voor de percentage-analyse
  cluster_stats <- merge(totale_opp_per_cluster, bos_per_cluster, by = "Cluster_ID", all.x = TRUE) %>%
    merge(water_per_cluster, by = "Cluster_ID", all.x = TRUE)
  
  cluster_stats[is.na(cluster_stats)] <- 0
  cluster_stats$Pct_Bos   <- (cluster_stats$Bos_ha / cluster_stats$Totaal_ha) * 100
  cluster_stats$Pct_Water <- (cluster_stats$Water_ha / cluster_stats$Totaal_ha) * 100
  
  # Selecteer de clusters die voldoen aan de openheids- en watereisen
  ids_kwaliteit_ok <- cluster_stats$Cluster_ID[cluster_stats$Pct_Bos < 75 & cluster_stats$Pct_Water < 90]
  
  # Tijdelijke objecten uit deze stap opschonen
  rm(area_ha_raster, totale_opp_per_cluster, bos_masker, bos_ha_raster, bos_per_cluster, water_masker, water_ha_raster, water_per_cluster, cluster_stats)
}
```

```{r kern en finale samenstelling}
# ==============================================================================
# STAP 4: TERRESTRISCHE KERN CHECK & FINALE SAMENVOEGING
# ==============================================================================
# Failsafe initialisatie van de eindrasters
watersnip_leefgebied_max <- template_DM * NA
watersnip_leefgebied_opp <- template_DM * NA

if (length(ids_kwaliteit_ok) > 0) {
  message("-> Check aaneengesloten terrestrische kern (min. 10 ha) binnen goedgekeurde clusters...")
  
  # Bepaling van de terrestrische laag (basisbiotoop zonder open water)
  r_terrestrisch_basis <- watersnip_basis_max & (r_grb == 0 | is.na(r_grb))
  r_terrestrisch_basis <- terra::ifel(r_terrestrisch_basis == 1, 1, NA)
  
  # Interne clustering om de terrestrische blokken te vinden (dist_m = 0)
  terrestrische_patches <- cluster_filter_compleet(
    masker       = r_terrestrisch_basis,
    opp_laag     = watersnip_basis_opp,
    drempel_m2   = 100000,   # Moet minimaal 10 ha (100.000 m²) groot zijn
    dist_m       = 0,       
    werkelijk    = FALSE      
  )
  
  r_terr_patch_ids <- terrestrische_patches$clusters
  
  if (!all(is.na(terra::values(r_terr_patch_ids, mat=FALSE)))) {
    message("-> Overlap matrices berekenen via snelle C++ unieke pixelcombinaties...")
    
    r_combinatie <- c(r_cluster_ids, r_terr_patch_ids)
    df_overlap <- as.data.frame(r_combinatie, na.rm = TRUE)
    
    if (nrow(df_overlap) > 0) {
      colnames(df_overlap) <- c("Cluster_ID", "Terr_Patch_ID")
      
      df_overlap_gefilterd <- df_overlap %>% 
        filter(Cluster_ID %in% ids_kwaliteit_ok)
      
      ids_finaal_goedgekeurd <- unique(df_overlap_gefilterd$Cluster_ID)
      
      if (length(ids_finaal_goedgekeurd) > 0) {
        message(paste("-> Finaal", length(ids_finaal_goedgekeurd), "cluster(s) voldoen aan ALLE biologische criteria."))
        
        watersnip_leefgebied_max <- terra::ifel(r_cluster_ids %in% ids_finaal_goedgekeurd, 1, NA)
        watersnip_leefgebied_opp <- terra::mask(watersnip_clusters$raster, watersnip_leefgebied_max)
      }
    }
    rm(r_combinatie, df_overlap)
  }
}

# Eind-synchronisatie en strak trekken op het studiegebied
watersnip_leefgebied_max <- terra::crop(watersnip_leefgebied_max, template_DM)
watersnip_leefgebied_opp <- terra::crop(watersnip_leefgebied_opp, template_DM)

# Grote eindschoonmaak van alle omgevingslagen en tussenproducten
rm(r_drainage, r_ecoregio, r_groen, r_grb, watersnip_basis_max, watersnip_basis_opp, 
   watersnip_clusters, r_cluster_ids, ids_kwaliteit_ok)
gc()
```

# Resultaten en Statistiek
```{r Resultaten en statistiek}
# --- RECHTSTREEKSE KOPPELING MET DE GEGENEREERDE RASTERS VAN DE WATERSNIP ---
opp_bron  <- watersnip_leefgebied_opp             
final_max <- watersnip_leefgebied_max         
final_opp <- watersnip_leefgebied_opp         

# Dynamisch pad naar de waarnemingen van de watersnip
waarnemingen_path <- here("data/input/Waarnemingen_Soorten/De_Maten/Waarnemingen_Watersnip.csv")

# ==============================================================================
# ECO-VISUELE FIX: Clusters definiëren via de echte 500m model-zoekstraal!
# ==============================================================================
if (!all(is.na(terra::values(final_max, mat=FALSE)))) {
  # 1. Vectoriseer de scherpe leefgebied-pixels
  p_ruw_max <- terra::as.polygons(final_max, aggregate = TRUE) %>% terra::disagg()
  
  # 2. Smelt elementen binnen 500m (breedte = 250m buffer) samen tot grote netwerkwolken
  p_buf_max        <- terra::buffer(p_ruw_max, width = 250)
  p_net_max_wolken <- terra::aggregate(p_buf_max) %>% terra::disagg()
  p_net_max_wolken$Netwerk_ID <- 1:nrow(p_net_max_wolken)
  
  # 3. Koppel de Netwerk-IDs terug aan een schoon raster (dit vervangt cl_max!)
  cl_max <- final_max * NA
  cl_max <- terra::rasterize(p_ruw_max, cl_max, field = 1, background = NA)
  
  # Breng de unieke Netwerk-IDs aan op het raster via een ruimtelijke relate
  p_ruw_max$Netwerk_ID <- terra::relate(p_ruw_max, p_net_max_wolken, "within") %>% 
    apply(1, function(x) { if(any(x)) which(x)[1] else NA })
  
  cl_max <- terra::rasterize(p_ruw_max, final_max * NA, field = "Netwerk_ID", touches = FALSE)
  
  rm(p_ruw_max, p_buf_max, p_net_max_wolken)
} else {
  cl_max <- final_max * NA
}

# Maak een binaire mal van het De Maten ZONDER de buffer
binnen_masker <- terra::rasterize(area_shape_proj, id_raster_DM, field = 1, background = 0)

# --- WAARNEMINGEN INLEZEN ---
if (file.exists(waarnemingen_path)) {
  waarnemingen_raw <- read_delim(waarnemingen_path, delim = ";", escape_double = FALSE, trim_ws = TRUE)
  df_points_sf <- st_as_sf(waarnemingen_raw, coords = c("x", "y"), crs = 31370)
  waarneming_smear_sf <- st_buffer(df_points_sf, dist = buffer_m) %>% st_union() %>% st_sf() 
  waarneming_blobs_vect  <- vect(waarneming_smear_sf)
  waarneming_punten_vect <- vect(df_points_sf)
  has_observations <- TRUE
} else {
  df_points_sf <- st_sf(st_sfc(crs = 31370)) 
  waarneming_smear_sf <- st_sf(st_sfc(crs = 31370))
  waarneming_blobs_vect  <- vect(st_sf(st_sfc(crs = 31370)))
  waarneming_punten_vect <- vect(st_sf(st_sfc(crs = 31370)))
  has_observations <- FALSE
}

verspreiding_raster <- terra::rast(template_DM, vals = NA)
verspreiding_netwerk_count <- 0

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  
  # 1. Totale binaire grootte van het cluster
  stats_max_raw <- terra::freq(cl_max)
  stats_max_raw <- as.data.frame(stats_max_raw)
  colnames(stats_max_raw) <- c("layer", "ID", "count")
  stats_max_raw$Pot_ha <- stats_max_raw$count * 0.01 
  
  # 2. Totale werkelijke habitat-oppervlaktes binnen deze clusters
  zonal_raw <- terra::zonal(opp_bron, cl_max, fun = "sum", na.rm = TRUE)
  zonal_raw <- as.data.frame(zonal_raw)
  colnames(zonal_raw) <- c("ID", "Werk_ha_raw")
  zonal_raw$Werk_ha <- zonal_raw$Werk_ha_raw * 0.01 
  
  stats_gecombineerd <- merge(stats_max_raw, zonal_raw, by = "ID", all.x = TRUE)
  stats_gecombineerd$Werk_ha[is.na(stats_gecombineerd$Werk_ha)] <- 0
  stats_gecombineerd$Werk_ha <- pmin(stats_gecombineerd$Werk_ha, stats_gecombineerd$Pot_ha)

  # 3. Welke clusters raken het De Maten
  zonal_zone <- terra::zonal(binnen_masker, cl_max, fun = "max", na.rm = TRUE)
  zonal_zone <- as.data.frame(zonal_zone)
  colnames(zonal_zone) <- c("ID", "In_DM")
  ids_die_DM_raken <- zonal_zone$ID[!is.na(zonal_zone$In_DM) & zonal_zone$In_DM > 0]
  
  # 4. DYNAMISCHE FILTERING
  ids_potentieel_totaal <- stats_gecombineerd$ID
  ids_werkelijk_totaal  <- stats_gecombineerd$ID[stats_gecombineerd$Werk_ha >= oppervlakte_ha]
  
  # 5. Ondeelbare opsplitsing naar Binnen/Buiten
  ids_pot_bin   <- intersect(ids_potentieel_totaal, ids_die_DM_raken)
  ids_pot_buit  <- setdiff(ids_potentieel_totaal, ids_die_DM_raken)
  
  ids_werk_bin  <- intersect(ids_werkelijk_totaal, ids_die_DM_raken)
  ids_werk_buit <- setdiff(ids_werkelijk_totaal, ids_die_DM_raken)
  
  # A. ARPL Extractie
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_max <- terra::extract(cl_max, waarneming_blobs_vect, ID = FALSE)
    ids_raken_waarneming <- if(nrow(extract_ids_max) > 0) unique(na.omit(extract_ids_max[[1]])) else c()
  } else {
    ids_raken_waarneming <- c()
  }
  ids_actueel_relevant <- intersect(ids_werkelijk_totaal, ids_raken_waarneming)

  # B. Actuele Verspreiding Extractie
  if(has_observations && nrow(df_points_sf) > 0) {
    extract_ids_verspr <- terra::extract(cl_max, waarneming_punten_vect, ID = FALSE)
    ids_raken_punten <- if(nrow(extract_ids_verspr) > 0) unique(na.omit(extract_ids_verspr[[1]])) else c()
  } else {
    ids_raken_punten <- c()
  }
  ids_verspreiding_clusters <- intersect(ids_werkelijk_totaal, ids_raken_punten)
  
  if(length(ids_verspreiding_clusters) > 0) {
    masker_verspr <- cl_max %in% ids_verspreiding_clusters
    clusters_actueel <- terra::mask(final_max, masker_verspr)
    verspreiding_raster <- terra::ifel(!is.na(clusters_actueel), 1, NA)
    verspreiding_netwerk_count <- length(ids_verspreiding_clusters)
  }

  # --- BEREKENING HECTARES VOOR TABEL ---
  ha_pot_bin      <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_bin]), 2)
  ha_pot_buit     <- round(sum(stats_gecombineerd$Pot_ha[stats_gecombineerd$ID %in% ids_pot_buit]), 2)
  ha_werk_bin     <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_bin]), 2)
  ha_werk_buit    <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_werk_buit]), 2)
  ha_arpl         <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_actueel_relevant]), 2)
  ha_verspreiding <- round(sum(stats_gecombineerd$Werk_ha[stats_gecombineerd$ID %in% ids_verspreiding_clusters]), 2)

  if(length(ids_actueel_relevant) > 0) {
    arpl_raster <- terra::match(cl_max, ids_actueel_relevant)
    arpl_raster <- (!is.na(arpl_raster)) * 1
  } else {
    arpl_raster <- terra::rast(cl_max, vals = NA)
  }

} else {
  ids_pot_bin <- ids_pot_buit <- ids_werk_bin <- ids_werk_buit <- ids_actueel_relevant <- c()
  ha_pot_bin <- ha_pot_buit <- ha_werk_bin <- ha_werk_buit <- ha_arpl <- ha_verspreiding <- 0
  arpl_raster <- terra::rast(template_DM, vals = NA)
}

tabel_totaal <- data.frame(
  Type = c("Maximaal Potentieel (Binnen)", "Maximaal Potentieel (Buiten)", 
           "Werkelijk Habitat (Binnen)", "Werkelijk Habitat (Buiten)",
           "Actueel Relevant (ARPL - Werkelijke ha)", "Actuele Verspreiding (Werkelijke ha)"),
  Clusters = c(length(ids_pot_bin), length(ids_pot_buit), 
               length(ids_werk_bin), length(ids_werk_buit), 
               length(ids_actueel_relevant), verspreiding_netwerk_count),
  Oppervlakte_ha = c(ha_pot_bin, ha_pot_buit, ha_werk_bin, ha_werk_buit, ha_arpl, ha_verspreiding)
)

tabel_totaal %>% 
  knitr::kable(caption = paste("Totaaloverzicht habitat & verspreiding voor de", soort)) %>% 
  kableExtra::kable_styling(bootstrap_options = c("striped", "hover"))
```

# Interactieve kaart
```{r Interactieve kaart}
if (has_observations && nrow(df_points_sf) > 0) {
  waarneming_smear_web <- st_transform(waarneming_smear_sf, 4326)
  points_web           <- st_transform(df_points_sf, 4326)
} else {
  waarneming_smear_web <- st_sf(st_sfc(crs = 4326))
  points_web           <- st_sf(st_sfc(crs = 4326))
}

if (!all(is.na(terra::values(cl_max, mat=FALSE)))) {
  cluster_info <- stats_gecombineerd %>%
    left_join(zonal_zone, by = "ID") %>% 
    mutate(
      Zone = ifelse(!is.na(In_DM) & In_DM > 0, "Binnen DM", "Buiten DM (Buffer)"),
      ARPL = ID %in% ids_actueel_relevant,
      Type = ifelse(ID %in% ids_werkelijk_totaal, "Werkelijk Habitat", "Potentieel (Niet Werkelijk)"),
      Popup = paste0(
        "<div style='font-family: Arial; min-width: 180px;'>",
        "<strong>Cluster ID:</strong> ", ID, "<br>",
        "<strong>Zone:</strong> ", Zone, "<br><hr>",
        "<strong>Status:</strong> ", Type, "<br>",
        "<strong>Actueel Relevant (ARPL):</strong> ", 
        ifelse(ARPL, "<span style='color:green; font-weight:bold;'>JA</span>", 
                     "<span style='color:red;'>NEE (Geen waarneming of onder drempel)</span>"), "<br><br>",
        "<strong>Potentieel Oppervlak:</strong> ", round(Pot_ha, 2), " ha<br>",
        "<strong>Werkelijk Habitat:</strong> ", round(coalesce(Werk_ha, 0), 2), " ha",
        "</div>"
      )
    )

  cl_poly <- terra::as.polygons(cl_max, aggregate = TRUE) %>% 
    st_as_sf() %>% 
    st_transform(4326)

  cl_poly_final <- cl_poly %>%
    inner_join(cluster_info, by = setNames("ID", names(cl_poly)[1]))
} else {
  cl_poly_final <- st_sf(st_sfc(crs = 4326))
}

r_status <- terra::ifel(!is.na(final_opp), 1, terra::ifel(!is.na(final_max), 2, NA))
r_web_status         <- terra::project(r_status, "EPSG:4326", method = "near")
grens_web            <- st_transform(st_as_sf(area_shape_proj), 4326)
bbox                 <- st_bbox(grens_web)

pal_status <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = c("Werkelijk Habitat", "Potentieel (Niet Werkelijk)"))
pal_4 <- colorFactor(palette = c("#4daf4a", "#e41a1c"), levels = 1:2, na.color = "transparent")

map <- leaflet() %>%
  addTiles(group = "OSM (Kaart)") %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelliet") %>%
  fitBounds(bbox[['xmin']], bbox[['ymin']], bbox[['xmax']], bbox[['ymax']])

if(!all(is.na(terra::values(r_web_status, mat=FALSE)))) {
  map <- map %>% addRasterImage(terra::as.int(r_web_status), colors = pal_4, opacity = 0.5, group = "Habitat Status (Pixels)")
}

if(nrow(cl_poly_final) > 0) {
  map <- map %>% addPolygons(
    data = cl_poly_final,
    fillColor = ~pal_status(Type),
    fillOpacity = ~ifelse(ARPL, 0.6, 0.1), 
    color = ~pal_status(Type), 
    weight = ~ifelse(ARPL, 3, 0.5), 
    opacity = 1,
    popup = ~Popup,
    group = "Leefgebieden"
  )
}

if(has_observations && nrow(df_points_sf) > 0) {
  map <- map %>%
    addPolygons(
      data = waarneming_smear_web,  
      color = "#377eb8", 
      weight = 2, 
      fill = FALSE,       
      dashArray = "10, 5", 
      group = "Dispersiebereik (Smear)"
    ) %>%
    addCircles(
      data = points_web,  
      color = "black", weight = 1, fillColor = "yellow", fillOpacity = 1,
      radius = 15, group = "Waarnemingen"
    )
}

map %>%
  addPolygons(data = grens_web, fill = FALSE, color = "black", weight = 3, group = "Grens Studiegebied") %>%
  addLayersControl(
    baseGroups = c("OSM (Kaart)", "Satelliet"),
    overlayGroups = c("Leefgebieden", "Habitat Status (Pixels)", 
                      "Dispersiebereik (Smear)", "Waarnemingen", "Grens Studiegebied"),
    options = layersControlOptions(collapsed = FALSE)
  )
```

```{r export}
base_dir <- here("data/output/De_Maten/Rasters_Soorten")
folders <- list(
  potentie     = file.path(base_dir, "01_Maximale_Potentie"),
  werkelijk    = file.path(base_dir, "02_Werkelijke_Oppervlakte"),
  arpl         = file.path(base_dir, "03_ARPL"),
  verspreiding = file.path(base_dir, "04_Actuele_Verspreiding")
)

purrr::walk(folders, ~if (!dir.exists(.x)) dir.create(.x, recursive = TRUE))

if(length(ids_actueel_relevant) > 0) {
  arpl_export_rast <- cl_max %in% ids_actueel_relevant
  arpl_export_rast <- terra::ifel(arpl_export_rast, 1, NA)
} else {
  arpl_export_rast <- terra::rast(template_DM, vals = NA)
}

if(!all(is.na(terra::values(final_opp, mat=FALSE)))) {
  werkelijk_export_rast <- terra::ifel(!is.na(final_opp) & final_opp > 0, 1, NA)
} else {
  werkelijk_export_rast <- terra::rast(template_DM, vals = NA)
}

export_config <- list(
  list(rast = final_max,              naam = "Maximale_Potentie",        folder = folders$potentie,     type = "INT1U"),
  list(rast = werkelijk_export_rast, naam = "Werkelijke_Oppervlaktes", folder = folders$werkelijk,    type = "INT1U"), 
  list(rast = arpl_export_rast,      naam = "ARPL",                     folder = folders$arpl,        type = "INT1U"),
  list(rast = verspreiding_raster,   naam = "Actuele_Verspreiding",    folder = folders$verspreiding, type = "INT1U")
)

for(item in export_config) {
  file_path <- file.path(item$folder, paste0("Habitat_", item$naam, "_", soort, ".tif"))
  
  export_rast <- terra::deepcopy(item$rast)
  export_rast <- terra::mask(export_rast, area_buffer_fix)
  
  if(all(is.na(terra::values(export_rast, mat=FALSE)))) {
    export_rast <- terra::ifel(is.na(export_rast), 0, 0)
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  } else {
    export_rast <- terra::mask(export_rast, area_buffer_fix, updatevalue = 0)
  }
  
  writeRaster(export_rast, 
              filename = file_path, 
              overwrite = TRUE, 
              gdal = c("COMPRESS=LZW"), 
              datatype = item$type)
}
```
